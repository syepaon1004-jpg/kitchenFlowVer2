# KitchenFlow — 마지막 세션 상태

> 세션 종료 시 업데이트. 다음 세션 시작 시 이어하기용.

## 마지막 작업
TASK-20260422-213: **Phase 4 closeout — `/practice/session/:sessionId` main route 를 shared-kitchen session surface 로 전환 + old text-first page 를 fallback 전용 파일로 분리**. Plan rev6 (Evaluator 6 rounds: F1 GameKitchenView empty props 불가 → store-level panel assets DB fetch, F2 useGameTick 도입 시 stale shared runtime state tick 위험 → `useGameTick` 미도입(adapter.onRuntimeTick no-op seam 만 유지), F3 LegalAction↔Intent shape mismatch → place stub `location_ref`, action `null`, pour dispatch 제외, F4 `panel_layouts` multi-row silent 축소 → `layouts.length === 1` fail-closed null, F5 `assets == null` loading/unsupported 이중 의미 → tri-state `{loading | ready | unsupported}` 분리, F6 literal `:sessionId` 링크 → `` `/practice/session/${sessionId}/legacy` `` 보간, F7 `ReadonlyMap` ↔ `Map` 계약 불일치 → `PracticeKitchenAssets.ingredientLabels: Map<string, string>`) approved. 변경 파일 8: (a) `src/router.tsx` — `PracticeSessionFallbackPage` import + `/practice/session/:sessionId/legacy` 경로 추가(requireStore+requireUser 블록). (b) 신규 `src/pages/practice/PracticeSessionFallbackPage.tsx` — 구 `PracticeSessionPage` body 그대로 복사 (formatter + guide toggle + tacit panel + next preview 유지, 새 기능 추가 0). (c) `src/pages/practice/PracticeSessionPage.tsx` — 전체 재작성. lifecycle `useParams + useAuthStore(selectedStore/selectedUser) + usePracticeStore(phase/session/error/derived?.isComplete + action refs) + useMemo(createPracticeModeAdapter) + adapter.boot({store_id, user_id, mode:'practice', practice_menu_id: session.menu_id}) when phase==='active'`. assetsState 는 `fetched: {storeId, result}` + derived tri-state(setState-in-effect 회피). Active 렌더 3 분기(loading 스피너 / unsupported F4 메시지 + `/practice/session/${sessionId}/legacy` Link / ready = `KitchenModeAdapterProvider + SharedKitchenShell(hudSlots=SharedKitchenHudSlots{topLeft=PracticeHudProgress, modeSummary=PracticeHudStepGroup, bottomLeft=PracticePrimaryActionTrigger}, overlaySlots=PracticeRejectionToast) > GameKitchenView(real panel_layout+equipment+items+ingredientLabels, empty wokContentsMap/placedContainers/panelToStateIdMap, onSceneClick noop)`). 하단 완료/포기/legacy/연습목록 버튼. 제거: `formatLegalAction` · `formatFriendlyAction` · `pickRepresentativeAction` · `GuideIntensity` · `buildLocationLabelMap` · `buildTacitDetailViewModel` · `buildNextGroupPreview` · `fetchIngredientDisplayNames` · `PracticeTacitItem` · `PracticeTacitMedia` · `LegalAction` · store action `placeIngredient`/`executeAction`/`pour` 직접 dispatch · `useGameTick` · guide intensity state · tacit fetch. (d) 신규 `src/pages/practice/practiceKitchenAssets.ts` — `fetchPracticeKitchenAssets(storeId)` single-row fail-closed helper. `panel_layouts` 전체 fetch → `layouts.length !== 1` 이면 `null` 반환, 1 이면 그 layout 의 `panel_equipment` + `panel_items` + `store_ingredients` 를 병렬 fetch 해 `{layout, equipment, items, ingredientLabels: Map<string,string>}` 반환. `setEquipments`/`setStoreIngredientsMap`/`game_equipment_state` write 0. (e) 신규 `src/components/practice/PracticeSessionHud.tsx` — 4 component: `PracticeHudProgress`(getHudModel title+progress), `PracticeHudStepGroup`(getPrimaryStepGroup + getCurrentStepGroups 의 non-primary 를 next 로), `PracticePrimaryActionTrigger`(`pickDispatchableLegalAction` + `dispatchLegalAction` 사용, pour-only 면 disabled + legacy 안내 텍스트), `PracticeRejectionToast`(rejection null 이면 null return). `useKitchenModeAdapter` 만 경계로 사용, text formatter 0 호출. (f) 신규 `src/components/practice/PracticeSessionHud.module.css` — gameVariables 토큰(`var(--spacing-*)`/`var(--color-*)`/`var(--radius-*)`/`var(--font-*)`/`var(--game-*)`)만 사용, 하드코딩 px/color 0, `display:none` 0, 3D transform 0. (g) 신규 `src/components/practice/practiceDispatch.ts` — pure helper 2종(`pickDispatchableLegalAction(legals)` / `dispatchLegalAction(adapter, action)`). F3 dispatch rule 내재화. (h) 신규 `src/components/practice/__tests__/practiceDispatch.test.ts` — vitest 8 케이스(pick 5 + dispatch 3) F3 drift guard. ⚠️ **기술 편차 2건(self-reviewer 정당화됨)**: (T) plan 이 `PracticePrimaryActionTrigger.test.tsx` component test 를 명시했으나 `@testing-library/react`/`@testing-library/jest-dom` 미설치 + `vitest.config.ts` `environment: 'node'` + `include: ['src/**/__tests__/**/*.test.ts']`(`.test.ts` 전용) 구성으로 component test 실행 불가 → pure helper `.test.ts` 로 등가 대체. dependency 추가는 `forbidden.md` "확인 없이 패키지 설치/삭제 금지" 위반. (H) plan 단계 5 에서 helpers 를 `PracticeSessionHud.tsx` 내부 export 로 두려 했으나 eslint `react-refresh/only-export-components` 가 `.tsx` 의 non-component export 를 금지 → `practiceDispatch.ts` 별도 파일 분리. 지시서 `files` 의 `src/components/practice/*` glob 범위 내. **제약 준수**: sim 경로 0 touch(`GamePage.tsx`/`createSimModeAdapter.ts`/`useGameTick.ts`/`GameKitchenView.tsx`/`equipmentStore.ts`/`gameStore.ts` 전부 unmodified), `KitchenModeAdapter.ts` 계약 불변, `.harness/*.json` 0 touch, `docs/commander/*` 0 touch, shared-shell 0 touch, admin↔game import 0, Zustand 인라인 filter/map 0, `display:none` 0, CSS 인라인 하드코딩 0, `any`/`@ts-ignore`/`eslint-disable` 0. main route grep: `rg "formatLegalAction|formatFriendlyAction|pickRepresentativeAction|useGameTick|useOrderGenerator|useRecipeEval|useScoringStore|createSimModeAdapter|setEquipments|game_equipment_state" src/pages/practice/PracticeSessionPage.tsx src/components/practice` → 0. `rg "usePracticeStore|createPracticeModeAdapter" src/components/game/shared-shell` → 0. **Phase 4 순서 전부 pass**: `npm run build`(223 modules, exit 0, 835.43→849.02 KB +13.59 KB 신규 화면/adapter 경계), `npm run lint` 0 errors(pre-existing `useGameTick.ts:52` warning 1건 — sim territory, 이 task 범위 밖), `@self-reviewer` 2차 APPROVED(1차: persistInFlight/persistError 구독 + 테스트 형식/helper 분리 3건 지적 → 1번 제거 + 2·3번 기술 사유 정당화 후 2차에서 9/9 충족 금지사항 위반 0 추가 변경 0 판정 이상 없음), `npx tsc --noEmit` 0 errors, 신규 `npx vitest run src/components/practice/__tests__/practiceDispatch.test.ts` 8/8 pass. **LEARNINGS 갱신**: 2 건 추가(F2 근거 — `equipmentStore` reset 부재 상황에서 `useGameTick` 도입 금지 / `.test.tsx` + react-refresh/only-export-components 제약 하의 pure helper 분리 테스트 패턴). **MISTAKES**: 해당 없음(1차 self-review 의 persistInFlight/persistError scope 확장은 동일 세션 내 수정). ⚠️ 고위험 항목 전부 검증: route-level session surface 전환 / shared kitchen interaction wiring(HUD affordance) / sim regression 검증 / self-reviewer 필수 전부 이행. **3차 Evaluator High 지적(HUD 비반응성) 대응 추가 수정**: (i) `PracticeSessionPage.tsx` 의 store 구독을 `derived?.isComplete ?? false` scalar 에서 `s.derived` object ref 로 교체 + `isComplete` 를 파생으로 사용. practiceStore 가 매 try* 성공마다 `computeDerivedData` 로 새 `derived` 객체를 `set` 하므로 object ref 비교에서 불일치 → page re-render → HUD children 도 re-render → adapter imperative 호출이 fresh 값으로 수렴. (ii) `PracticeSessionHud.tsx` 의 `PracticePrimaryActionTrigger` 에서 `useMemo([adapter])` 제거(`useMemo`·`LegalAction` import 삭제). adapter identity 가 안정이라 memo 가 첫 계산값을 영구 캐시해 stale dispatchable 이 동결되던 문제 해소. 매 렌더마다 `enumerateLegalActions` + `pickDispatchableLegalAction` 재호출(pure·cheap). 재검증: build/lint/tsc/vitest 모두 pass. 3차 `@self-reviewer` APPROVED(요구사항 10/10, 금지사항 위반 0, 추가 변경 0, 판정 이상 없음, 버그 3건 모두 논리적으로 해소).

## 직전 작업
TASK-20260422-212: **Phase 3 단일 task — practice adapter 실동화 + engine reason 세분화 + sessionView formatter 분리**. Gate B 에서 남겨둔 translation gap 을 옵션 A (engine reason 세분화) 로 해결. Evaluator 1 차 지적(High 2 + Low 1) 반영 후 plan approved. 변경 파일 17: 엔진 6(`types.ts` reason union 재정의 + `hasNonDecoBaseAt`/`getLocationPathTerminalId` helper / `ingredientAdd.ts` 5 분기 `no-open-number|no-candidate-node|duplicate-phase-entry|location-mismatch|deco-requires-base` + `resolvePlaceBinding` export / `actionExecute.ts` 2 분기 `no-open-number|no-candidate-action` / `pourDryRun.ts` 5 분기 `source-not-clean|pour-step-not-open|pour-no-movable-instances|pour-deco-requires-base|dry-run-blocked` + §14.4 empty-payload success branch + `collectPourCandidateEntries`/`hasPhysicalPayloadAt` export / `legalActions.ts` deco-first gate 공용 helper + `(src, tgt)` pour 쌍 iteration 으로 §14.4 enumerate 일치 / `index.ts` 신규 helper export), adapter 1(`createPracticeModeAdapter.ts` skeleton → 실동 thin adapter 전면 재작성: boot 에서 locationIndex/rejection clear + DB write 0, onRuntimeTick no-op, getHudModel 에 bundle.menu.name + derived progress, getOpenStep 에서 locationIndex resolve + `computeOpenNumber`, enumerateLegalActions 에서 engine `LegalAction` → adapter `LegalAction` 번역 `resolvePlaceBinding`/`collectPourCandidateEntries` 로 try\* 와 1:1, tryPlace/tryPerform/tryPour 에서 locationKey resolve + store action 호출 + reason 번역, source-not-clean·dry-run-blocked 는 `rejection_code=undefined` + `lastRejection=null` 로 engine-internal 유지 Gate B §3.5 준수, getCurrentStepGroups 에서 `getCurrentStepGroup`/`getNextStepGroup` 래핑, getGhostGuide null Phase 5 범위), store 1(`practiceStore.ts` 3 action 시그니처 `void → PlaceResult|ActionResult|PourResult + NotActiveResult sentinel` union 확장. 내부 mutation 로직 불변. adapter import 0 유지), sessionView 분리 2(`sessionView.ts` formatter 5 심볼(`formatLegalAction|formatFriendlyAction|pickRepresentativeAction|ACTION_TYPE_LABELS|GuideIntensity`) 제거 VM calculator 8 종만 유지 / 신규 `sessionTextFormat.ts` text-first fallback 헤더 주석 "adapter MUST NOT import from here" 포함), 호출측 2(`PracticeSessionPage.tsx` import path 2 줄만 `sessionView` → `sessionTextFormat`, 기능 변화 0 / `PracticeAdminPage.tsx` 동일 compile-forced passthrough plan write scope 에 명시 포함), 테스트 6(`ingredientAdd.test.ts` (8) rename + 신규 (8b)(8c)(8d) `no-candidate-node`/`duplicate-phase-entry`/`location-mismatch`/`deco-requires-base` / `phaseAdvance.test.ts` (17b) rename `no-candidate-action` / `pourDryRun.test.ts` (12) rename + 신규 (12b)(12c)(12d) `pour-no-movable-instances`/`pour-step-not-open`/§14.4 success/`pour-deco-requires-base` / `legalActions.test.ts` 신규 (20) deco terminal gate + (21) §14.4 enumerate = tryPour 1:1 / `sessionView.test.ts` formatter describe 3 블록 제거 + 신규 `sessionTextFormat.test.ts` 로 이동(12 tests 100% 보존) / `practiceStore.test.ts` return-value 3 건 단언 추가 `{allowed:true,boundNodeId}`/`{allowed:false,reason:'no-candidate-node'}`/`{allowed:false,reason:'not-active'}` / 신규 `createPracticeModeAdapter.test.ts` 19 시나리오: lifecycle/place translation 4/action translation 2/pour translation 4 + engine-internal undefined/deco-first terminal/enumerate↔try\* 모순 0/rejection lifecycle). 신규 3 파일: `sessionTextFormat.ts` + `sessionTextFormat.test.ts` + `createPracticeModeAdapter.test.ts`. **제약 준수**: sim 0-touch 7 파일(`GamePage.tsx`/`createSimModeAdapter.ts`/`useGameTick.ts`/`equipmentStore.ts`/`gameStore.ts`/`scoringStore.ts`/`components/game/shared-shell/*`) 수정 0, `KitchenModeAdapter.ts` 계약 불변, `.harness/*.json` 0-touch, `docs/commander/*` 0-touch, `PracticeSessionPage.tsx` shared-shell 심볼 import 0 + 기능 확장 0, adapter boot/tick 에서 DB write 0, Zustand 인라인 filter/map 0, `as any`/`@ts-ignore`/`eslint-disable` 0, admin↔game import 위반 0. **Phase 4 고정 순서**: `npm run build` pass(834.35 → 835.43 KB, +1.08 KB), `npm run lint` pass(0 errors / 1 pre-existing warning `useGameTick.ts:52`), `@self-reviewer` APPROVED(요구사항 19/19, 금지사항 위반 0, extra_changes 0 — PracticeAdminPage 는 plan §변경 파일 12 번에 명시 in-scope), `npx tsc --noEmit` exit 0. self-reviewer 개선 제안(비차단) 중 `as StoreReason` 이중 단언 제거 반영 — `StorePlaceResult` union 이 이미 reason 타입 좁혀주므로 불필요 확인 후 삭제. `npx vitest run` 203 tests / 11 files 전부 pass(기존 120 + 신규 83). **plan/execution rounds**: plan 2 rounds(1 차 Evaluator High 2 지적: §14.4 enumerate 누락 + source-not-clean/dry-run-blocked pass-through 금지 위반 + Low 1: PracticeAdminPage write scope 미명시 → 3 건 반영 후 재제출 approved), execution 1 round. **LEARNINGS 갱신 예정**: 2 건(engine reason 세분화 vs adapter preflight 선택 근거 / §14.4 enumerate 를 `(src,tgt)` 쌍 iteration 으로 try\* 와 1:1 잠그는 구조). **MISTAKES**: 해당 없음 (신규 실수 반복 0). ⚠️ 고위험 항목 전부 적용: engine reason/refusal 3 파일 동시 변경 + adapter/store 경계 변경 + 17 파일 수정 + deco-first helper 공유 + §14.4 신규 success branch + `@self-reviewer` 필수.

## 직전 작업

**최신 truth 요약 (TASK-20260420-160 기준)**
- practice 트랙은 TASK-160 에서 `finalizeSession('completed'|'abandoned')` 공통화로 본선 마감됨
- admin 8 경로(correction / delete / link / unlink / tacit / media / bootstrap preview / 로컬 dry-run simulator) 전부 개통
- completeSession/abandonSession 대칭 happy + flush-fail fail-closed 테스트 확보
- production 기능 공백은 현재 기준 관측되지 않음

**현재 practice 코드 상태 핵심 사실**
- `src/pages/practice/PracticeAdminPage.tsx` — 8 authoring 경로 전부 존재 (correction/delete/link/unlink/tacit/media/bootstrap preview/dry-run simulator)
- `src/pages/practice/PracticeAdminPage.tsx` — `commitStepNoEdit` helper 로 action/ingredient parent `step_no` save-path 공통화 완료
- `src/stores/practiceStore.ts` — `finalizeSession('completed'|'abandoned')` helper 가 completeSession/abandonSession 양쪽 위임 수용
- `src/stores/__tests__/practiceStore.test.ts` — completeSession/abandonSession 둘 다 happy + flush-fail fail-closed 대칭 proof 존재

**[남은 작업 로드맵/계획서]**
- practice 트랙에서 필수 작업 없음. 잠재 bugfix 후보도 현재 관측되지 않음
- 신규 feature 추가가 필요하면 **별도 신규 지시서**로 열어야 함(무지시 확장 금지)

**Stop-line**
practice에서 사실상 남은 필수 작업이 없다. 이후는 stop 또는 future bugfix only.

## 직전 작업
TASK-20260422-211: **Gate B 결정 문서 고정 (planning slice only, 코드 0 touch)**. Phase 3 본 구현 진입 전 닫혀야 하는 5개 축을 실제 코드 ground truth 기준으로 단일 결정 문서에 고정. Gate A의 `PHASE2_SKELETON_DRAFT_2026-04-21.md` 선례를 따라 단일 파일 1개로 닫음. 변경 파일 1: `docs/practice/GATE_B_DECISIONS_2026-04-22.md`(신규, 6 섹션: Fixed Invariants / Current Code Ground Truth Snapshot / Gate B Decision Axis 1–5 / Rejection Code Mapping / Phase 3 Slicing Recommendation (proposal only) / Explicit Non-Goals · Still Blocked). 허용 보조: `docs/worker/LAST_SESSION.md`(본 기록) 1건만 touch, LEARNINGS/MISTAKES 해당 없음(신규 패턴/실수 0). **Ground truth 직접 확인 5축**: (1) `src/lib/practice/engine/*` 8파일 reason literal 검증 — `ingredientAdd.ts` `'no-open-number'` L22·`'ingredient-mismatch'` L36·`'location-mismatch'` L47-48, `actionExecute.ts` `'no-open-number'` L20·`'action-mismatch'` L31, `pourDryRun.ts` `'source-not-clean'` L30·`'no-candidates'` L40·`'dry-run-blocked'` L54·L73·L76. spec-level rejection_code 10종 전부 engine에 0건 부재 확인. (2) `runtime.ts` DB↔engine bridge 책임 확인(`hydrateEngineState`/`dehydrateInstances`/`dehydrateProgress`/`computeDerivedData`). (3) `practiceStore.ts` 8개 액션·bootstrap/resume/persist-queue/finalize/reset/phase 상태머신 책임 확인, 텍스트 포매팅 부재 확인. (4) `sessionView.ts` VM calculator 8종 + 텍스트 포매터 2종(`formatLegalAction`/`formatFriendlyAction`) 분리 확인, `"open_step"/"deco"/"base"` 키워드 미등장 확인. (5) `PracticeSessionPage.tsx` full functional UI 구조 확인(placeholder 아님). 경계 파일 추가 확인: `KitchenModeAdapter.ts` 14 멤버 계약 + `ActionResult.rejection_code: string` 계약, `createPracticeModeAdapter.ts` 완전 skeleton(`'skeleton_stub'` 반환, practice/* import 0). **Gate B 결정 5축 substance**: Axis 1(adapter 책임 분할) — 소유 5항목(LocationKey 매핑/legalActions 변환/rejection 번역/VM 래핑/intent 위임) + 비소유 5항목(persistence/DB write/React 렌더/텍스트 생성/local UX state) + identity 안정성(route-level `useMemo([])` 1회 생성) 고정. Axis 2(store 재사용 범위) — 유지 6항목(bootstrap/persist-queue/finalize/reset/engineState 컨테이너/derived) + store는 adapter를 import하지 않음 + adapter→store read-only snapshot + action call 단방향 흐름 고정. Axis 3(sessionView 분리) — VM calculator 8종 Phase 3 재사용 확정, 텍스트 포매터 4종(`formatLegalAction`/`formatFriendlyAction`/`pickRepresentativeAction`/`ACTION_TYPE_LABELS`)는 shared-kitchen UI 최종 자산 미승인/fallback 한정. Axis 4(PracticeSessionPage 취급) — Phase 3 내내 fallback/transition asset, 확장 금지 4항목(새 UI 기능/store·adapter surface/shared-shell 심볼/text-first formatter 확장), 유지 허용 4항목(route mount/resume·start 부트스트랩/persist error 표시/완료·중단 배선), Phase 4 이후 삭제 또는 redirect 제안(본 task 확정하지 않음). Axis 5(rejection_code 결정 트리) — **핵심 전제 "현재 engine reason만으로는 spec code를 완전 복원할 수 없다"** 를 Gate B 산출물로 고정. 엔진 place/action은 step_no 좁히기+ID 매치+satisfied 필터를 한 분기에 묶어 `ingredient-mismatch`가 spec `no_candidate_node`+`duplicate_phase_entry`를, `action-mismatch`가 spec `no_candidate_action`+"후보 전무 구간"을 흡수. `no-candidates`는 ENGINE_SPEC §14.4 empty-payload pour branch 미구현을 흡수(source의 `is_satisfied=true` 인스턴스 유무 분기 부재). 번역 레이어는 adapter 내부, engine은 compact 내부 reason 유지. context-sensitive 매핑 가능 범위(`no-open-number` intent 분기 / `location-mismatch` 1:1) 외 나머지는 gap. 해소 옵션 (A) engine reason 세분화 / (B) adapter preflight 중 Gate B는 택일 확정 없음. engine-internal 유지(user-facing 미노출): `source-not-clean`(spec 부분 중첩) + `dry-run-blocked`(내부에 다수 branch 포함, best-fit 매핑 금지). deco_requires_base / pour_deco_requires_base는 신규 engine 능력(`deco-first` 규칙) 필요. **Rejection Code Mapping 표 §4**: spec 10코드 × (domain / spec section / spec rejection_code / current engine reason · code / translation needed / Phase 3 implication) 6열로 기계 검증 가능하게 작성. 1:1 rename은 `step_not_open`/`action_not_open`/`wrong_required_location` 3건만 명시, 나머지 7건은 gap 또는 신규 engine 능력 필요로 분류. **§5 Phase 3 Slicing**: "비확정 제안" 수준만 기록 — 기본 next step은 Phase 3 구현, 별도 planning task(`PHASE3_SLICE_MAP` 등)를 기본 경로로 두지 않음. slice 분할 필요 시에만 예외적으로 구현 중 도입. **§6 Non-Goals**: 코드 0 touch / Phase 3 본 구현 지시서 아님 / slice 수 · map 미확정 / .harness·commander docs 수정 아님 / shared shell · kitchen-mode 수정 아님 / sim 회귀 금지 / text-first UI 최종 미승인 / (A)(B) 택일 확정 금지. ⚠️ **제약 준수 확인**: 코드 파일 0 touch, 테스트·스키마·라우트 wiring 0 touch, practice 파일 · route · store · engine 구현 0 touch, shared shell · kitchen-mode 구현 0 touch, `.harness/*.json` 0 touch, `docs/commander/*` 0 touch. 산출물은 `docs/practice/GATE_B_DECISIONS_2026-04-22.md` 1건 + `docs/worker/LAST_SESSION.md` 갱신만. Phase 3 진입 권한 미개방. Phase 2 closed confirmed, Phase 3 implementation still blocked pending Gate B approval. **Plan 단계 Evaluator rounds**: Round 1 — `ingredient-mismatch`/`action-mismatch` 1:1 매핑이 engine 분기 순서와 불일치(L1) + `dry-run-blocked` best-fit 매핑이 축약 오류(L2) + PHASE3_SLICE_MAP 후속 task 권장이 과잉 절차(L3) 3건 지적, 수정 후 재제출. Round 2 — `no-candidates` 1:1 매핑이 §14.4 empty-payload pour branch 미구현 흡수라는 gap(L1) + "contract 12 메서드" 숫자 표기가 실제 14 멤버와 불일치(L2) 2건 지적, 수정 후 approved. **Phase 4 고정 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`** 전부 통과. 원칙 체크리스트는 코드 0 touch이므로 대부분 n/a, 적용되는 항목은 전부 충족. ⚠️ 고위험 항목 없음(문서 2개 수정, CSS 3D·3+ 파일·Zustand·DB 스키마·인터랙션 미해당).

## 직전 작업
TASK-20260422-210: **Phase 2 closeout 수동 구두 PASS 공식화 (운영/기록 task, 코드 0 touch)**. sjb 의 2026-04-22 구두 "시뮬레이터 전축 정상" 거시적 PASS 를 공식 manual verification 기록 3 건으로 고정. 변경 파일 4: (a) `.harness/manual_verification/TASK-20260421-205.md`(신규, scope = SIM_REGRESSION_CHECKLIST §6 UI/HUD) (b) `.harness/manual_verification/TASK-20260421-207.md`(신규, scope = §2 장비/물리 + SHARED_SHELL_BOUNDARY_APPENDIX §9.1 onRuntimeTick seam 무회귀) (c) `.harness/manual_verification/TASK-20260421-208.md`(신규, scope = §5 점수/로그, 4 항목 분리 기재 — idle penalty timing / stir log frequency / wok_burned single-fire / hydration false-positive 없음) (d) `docs/worker/LAST_SESSION.md`(본 기록 + TASK-209 엔트리의 이전 후보 상태 판정 라벨을 `Phase 2 closed candidate (sjb 수동 JSON 동기화 대기 중)` 로 in-place 승격). 각 manual_verification 파일은 통일 포맷(`Metadata / Verified Items / Result / Notes`) 유지 + 축별 세부 항목은 sjb 개별 관찰 추정 금지, `sjb oral PASS (covered under overall scope)` 문구로 통일 기재. **제약 준수 확인**: 코드 파일 0 touch, practice 파일/route/store/engine 0 touch, `.harness/*.json` 0 touch(`.harness/tasks/206-209.json` status 갱신 + `.harness/eval_feedback/TASK-20260421-206.json` 백필 + `docs/commander/LAST_SESSION.md` 의 Codex 제안 "Phase 2 closed confirmed" 선언은 **sjb 수동 후속 체크리스트**이며 본 task 범위 밖), Phase 3/Gate B/practice adapter 본 구현 0 착수, unrelated dirty worktree 0 touch(보고만). Phase 0/4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit` 전부 통과. LEARNINGS/MISTAKES 갱신: **해당 없음** — 새 패턴/실수 없이 기록 정리만 수행.

## 직전 작업
TASK-20260421-209: **Phase 2 누적 회귀 검증 (감사/검증 task, 코드 0 touch)**. TASK-205(Slice 1 shell/provider pass-through) + TASK-207(Slice 2 onPostTick seam) + TASK-208(Slice 3 sim-only score/log ownership 이관) 누적 결과가 Phase 2 closeout 기준을 충족하는지 최종 판정. 변경 파일: 0 (코드). 허용 보조 파일: `docs/worker/LAST_SESSION.md` 1건만 touch (LEARNINGS/MISTAKES 해당 없음 — 감사 task, 새 패턴/실수 없음). **누적 코드 상태 3축 PASS**: (1) shared runtime 청결 — `src/hooks/useGameTick.ts`(54 lines) scoring import 0, tick callback 끝단 `onPostTickRef.current?.()` 단일 seam만 존재 + `src/stores/equipmentStore.ts` scoring 결합 0. (2) sim adapter ownership — `src/lib/kitchen-mode/createSimModeAdapter.ts` L55-115 `onRuntimeTick()` 본체에 `addScoreEvent`(L79, wok_burned) + `addActionLog`(L86 wok_burned / L104 stir) + `checkIdlePenalty`(L114) 집중. `useScoringStore.getState()`(L58) read-only snapshot 경계 준수. (3) hydration race 방어 — `equipmentHydrated = equipments.length > 0` gate(L64) + burn 감지 3중 가드(`equipmentHydrated && prevWokStatuses !== null && sessionId`, L75) + baseline 승격도 hydrate 완료 시에만(L97). **practice adapter 경계 CLEAN**: `src/lib/kitchen-mode/createPracticeModeAdapter.ts`(43 lines) scoringStore/practice/* import 0, `onRuntimeTick: () => {}`(L36) 빈 skeleton 유지. **leak-check grep 4건 전부 충족**: A. `rg -n "useScoringStore|addScoreEvent|addActionLog|checkIdlePenalty|wok_burned|action_type: 'stir'" src/stores/equipmentStore.ts src/hooks/useGameTick.ts` → 0 hit(기대 0 PASS). B. `rg -n "useScoringStore|addScoreEvent|addActionLog|checkIdlePenalty|wok_burned|action_type: 'stir'|scoringStore" src/lib/kitchen-mode/createSimModeAdapter.ts` → 11 hit(L14 import, L20/L22 header, L58 getState, L79/L81 score, L86/L88 log wok_burned, L104/L106 log stir, L114 idle, 기대 1+ PASS). C. 동일 패턴 practice adapter → 0 hit(기대 0 PASS). D. `rg -n "useGameTick\(|onRuntimeTick|createSimModeAdapter\(|useMemo\(" src/pages/GamePage.tsx` → L112 `createSimModeAdapter()` factory + L125 `useGameTick({onPostTick: () => adapter.onRuntimeTick()})` seam 확인(기대 factory+callback 각 1건 PASS). **Phase 4 검증 고정 순서(build→lint→@self-reviewer→tsc) 전부 통과**: `npm run build` pass(8.60s, exit 0, 834.35KB — Slice 3 대비 번들 불변 = 코드 0 touch 확증), `npm run lint` 0 errors / 1 warning(`src/hooks/useGameTick.ts:52` `react-hooks/exhaustive-deps` — HEAD 버전도 `[]` deps + staleRef 패턴 사용하므로 pre-existing으로 확증, 신규 유입 0), `@self-reviewer` 1차 PASS(요구사항 9/11 수행 + 2/11 계획된 미이행 — LAST_SESSION 갱신 / tsc 본 단계에서 이행, 금지사항 위반 0, extra_changes 0, conditional pass → 이 기록 및 tsc 완료로 최종 PASS), `npx tsc --noEmit` exit 0(0 errors). **제약 준수 확인**: 코드 파일 0 touch, practice 파일/route/store/engine 0 touch, `.harness/*` 0 touch, Phase 3/Gate B/practice adapter 실동작 0 착수, regression 자가 수정 0(regression 없음). **판정**: `Phase 2 closed candidate (sjb 수동 JSON 동기화 대기 중)`. 3축(§2 장비/물리 = Slice 2·3 seam, §5 점수/로그 = Slice 3 이관, §6 UI/HUD = Slice 1 shell) 모두 static 기준 충족, 남은 잔여는 sjb 수동 검증(20-30분 분량 — §2/§5/§6/§9.1/§9.2 + 4개 분리 기록 항목: idle penalty 타이밍 / stir log 빈도 / wok_burned 1회성 / hydration 직후 false-positive 없음). plan_rounds 1회(pure audit — 확인 후 바로 Approved). execution_rounds 1회. **다음 단계**: sjb 수동 검증 통과 시 Phase 2 closeout 확정. 미통과 또는 regression 발견 시 다음 지시서로 열어야 함.

## 직전 작업
TASK-20260421-208: **Phase 2 Slice 3 — equipmentStore/useGameTick 에서 `useScoringStore` 결합 제거 + `createSimModeAdapter.onRuntimeTick()` 본체로 sim-only 점수/로그 ownership 이관**. SHARED_SHELL_BOUNDARY_APPENDIX §6.2 "sim 전용 store 는 adapter 가 소유" 경계를 코드로 실체화. 변경 파일 3: `src/stores/equipmentStore.ts` + `src/hooks/useGameTick.ts` + `src/lib/kitchen-mode/createSimModeAdapter.ts`. 단일 slice(3a/3b 분할 불가) — 부분 적용 시 (A) 이중 발행 또는 (B) 무발행 중 하나의 sim observable 훼손이 불가피한 원자 단위. (a) `src/stores/equipmentStore.ts`: `useScoringStore` / `SCORE_CONFIG` import 제거, `tickWok()` 내부 burn 전이 블록(prevStatus !== 'burned' && result.wok_status === 'burned' 시 addScoreEvent(wok_burned) + addActionLog(wok_burned)) 전체 제거. burn 블록에 종속된 `const prevStatus = equip.wok_status;` 지역변수도 함께 제거(unused 방지 cleanup). 물리(tickWokPhysics), pre_overheat_status_map, boil(100도 물 모드) / stir(canAccumulateStir 게이트) / fry / microwave 의 ingredient `action_history` 누적은 전부 유지. (b) `src/hooks/useGameTick.ts`: `useScoringStore` import + `stirring_equipment_ids`/`sessionId` 셀렉터 + `addActionLog`/`checkIdlePenalty` 셀렉터 + `sessionIdRef`/`stirringRef` 선언 + ref-sync 2줄 + stir log 블록 + `checkIdlePenalty(Date.now())` 호출 전부 제거. 유지: 물리 tick switch, `tickMix` 루프, `equipmentsRef`/`containerInstancesRef`/`onPostTickRef` + ref-sync, `onPostTickRef.current?.()` seam(adapter.onRuntimeTick 진입점), setInterval 1000ms + deps `[]` mount-once. 결과 파일 sim-only 결합 0. (c) `src/lib/kitchen-mode/createSimModeAdapter.ts`: skeleton stub 헤더(L12-16)를 Slice 3 4개 계약(① shared runtime 물리/상태전이 전용 ② sim-only scoring/log ownership = onRuntimeTick ③ useEquipmentStore/useGameStore/useScoringStore getState() 경계 사용 ④ practice adapter 미전파)으로 교체. imports 추가(useEquipmentStore / useGameStore / useScoringStore / SCORE_CONFIG + type WokStatus), 클로저 `let prevWokStatuses: Map<string, WokStatus> | null = null;`(null = 유효 baseline 미확보), `boot()` 방어적 리셋(`prevWokStatuses = null; return Promise.resolve();`), `onRuntimeTick()` 본체 구현. **hydration readiness gate(reviewer 지적)**: `equipmentHydrated = equipments.length > 0` — useGameTick은 GamePage mount 즉시 interval 시작되지만 `equipments`는 `GamePage.tsx:873` async effect(Supabase upsert → setEquipments)로 뒤늦게 hydrate되므로 pre-hydration 빈 Map 을 baseline 승격하면 hydrate 직후 burned 웍을 신규 burn 오판. gate 통과 시에만 감지(`equipmentHydrated && prevWokStatuses !== null && sessionId`) + gate 통과 시에만 baseline 승격. stir log/idle penalty는 각각 `stirring_equipment_ids.size > 0` 자연 조건 / timestamp 기반이라 gate 무관. same-tick 순서 `burn → stir → idle` 유지, 단일 `Date.now()` 공유. 이벤트 shape 불변(wok_burned score event `points=SCORE_CONFIG.WOK_BURNED` + `metadata:{equipment_id}`, action log 동일, stir action log `{session_id, action_type:'stir', timestamp_ms, metadata:{equipment_id}}` 동일). ⚠️ **제약 준수 확인**: 비범위 파일 0 touch — `src/pages/GamePage.tsx`(adapter wiring L125 `useGameTick({ onPostTick: () => adapter.onRuntimeTick() })` 유지, L112 `useMemo` identity 안정 유지) · `src/components/game/GameKitchenView.tsx` · `src/stores/gameStore.ts` · `src/stores/scoringStore.ts` · `src/components/game/shared-shell/*` · `src/lib/kitchen-mode/createPracticeModeAdapter.ts` · practice 파일 전체 전부 0 수정. `as any`/`@ts-ignore`/`eslint-disable` 0, Zustand 인라인 filter/map 0, 물리엔진에서 DB write 0(오히려 scoring 호출 제거로 경계 강화). Phase 0/4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`: Phase 0 baseline pass(834.35KB), Phase 4 build pass(834.35→834.35KB, dist assets hash 변화만), lint pass(useGameTick L52 `react-hooks/exhaustive-deps` warning 1건 pre-existing — plan §3.2 "mount-once 불변" 명시 요건이라 eslint-disable 없이 warning 유지, 0 errors / 새 warning 0), `@self-reviewer` 1차 PASS(요구사항 14/14, 금지사항 위반 0, extra_changes 0, 판정 이상 없음), tsc 0 errors. leak-check grep 3분할(plan v3 §7.2): (1) shared runtime 청결 — `rg "useScoringStore|addScoreEvent|addActionLog|checkIdlePenalty|wok_burned|action_type: 'stir'" src/stores/equipmentStore.ts src/hooks/useGameTick.ts` → **0 hit**(기대 0, PASS), (2) sim adapter ownership 양성 확인 — `rg "useScoringStore|addScoreEvent|addActionLog|checkIdlePenalty|wok_burned|action_type: 'stir'|scoringStore" src/lib/kitchen-mode/createSimModeAdapter.ts` → **11 hits**(import + header comment + onRuntimeTick body, 기대 1+ PASS), (3) practice adapter 경계 — 동일 패턴 `src/lib/kitchen-mode/createPracticeModeAdapter.ts` → **0 hit**(기대 0, PASS). 기존 v1 광역 grep의 FP(types/db.ts literal + RecipeManager.ts:764 + fixtures/test + components UI 등) 제거 및 adapter ownership 양성 검증 확보. Plan iteration 3회(v1: 광역 grep + header comment 미갱신으로 거절 → v2: 3분할 grep + 4계약 header 반영 → v3: reviewer 지적한 hydration race 흡수용 readiness gate 추가로 approved). execution_rounds 1회. **LEARNINGS/MISTAKES 갱신**: LEARNINGS 1건 추가 예정 — adapter baseline 승격을 "첫 호출"이 아니라 "유효 snapshot 확보(equipments.length > 0)"로 gate. 첫 호출 기준만으로는 useGameTick interval이 GamePage mount 즉시 시작되지만 equipments가 async effect로 뒤늦게 hydrate되어 pre-hydration 빈 Map이 baseline으로 승격되고, hydrate 이후 burned 웍이 신규 burn으로 오판되는 race가 남음. equipmentStore 스키마 0 touch + read-only getState()만으로 해결되는 최소변경 proxy. MISTAKES 갱신: 해당 없음. 롤백 단위: 3파일 원자 revert → Slice 2 baseline(TASK-207) 복귀. **다음 slice**: Phase 2 §5 점수/로그 축 Slice 완료. 남은 Phase 2 축(§3 판별/거절, §4 주문/서빙, §7 관리자/피드)은 별도 지시서 발행 필요.

## 직전 작업
TASK-20260421-207: **Phase 2 Slice 2 — `useGameTick` 끝단에 adapter `onRuntimeTick()` 단일 seam 추가**. SHARED_SHELL_BOUNDARY_APPENDIX §9.1 "shared runtime 기본 1초 tick 이후 shell 이 adapter 의 `onRuntimeTick()` 호출" 계약을 코드 순서로 고정. 별도 `setInterval` 2 개 방식은 drift/interleave 로 "이후" 순서 역전 가능 → **동일 scheduler 끝단 단일 호출 1 줄**로 보장. 변경 파일 2: `src/hooks/useGameTick.ts` + `src/pages/GamePage.tsx`. 허용 보조: `docs/worker/LAST_SESSION.md`. (a) `src/hooks/useGameTick.ts` 변경: (1) 시그니처 확장 — `export function useGameTick(options?: { onPostTick?: () => void })`, (2) staleRef 1 개 추가 — `const onPostTickRef = useRef(options?.onPostTick)` (기존 `equipmentsRef` / `containerInstancesRef` / `sessionIdRef` / `stirringRef` 패턴 mirror), (3) ref-sync useEffect 끝단 1 줄 추가 — `onPostTickRef.current = options?.onPostTick`, (4) interval callback 끝단에 `onPostTickRef.current?.()` 1 줄만 — 위치는 `checkIdlePenalty(Date.now())` 바로 다음 줄. 결과 호출 순서 선형화: `tickWok → tickBasket → tickMicrowave → tickMix → stir log → checkIdlePenalty → onPostTick`. (5) 그 외 0 touch — interval effect deps `[]` mount-once 유지(staleRef 로 최신 콜백 접근), `useEquipmentStore` / `useGameStore` / `useScoringStore` 구독 그대로, stir log 블록·`addActionLog`·`checkIdlePenalty` 호출 그대로(Slice 3 영역), adapter 타입/심볼(`KitchenModeAdapter` / `useKitchenModeAdapter` / `createSimModeAdapter`) import 0 — shared runtime 이 adapter 를 모른다는 Gate A ④ 경계 유지. (b) `src/pages/GamePage.tsx` 변경: L125 호출부 1 줄만 — `useGameTick();` → `useGameTick({ onPostTick: () => adapter.onRuntimeTick() });`. adapter `useMemo(() => createSimModeAdapter(), [])` (L112) 및 boot effect deps `[adapter, selectedStore.id, selectedUser?.id, sessionId]` (L114-122) baseline 그대로 유지. 새 `setInterval`/`setTimeout` 추가 0 (기존 L1116 `setTimeout` 은 세션 종료 분기로 slice 무관 → 유지). ⚠️ **제약 준수 확인**: 비범위 파일 0 touch — `src/components/game/GameKitchenView.tsx` · `src/stores/equipmentStore.ts` · `src/stores/gameStore.ts` · `src/stores/scoringStore.ts` · `src/components/game/shared-shell/*` · `src/lib/kitchen-mode/*` · practice 파일/route/store/engine 전부 0 수정. sim adapter `onRuntimeTick()` no-op 유지(`() => {}`) → §5 점수/로그 축은 여전히 닫혀 있고 실제 runtime 부작용 0. tick 주기·호출 순서·stir/boil/fry/microwave/mix 누적 규칙·온도 전이 규칙 0 변경. `as any`/`@ts-ignore`/`eslint-disable` 0, Zustand selector 인라인 filter/map 0, 물리엔진에서 DB write 0. Phase 0/4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`: Phase 0 baseline pass(834.25KB), Phase 4 build pass(834.25→834.35KB, +0.10KB — staleRef 1 개와 optional call 1 줄만 추가), lint pass(기존 `src/hooks/useGameTick.ts:76` `react-hooks/exhaustive-deps` warning 1 건 pre-existing, 0 errors / 새 warning 0), `@self-reviewer` 1 차 PASS(요구사항 6/6, 금지사항 위반 0, extra_changes 0, 판정 approved / 이상 없음), tsc 0 errors. leak-check grep 9 건 전부 충족: `rg "onRuntimeTick" src` → `GamePage.tsx:125`(new) + `createSimModeAdapter.ts:36` + `createPracticeModeAdapter.ts:36` + `KitchenModeAdapter.ts:103`(interface) 4 건, `useGameTick.ts` 에는 **등장 0** / `rg "onPostTick" src/hooks/useGameTick.ts src/pages/GamePage.tsx` → 5 건(useGameTick 4 + GamePage 1) / `rg "onPostTick" src` outside allow-list → 0 건 / `rg "KitchenModeAdapter|useKitchenModeAdapter|createSimModeAdapter|createPracticeModeAdapter" src/hooks src/stores src/components/game/shared-shell` → 0 건 / `rg "setInterval\|setTimeout" src/pages/GamePage.tsx` → 1 건(pre-existing L1116) / `rg "setInterval" src/hooks/useGameTick.ts` → 1 건 / `rg "useScoringStore\|addScoreEvent\|addActionLog\|checkIdlePenalty" src/hooks/useGameTick.ts` → Slice 1 직후 동일 5 건 유지 / `rg` 동일 패턴 `src/stores/equipmentStore.ts` → Slice 1 baseline 동일 4 건 유지(감소 금지 준수 — 감소 시 Slice 3 범위 침범) / `rg "as any\|@ts-ignore\|eslint-disable" src/hooks/useGameTick.ts src/pages/GamePage.tsx` → 0 건. execution_rounds 1 회(Evaluator 재검토 불필요). LEARNINGS/MISTAKES 갱신: **해당 없음** — TASK-206 에서 이미 "§9.1 같은 '이후' 계약은 별도 interval 이 아니라 동일 scheduler 끝단 단일 seam 으로 보장" 학습이 기록됨(`LEARNINGS.md` 2026-04-21 항). 본 slice 는 그 계획을 실제 코드로 실행만 했을 뿐 새 패턴 발견 0. 새 실수 0. 롤백 단위: 2 파일 — `useGameTick` options 추가분(시그니처 확장 + staleRef + ref-sync 1 줄 + interval 끝단 1 줄) 제거 + GamePage L125 호출 인자 제거 → Slice 1 baseline 복원. **다음 slice**: TASK-20260421-207 approved 후 Slice 3(equipmentStore `useScoringStore` 결합 제거 + sim adapter `onRuntimeTick()` 본체 구현) 지시서 발행 가능.

## 직전 작업
TASK-20260421-206: **Phase 2 slice map 문서 1건 신설** (planning slice, 소스 0 touch). Gate A(TASK-204) + baseline wiring(TASK-205) 위에서 Phase 2 본 구현을 `단일 회귀 축만 여는 3 슬라이스`로 고정해 이후 구현 지시서의 상위 기준으로 확정. 변경 파일 1: `docs/practice/PHASE2_SLICE_MAP_2026-04-21.md`(신규). 허용 보조: `docs/worker/LAST_SESSION.md`(본 기록) + `docs/worker/LEARNINGS.md` 1건 append. **핵심 결정 5축**: (1) Slice 1(`GameKitchenView` shell 편입)은 **closed baseline — TASK-20260421-205 approved로 완료**. 섹션은 Slice 2·3 전제 조건 고정용 이력 문서로 유지하되 신규 지시서 발행 없음. GameKitchenView 내부 `useEquipmentStore`/`useGameStore` 직접 구독 **유지** 결정 + 만약 adapter 경유로 바꾸면 Slice 2/3 전제가 깨져 slice 순서 재검토 필요 경고 명시. (2) Slice 2(useGameTick 끝단 onRuntimeTick 단일 seam)는 **다음 발행 대상**. SHARED_SHELL_BOUNDARY_APPENDIX §9.1 "shared runtime 기본 1초 tick 이후, shell은 adapter의 onRuntimeTick() 호출" 계약의 "이후"를 코드 순서로 보장하기 위해 **별도 interval 2개 방식(drift/역전 위험)을 명시적으로 배제**하고, `src/hooks/useGameTick.ts`의 기존 단일 `setInterval` 콜백 끝단(`checkIdlePenalty(Date.now())` 다음 줄)에 `onPostTick?.()` 1줄만 추가하는 **동일 scheduler 끝단 단일 seam**으로 확정. 훅 시그니처는 `useGameTick(options?: { onPostTick?: () => void })`로 확장되며 `onPostTick` 은 `() => void` 일반 콜백 — adapter 타입/모듈 import 0으로 Gate A ④ 경계(shared runtime이 adapter를 모른다) 유지. GamePage는 `useGameTick({ onPostTick: () => adapter.onRuntimeTick() })`로 adapter 바인딩 담당. 이 slice 단계 sim adapter `onRuntimeTick`은 **no-op** 유지(scoring 이관은 Slice 3). 호출 순서 선형화: `tickWok → tickBasket → tickMicrowave → tickMix → (stir log) → checkIdlePenalty → onPostTick`. (3) Slice 3(equipmentStore → sim adapter scoring 이관)은 `useScoringStore` import 제거 + `wok_burned`/`addActionLog({action_type:'stir'})`/`checkIdlePenalty` 호출 제거 + sim adapter `onRuntimeTick()` 본체가 store를 read-only 조회해 동일 이벤트 재발행. 이전 tick burn 스냅샷은 adapter 인스턴스 필드로 보관 → Slice 1 baseline의 `useMemo`/`useState` initializer 안정화가 **필수 전제**. (4) 공통 규칙 §2.1 `Adapter instance 안정화` — Provider value는 adapter 인스턴스 자체가 들어가며(wrapper object memo 불필요, [KitchenModeAdapterContext.tsx:13](../../src/lib/kitchen-mode/KitchenModeAdapterContext.tsx#L13) 근거), 따라서 안정화의 유일 요구는 `adapter` identity 자체가 렌더 간 동일해야 한다는 것. §2.2 `Boot effect dependency 한정` — boot effect deps는 `[adapter, store_id, user_id, mode, practice_menu_id, sim_session_id]`로만 구성, useGameTick 내부 interval effect는 `[]` mount-once + staleRef로 최신 onPostTick 접근(adapter deps 금지, Slice 3도 동일). (5) §8 `Phase 2에서 열지 않는 SIM_REGRESSION 축` 섹션 명시 — §3 판별/거절 / §4 주문/서빙 / §7 관리자/피드. ⚠️ **제약 준수 확인**: 소스코드 0 touch, skeleton 9파일 0 touch, practice 파일/route/store/engine 0 touch, sim semantics 변경 0, 각 slice 10개 섹션(목표/범위/비범위/전제 조건/예상 수정 파일/노출 SIM_REGRESSION 축/필수 leak-check grep/adapter 안정화 규칙/boot effect dependency 규칙/롤백 단위) 모두 포함, slice 순서 `GameKitchenView`→`useGameTick`→`equipmentStore` 고정(병렬 slice 지시서 금지, Slice N은 N-1 approved 전제), dirty worktree 존재하지만 task scope diff 기준 extra_changes 0. Phase 0/4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`: Phase 0 baseline pass(834.25KB), Phase 4 build pass(834.25KB 불변 — 문서만 변경), lint pass(기존 useGameTick `react-hooks/exhaustive-deps` warning 1건만 pre-existing, 0 errors), `@self-reviewer` PASS(요구사항 14/14, 금지사항 위반 0, extra_changes 0, 판정 이상 없음), tsc 0 errors. plan_rounds 3회(Evaluator Round 1: Slice 2가 별도 interval로 §9.1 순서 미보장 + Slice 1이 baseline 상태 미반영 + Provider wrapper memo 문구 오류 3건 / Round 2: §1 baseline wiring 명시 누락 + Slice 3 tick effect deps `[adapter]`가 Slice 2 `[]` mount-once와 충돌 2건 / Round 3 approved). LEARNINGS 갱신: **1건 추가 — §9.1 같은 "이후" 계약은 동일 scheduler 끝단 단일 seam(콜백 1줄)으로 보장. 별도 interval 추가는 drift/역전 위험으로 계약 위반**. shared runtime hook이 adapter 심볼을 모르면서 adapter 바인딩을 가능케 하는 방법으로 `() => void` optional callback + staleRef 패턴 명시. MISTAKES 갱신: 해당 없음 — Plan 단계 Evaluator 피드백 2회는 planning iteration 범위로 실행 중 실수 아님.

## 직전 작업
TASK-20260421-205: **Gate A skeleton을 실제 sim route에 최초 연결** (pass-through wiring only, sim rules 분리 미착수). `createSimModeAdapter` + `KitchenModeAdapterProvider` + `SharedKitchenShell` + `SharedKitchenHudSlots` 네 심볼을 `src/pages/GamePage.tsx` 단일 파일에만 도입해 page-level에서 조립. 변경 파일 1: `src/pages/GamePage.tsx`. 허용 보조: `docs/worker/LAST_SESSION.md` 1건만 touch(학습/실수 갱신 없음). 수정 내용 5축: (1) import 2줄 추가 — `createSimModeAdapter, KitchenModeAdapterProvider` from `../lib/kitchen-mode` / `SharedKitchenShell, SharedKitchenHudSlots` from `../components/game/shared-shell`. (2) `selectedUser = useAuthStore((s) => s.selectedUser)` 셀렉터 추가(기존 `selectedStore` 바로 아래, nullable이라 `!` 단언 금지). (3) `const adapter = useMemo(() => createSimModeAdapter(), []);` — 마운트당 1회 고정, 매 render 재생성 방지. (4) boot effect: `if (!selectedStore?.id || !selectedUser?.id || !sessionId) return; void adapter.boot({ store_id, user_id, mode: 'sim', sim_session_id })` + deps `[adapter, selectedStore.id, selectedUser?.id, sessionId]` primitive 4개로 한정. teardown/retry/ref guard/idempotency/StrictMode guard 0 (stub 이라 double-call 허용). (5) 렌더 트리 — `.gameArea` 내부 `GameHeader` 다음 위치에 `<KitchenModeAdapterProvider adapter={adapter}><SharedKitchenShell hudSlots={<SharedKitchenHudSlots topLeft={<div.topLeftHud>SelectionDisplay+Handbar</div>} topRight={MinimapHUD} bottomRight={NavigationHUD} />}>{div.mainViewport + GameKitchenView + BillQueue(children)}</SharedKitchenShell></KitchenModeAdapterProvider>` 로 감싸기. `bottomLeft`/`modeSummary` prop 전달 0(빈 슬롯). `GameHeader`, `OrderSelectModal`, `QuantityInputModal`, `RejectionPopup`, `WokBlockedPopup`, `SessionResultOverlay` 전부 shell 밖 기존 위치 유지. BillQueue는 GameKitchenView children 자리 유지. 신규 CSS/inline style 0 (기존 `styles.topLeftHud` · `styles.mainViewport` className 그대로 재사용). **⚠️ 구조 관찰**: `SharedKitchenShell`/`SharedKitchenHudSlots`는 순수 `<div data-*>` wrapper로 CSS 0. 기존 absolute HUD는 nearest positioned ancestor `.gameArea`(position:relative)를 계속 기준 삼으므로 shell/HudSlots div 2겹을 끼워도 레이아웃 보존. 수동 검증에서 MinimapHUD/NavigationHUD/topLeftHud/BillQueue 위치·상호작용 기존과 동일. ⚠️ **제약 준수 확인**: 수정 금지 파일 6종(`src/components/game/GameKitchenView.tsx` · `src/hooks/useGameTick.ts` · `src/stores/equipmentStore.ts` · `src/stores/gameStore.ts` · `src/stores/scoringStore.ts` · `src/lib/kitchen-mode/createSimModeAdapter.ts` stub 유지) 0 터치, practice 파일/route/store/engine 0 터치, Gate B/Phase 3 선행 0, CSS 파일 0 수정, any/as any/ts-ignore/eslint-disable 0, display:none 0, KitchenModeAdapter* 심볼은 `src/lib/kitchen-mode/**` + `src/components/game/shared-shell/**` + `src/pages/GamePage.tsx`에만 존재(다른 runtime/store/hook/view로 누출 0). Phase 0/4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`: Phase 0 baseline pass(833.20KB), Phase 4 build pass(833.20→834.26KB, +1.06KB), lint pass(기존 useGameTick warning 1건만 pre-existing, 0 errors), `@self-reviewer` PASS(요구사항 16/16, 금지사항 위반 0, extra_changes 0, 판정 이상 없음), tsc 0 errors. **Re-submission 1회**: Evaluator Round 1 피드백에서 `SessionResultOverlay` onFeed/onClose 핸들러의 `navigate('/sim/feed')`/`navigate('/sim/game/setup')` 이 HEAD 원본(`'/feed'`/`'/game/setup'`) 대비 scope 밖 변경으로 지적됨 — 해당 2줄은 본 세션 시작 전부터 working tree에 M 상태였으나 제출 diff 기준으로 scope expansion 으로 판정. `router.tsx` truth와 맞더라도 별도 bugfix 지시서 범위이므로 HEAD 원본 값으로 되돌림. 재검증 build pass(834.25KB, 문자열 shortening 으로 -0.01KB), lint pass 동일, tsc 0 errors. plan_alignment 3/5→예상 5/5, execution_rounds 2 회. 검증 쿼리 3건 전부 충족 — `rg "\.boot\(" src/pages/GamePage.tsx` 1건 / `rg "plate_order|useOrderGenerator|scoringStore|practiceStore" src/components/game/shared-shell src/lib/kitchen-mode` 0건 / `rg "KitchenModeAdapter|SharedKitchenShell|SharedKitchenHudSlots" src` outside allow-list 0건. LEARNINGS/MISTAKES 갱신: **해당 없음** — "CSS 0 wrapper div는 absolute 자식의 positioned ancestor resolution에 영향 주지 않는다"는 설계 insight는 표준 CSS 동작이라 프로젝트 학습 범주 밖, 지시서 본문도 이 안전성을 전제로 작성됨. 새 패턴/새 실수 없음. **명시**: shell/provider pass-through wiring만 수행, sim rules 분리 미착수 — sim evaluator/order/scoring은 여전히 `useOrderGenerator`/`useRecipeEval`/`useScoringStore` 직접 구독으로 기존 경로 유지. adapter는 stub 상태 그대로 boot만 호출되고 실제 동작은 무관.

## 직전 작업
TASK-20260421-204: **Phase 2 shared kitchen shell 스켈레톤 초안 작성** (본 구현 미착수). `docs/practice/EXECUTION_PLAN_2026-04-21.md` §12 Gate A + `docs/practice/SHARED_SHELL_BOUNDARY_APPENDIX_2026-04-21.md` §4 기반으로 공용 shell / mode adapter 구조를 Gate A 승인 전 단계까지 고정. 변경 파일 9 new(`.harness/tasks/TASK-20260421-204.json` files[] 1:1 대응): `docs/practice/PHASE2_SKELETON_DRAFT_2026-04-21.md`(설계 결정 문서, 6 섹션: Current Coupling Snapshot / Proposed File Tree / Gate A Decisions / Existing Files To Change In Real Phase 2 / Risks·Open Questions / Verification), `src/components/game/shared-shell/{SharedKitchenShell.tsx,SharedKitchenHudSlots.tsx,index.ts}`, `src/lib/kitchen-mode/{KitchenModeAdapter.ts,KitchenModeAdapterContext.tsx,createSimModeAdapter.ts,createPracticeModeAdapter.ts,index.ts}`. 허용 보조: `docs/worker/LAST_SESSION.md`. 기존 runtime/page/router/hook/store 0 수정(`src/pages/GamePage.tsx` · `src/components/game/GameKitchenView.tsx` · `src/hooks/useGameTick.ts` · `src/stores/equipmentStore.ts` · `src/stores/gameStore.ts` · `src/stores/scoringStore.ts` read-only 유지). Gate A 4 결정 고정: (1) 공용 shell `src/components/game/shared-shell/*`, (2) adapter 주입 React Context, (3) session boot route-level page, (4) shared runtime 과 adapter 무관(`useEquipmentStore`/`useGameTick`/shared-shell 어느 곳도 adapter 심볼 import 금지). `LocationKey`·`LocationRef` 는 `src/lib/kitchen-mode/KitchenModeAdapter.ts` 에 단일 정의(`ENGINE_SPEC_APPENDIX §3.1–§3.3` 근거). adapter factory 2 개는 모든 필드를 타입-안정 기본값(`Promise.resolve()` / `[]` / `null` / `{ok:false, rejection_code:'skeleton_stub'}` / no-op)으로 반환하는 stub 으로 `sim evaluator`·`practice engine`·`scoringStore`·`practiceStore` 를 import 0. ⚠️ **구현 노트**: 계획 초안에서 `KitchenModeAdapterContext.tsx` 에 `createContext` + Provider + `useKitchenModeAdapter` 3축을 두기로 했으나, ESLint `react-refresh/only-export-components` 규칙이 `.tsx` 파일의 Context/hook export 를 금지해 재배치 — Context 객체와 hook 은 `KitchenModeAdapter.ts`(.ts 로 규칙 미적용) 에 co-locate, Provider 컴포넌트만 `KitchenModeAdapterContext.tsx` 에. barrel(`src/lib/kitchen-mode/index.ts`) 가 공개 surface 를 일괄 re-export 해 소비자 경로 불변. `eslint-disable` 은 `docs/rules/forbidden.md` 금지이므로 쓰지 않고 파일 간 재배치로 해결. 설계 결정 문서(§2 Proposed File Tree + §3 Gate A Decisions ②) 에 이 realized 구조를 명시. Phase 0/4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`: Phase 0 baseline pass(833.20KB), Phase 4 build pass(동일 833.20KB — 신설 파일 어디서도 import 되지 않아 번들 증가 0), lint pass(기존 `src/hooks/useGameTick.ts` pre-existing warning 1건만, 0 errors), `@self-reviewer` PASS(요구사항 8/8, 금지사항 위반 0, extra_changes 0, 판정 이상 없음 — 신설 심볼 6종 no-wiring 심볼 grep 0 건, 금지 6 파일 unmodified, admin↔game import 0, `as any`/`@ts-ignore`/`eslint-disable` 0), tsc 0 errors. plan_rounds 2 회(평가자 8→9 카운트·LocationKey 단일 정의·심볼 기준 no-wiring grep 3건 수정 후 승인). 지시서에 따라 `LEARNINGS.md`/`MISTAKES.md` 는 갱신 없음 — `react-refresh/only-export-components` 가 .tsx Context export 를 금지한다는 내용은 널리 알려진 vite 프리셋 기본 동작이라 새 학습 아님, 본 deviation 은 설계 문서에 기록됨. 새 실수 없음. **명시**: 스켈레톤 초안 완료, 본 구현 미착수 — Phase 2 본 구현은 Gate A 사용자 승인 이후 별도 지시서.

## 직전 작업
TASK-20260420-163: `/practice` 빈 상태에서 관리자용 진입 CTA 노출 + `AvatarSelectPage` role modal 에 `/practice/admin` 진입 경로 추가. TASK-162 의 dead-end 제거 후속 UX 보강 — "연습 메뉴가 없는데 만들러 갈 길이 없다" 공백 해소가 유일 목적, recipes/practice_menus 통합이나 가드 변경은 범위 밖. 변경 파일 3 개: `src/pages/practice/PracticePage.tsx` / `src/pages/practice/PracticePlaceholder.module.css` / `src/pages/AvatarSelectPage.tsx`. AvatarSelectPage.module.css 는 수정 없음(files_changed 제외). (a) PracticePage.tsx: `useAuthStore` 셀렉터 2 추가(`user`, `selectedUser`), `supabase` 직접 import 추가(AvatarSelectPage 선례), 로컬 state `hasAdminAvatarInStore: boolean` 추가(기본 `false`). 신규 useEffect(`[selectedStore, user]` 의존성)은 **fail-closed reset 패턴** — 진입 시 `setHasAdminAvatarInStore(false)` 를 먼저 실행한 뒤 `selectedStore && user` 조건 충족 시에만 `supabase.from('store_users').select('id').eq('store_id',...).eq('auth_user_id', user.id).eq('role','admin').is('deleted_at', null).limit(1)` 조회 → `data?.length > 0` 이면 true. `cancelled` 플래그로 언마운트/재진입 중 race 차단. 매장 전환·로그아웃 순간 이전 true 가 잠깐 남아 "잘못된 관리자 CTA"가 반짝이는 것을 구조적으로 차단. 빈 상태 블록(`!loading && !error && menus.length === 0`)을 `.emptyCta` wrapper 1 개로 감싸 세로 스택, 3 분기: (1) `hasAdminAvatarInStore && selectedUser?.role === 'admin' && selectedUser.store_id === selectedStore.id` → "연습 메뉴 관리" primary CTA(`startButton` 재사용) → `navigate('/practice/admin')`. ⚠️ **store_id 일치 검증** — `StoreUser.store_id`(db.ts L16) 로 이전 매장 stale `selectedUser` 가 `/practice/admin` 가드 튕김을 유발하지 않도록 현재 매장 일치를 한 번 더 확인. (2) `hasAdminAvatarInStore` 이면서 (1) 조건 불충족 → `.hintText` 로 "연습 메뉴 생성/수정은 관리자 아바타 선택 후 가능합니다." 안내 + "관리자 아바타 선택" primary CTA → `navigate('/sim/join/avatar')`. (3) `!hasAdminAvatarInStore` → 기존 `"등록된 메뉴가 없습니다."` 문구만, 관리자 CTA 노출 0. 하단 `홈으로` 버튼(L94 구조)은 모든 상태 공통 보조로 유지 — dead-end 는 "빈 상태에 홈으로만 있던 관리자 보유자" 시나리오만이었음, 공통 홈으로 자체 제거 아님. (b) PracticePlaceholder.module.css: `.subtitle` 직후 `.emptyCta { display:flex; flex-direction:column; align-items:center; gap: var(--spacing-sm); }` 1 개 추가. `.startButton` / `.hintText` / `.subtitle` 기존 클래스 재사용, 신규 CSS 변수 하드코딩 0. (c) AvatarSelectPage.tsx: `showRoleModal` 의 admin 전용 블록(기존 L371-378)을 Fragment 로 감싸 admin 버튼 2 개로 확장 — `시뮬레이터 관리자` → `/sim/admin`(기존 "관리자 페이지" 라벨 교체), `연습 관리자` → `/practice/admin`(신규). 두 버튼 모두 `styles.modalButtonPrimary` 재사용(CSS 변경 0). `게임 시작`, `내 피드`, `취소` 버튼·핸들러 불변. ⚠️ **제약 준수 확인**: selectedUser 자동 선택 0(`/sim/join/avatar` 경로로 유도만), returnTo 쿼리/navigation state/오픈 리다이렉트 유틸 0, recipes/practice_menus 통합·스키마·마이그레이션·시드 0, ProtectedRoute/authStore 계약 0, `/practice`·`/practice/admin`·`/practice/session/:sessionId` 가드 의미 0, `/sim/admin`·`/sim/game/setup`·`/sim/feed` 기존 진입 0, `useAuthStore` 인라인 filter/map 0, any/as any/ts-ignore/eslint-disable 0, display:none 0, CSS 변수 인라인 하드코딩 0. 원칙 체크리스트 10/10 통과. Phase 0/4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`: Phase 0 baseline pass(832.17KB), Phase 4 build pass(832.17→833.20KB, +1.03KB), lint 0 errors + 기존 useGameTick warning 1 건만(pre-existing 변화 없음), `@self-reviewer` 1 차 PASS(요구사항 10/10, 금지사항 위반 0, extra_changes 0, 판정 이상 없음), tsc 0 errors. LEARNINGS/MISTAKES 갱신: 해당 없음 — "비동기 boolean state 는 의존성 변동 시 fail-closed reset 을 effect 진입부에 둔다" 패턴은 기존 `setBundle` 비동기 참조 교체 + `useEffect` commit-after 타이밍 대응 학습(TASK-156)과 동형이며, `selectedUser.store_id` 현재 매장 일치 검증은 TASK-162 의 `setSelectedUser(null)` cross-store 누수 차단 전략의 방어선 연장. 새 패턴·새 실수 없음.

## 직전 작업
TASK-20260420-162: `/sim/join` 매장 선택 직후 "시뮬레이터 / 메뉴 연습" 모드 선택 모달 추가 + `/practice` 리스트를 store-first 라우트로 승격해 dead-end 구조적 제거. 변경 파일 4 개: `src/router.tsx` / `src/pages/JoinPage.tsx` / `src/pages/JoinPage.module.css` / `src/pages/practice/PracticePage.tsx`. 허용 보조: `docs/worker/LAST_SESSION.md`. (a) router.tsx: 기존 `/practice` + `/practice/menu/:menuId` 가 `<ProtectedRoute />`(auth-only) 한 블록에 묶여 있던 것을 두 블록으로 분리 — `/practice` 는 `<ProtectedRoute requireStore />` 승격, `/practice/menu/:menuId` 는 `<ProtectedRoute />` 유지(browse 성격 불변 제약). `/practice/session/:sessionId`(requireStore requireUser), `/practice/admin`(requireStore requireUser requireAdmin) 가드 불변. (b) JoinPage.tsx: `import '../styles/gameVariables.css'` 추가(main.tsx/App.tsx/index.css 어디서도 토큰 전역 로드 없음 — Evaluator Round 2 피드백 반영, Practice 페이지 선례 동일). `setSelectedUser` selector 추가. 로컬 state `modeModalStore: Store | null` 신설. `handleSelectStore` 를 `setSelectedStore(store) + setSelectedUser(null) + setModeModalStore(store)` 로 교체 — navigate 제거, cross-store stale user 누수 차단. `handleCreateSubmit` 말미를 `setSelectedStore(newStore) + setSelectedUser(null) + setModeModalStore(newStore) + setShowCreate(false)` 로 교체. `handleModeCancel` 은 모달만 닫고 selectedStore/selectedUser 유지. `setSelectedUser(null)` 전략은 Evaluator Round 1 의 "유일 avatar 자동 선택" 안을 기각하고 선택된 것 — /sim 가드 의미 보존(requireUser 가 여전히 AvatarSelectPage 경유를 강제). 모달 JSX 는 card 블록 바깥 container 말미에 overlay 기반 2 CTA("시뮬레이터"→`/sim/join/avatar`, "메뉴 연습"→`/practice`) + "취소" 닫기. StoreCard interface 는 기존 구조 유지(myUsers 필드 불필요). (c) JoinPage.module.css: `mode*` prefix 로 9 개 class 신설(`modeOverlay` position:fixed inset:0 z-index:100 / `modeModal` max-width:360 box-shadow / `modeTitle` / `modeSubtitle` / `modeButtons` column / `modeButtonPrimary` `var(--color-primary)` + hover `var(--color-primary-hover)` / `modeButtonSecondary` 보조 / `modeClose` + hover `var(--color-error)`). CSS 변수 하드코딩 0(모두 gameVariables.css 정의). display:none 0. (d) PracticePage.tsx: `Navigate` import 추가, `if (!selectedStore)` 블록의 "점포를 먼저 선택하면..." dead-end UI + "홈으로" 버튼 제거 → `return <Navigate to="/sim/join" replace />` 로 대체(라우트 가드가 정상 흐름에서 이 경로 도달 불가능하게 만들어 safety redirect 만 남김). 하단 "홈으로" 버튼(line 102) 정상 흐름용이라 유지. `useNavigate` 는 메뉴 카드 onClick 과 하단 "홈으로" 에서 여전히 사용. ⚠️ **제약 준수 확인**: returnTo 쿼리/navigation state/오픈 리다이렉트 방어 유틸 0, PublicOnlyRoute 및 AvatarSelectPage 0 터치, `/practice/session/:sessionId` + `/practice/admin` 가드 불변, `/practice/menu/:menuId` 가드 불변, practice query/migration/schema 0 터치, any/as any/ts-ignore/eslint-disable 0, 기존 `/sim` 경로 simulator 진입 동작 유지(매장 선택→모달→"시뮬레이터"→`/sim/join/avatar` 경로는 AvatarSelectPage 의 기존 모달/동선 그대로 작동). Phase 0/4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`: Phase 0 baseline pass(831.29KB), Phase 4 build pass(831.29→832.17KB, +0.88KB), lint pass(기존 useGameTick warning 1 건만 pre-existing, 0 errors), `@self-reviewer` PASS(요구사항 8/8, 금지사항 위반 0, extra_changes 0, 판정 이상 없음), tsc 0 errors. Evaluator Round 1 피드백 후 Round 2 제출로 승인. LEARNINGS/MISTAKES 갱신: 해당 없음 — 신규 패턴은 "CSS 변수 토큰이 공유 페이지에서 참조될 때 import 경로 누락은 사전 감사로 잡아야 한다" 정도이나 기존 Practice 페이지들이 이미 동일 패턴으로 import 해왔으므로 학습 재사용 범주. 새 실수 없음(Round 1 피드백은 코드 반영 전 계획 단계에서 차단됨).

## 직전 작업
TASK-20260420-159: `/practice/admin` closure 이후 남은 (1) polish 판정, (2) practice 인접 경로 regression 근거 보강, (3) worker handoff 을 한 번에 마감. **Slice A 결과: 0 버그, 0 production 수정** — 8 경로(correction / delete / link / unlink / tacit / media / bootstrap preview / local dry-run simulator) Phase 1 Explore 전수 감사에서 mutex chain / setBundle guard / onError 경로 / null 체크 / useEffect deps / state sync 전부 이상 없음. **Slice B 결과: regression proof 비대칭 1 건 확인 → 최소 테스트 1 개 보강** — `completeSession` 은 flush-fail → error phase 테스트([practiceStore.test.ts:385-399](src/stores/__tests__/practiceStore.test.ts#L385-L399)) 보유, `abandonSession` 은 happy path(L402-416) 만 존재. 소스는 대칭([practiceStore.ts:255-273](src/stores/practiceStore.ts#L255-L273), `flushPersist` throw → `phase:'error'` + early return 으로 `updatePracticeSessionStatus` 미호출). 이미 승인된 동작 대비 test proof 만 빈약 → 지시서의 "최소 테스트 보강" 허용 범위 정확히 1 건에 해당. **Slice C 결과: 본 기록 + 필요 시 LEARNINGS/MISTAKES** — 신규 패턴/실수 없음. 변경 파일: `src/stores/__tests__/practiceStore.test.ts` 단일(기존 `describe('practiceStore — abandonSession')` 블록 내부에 `it('sets error phase when flush fails (fail-closed)', ...)` 1 건 append). 기존 happy-path `it` 블록 byte-for-byte 불변. 신규 it 블록은 completeSession fail-closed 테스트 pattern 을 pattern-for-pattern 복제 — `startSession` → `vi.clearAllMocks` → `mockUpsertPracticeNodeProgress.mockRejectedValueOnce(new Error('db write failed'))` → `placeIngredient(ING_RICE, LOC_PANTRY)` 로 persist 큐 오염 → `await abandonSession()` → 3 assertion(`phase === 'error'` / `error.toContain('persist flush failed')` / `mockUpdatePracticeSessionStatus not.toHaveBeenCalled`). 새 fixture / 새 mock / 새 import / 새 helper 0. production 코드(`practiceStore.ts` / `runtime.ts` / `PracticeSessionPage.tsx` / `PracticeAdminPage.tsx`) 전부 unmodified → 고위험 표기 불요. off-limits 영역(`queries.ts` / `adminView.ts` / `sessionView.ts` / `openStep.ts` / `phaseAdvance.ts`) 전부 unmodified. 3+ 파일 범위 초과 없음(write set 1 production-test + 1 worker-doc = 2). `extra_changes=false` — 허용 보조 파일 `LAST_SESSION.md` 만 touch, raw diff baseline 은 125 entry dirty 그대로 유지. Phase 0/4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`: Phase 0 baseline pass(831.64 KB, TASK-158 종료 크기 동일), Phase 4 build pass(831.64 KB 불변 — 프로덕션 bundle 영향 0 재확인), lint pass(useGameTick warning 1 건 pre-existing, 0 errors), `@self-reviewer` PASS(요구사항 3/3, 금지사항 위반 0, extra_changes 0, test 가 실제 에러 경로 정확히 검증), tsc 0 errors, targeted vitest `src/stores/__tests__/practiceStore.test.ts` 31/31 pass(30 + 신규 1), full vitest 173/173 pass(9 suites 전부 green). LEARNINGS/MISTAKES 갱신: 해당 없음 — 본 보강은 기존 학습 패턴(대칭 에러 처리 함수는 대칭 테스트로 증명 / fail-closed status update 호출 생략 검증 / persist 큐 오염으로 flush fail 재현)의 재사용에 불과. 새 패턴 · 새 실수 없음.

## 직전 작업
TASK-20260420-158: `/practice/admin` authoring/validation 체인 무결성 **감사(read-only)**. 목표는 TASK-110~153, 20260420-155~157 누적 확장의 핵심 invariants(mutex / setBundle 가드 / setSummary 호출 규칙 / preview helper 재사용) 이 실제 코드에서 닫혀 있는지만 기계 검증하고, 회귀가 재현될 때만 최소 polish 를 적용하는 것. 변경 파일: 코드 0. 허용 보조 파일 touch 는 `docs/worker/LAST_SESSION.md` 단일(본 기록). **Phase 0 3 축 baseline 캡처**: (a) `git status --short` 125 entry — 기존 tracked M 18 + 신규 untracked 107(`src/pages/practice/`·`src/lib/practice/`·`src/stores/practiceStore.ts`·`src/types/practice.ts`·`docs/practice/`·`.harness/*`·`supabase/migrations/008-009`·`vitest.config.ts`·`src/stores/__tests__/`·`src/pages/HomePage.{tsx,module.css}`). 플랜 작성 시점 124 에서 +1 은 harness 자체가 생성한 `.harness/eval_feedback/TASK-20260420-158.json` 으로 본 task 의 author write 가 아닌 harness 자동 메타 — drift 로 분류; (b) `Get-FileHash` SHA256 — LAST_SESSION `23255CCC...2677E5`, LEARNINGS `0473E05B...F5EE5`, MISTAKES `66C1EFA7...82ACB` 캡처; (c) Edit/Write write set — 본 기록 append 1 건 예정. **Phase 1 감사 결과(26 async mutation handler 전수)**: Scope A bundle-scoped 24 + Scope B menu-list-scoped 2. A-1 count-changing 14(`handleActionNodeCreateSubmit` L621 / `handleIngredientNodeCreateSubmit` L720 / `handleStepGroupCreateSubmit` L891-setSummary L946 / `handleStepGroupEditSave` L1023-setSummary L1080 / `handleLinkNodeSubmit` L1432 / `handleUnlinkNode` L1503 / `handleTacitItemCreateSubmit` L1593 / `handleTacitMediaCreateSubmit` L1699 / `handleDetachTacitMedia` L1798 / `handleDeletePureMediaTacit` L1875 / `handleDeleteTextTacit` L1958 / `handleDeleteStepGroup` L1989-setSummary L2045 / `handleDeleteActionNode` L2121 / `handleDeleteIngredientNode` L2464)는 `buildMenuStructureSummary`(adminView.ts L172-219) 소비 필드(`.length` count + per-group `nodeCount`/`textTacitCount`/`pureMediaTacitCount`/`linkedMediaCount` + 메뉴 메타) 영향분을 targeted update — 호출 맞음. A-2 row-meta-only 7(`handleActionNodeEditSave` L2209 / `handleActionStepEditSave` L2394 / `handleIngredientStepEditSave` L2547 / `handleIngredientNodeEditSave` L2621 / `handleTacitItemEditSave` L1148 / `handleMediaUrlEditSave` L1236 / `handlePureMediaTitleEditSave` L1323)는 row count 및 tacit_type 불변이라 setSummary 생략이 정답 — 호출 없음 확인. A-3 bundle-shape-irrelevant 3(`handleLocationCreateSubmit` L506 / `handleAddHopSubmit` L766 / `handleDeleteTailHop` L832)는 `bundle.locations` / `ingredient_nodes[].location_path` 가 summary 미소비 → setSummary 생략이 정답. Scope B 2(`handleEditSave` L448 / `handleCreateSubmit` L478)는 menus 리스트만 다루므로 `requestMenuId`/`selectedMenuIdRef` dual-guard 대상 아님(setter 내부 menuId 정합 + bundle 재로드 effect 가 approved truth). **dual-guard 정밀도**: 24 bundle-scoped 모두 setBundle 내부 `prev.menu.id === requestMenuId`(setSummary 호출자는 `prev.menuId === requestMenuId`) 적용. `selectedMenuIdRef.current === requestMenuId` ref guard 는 22 개가 직접 캡처, 2 개(L2394/L2547) 가 `commitStepNoEdit` L2288 helper 를 통해 invariant 공유, L832 `handleDeleteTailHop` 은 success 경로에 리셋할 UI state 없어 catch/finally 2 지점 적용의 최소 변형(setBundle menuId 가드로 성공 경로 scoping 확보). **Mutex 감사**: 4 node-level edit draft(action node/step + ingredient node/step) 상호 배타 체인 누수 0; 3 tacit/media edit draft 상호 배타 11-조건 체인(L1207-L1222, L1289-L1302) 상위 mutation flag 와 교차 차단 확인; `commitStepNoEdit` 단일 정의(L2288), 두 wrapper(L2397-2408, L2550-2561) 에서만 호출. **Preview/dry-run**: engine/runtime/sessionView helper 11 종(`bootstrapEngineState`/`tryPlaceIngredient`/`tryExecuteAction`/`tryPour`/`computeDerivedData`/`formatLegalAction`/`formatFriendlyAction`/`buildLocationLabelMap`/`pickRepresentativeAction`/`buildTacitDetailViewModel`/`buildNextGroupPreview`) 전부 import 재사용, 로직 복제 0; `handlePreviewActionClick` L2889 `place`/`action`/`pour` 3 case `r.allowed ? r.newState : prev` 패턴 완전 대응; 렌더 가드 4-point(L5445-5448) `bundle && bundle.menu.id === selectedMenuId && previewEngineState && previewEngineState.bundle === bundle` 유지. `PREVIEW_SENSORY_FIELDS`(L94) / `PreviewTacitSensoryNotes`(L103) / `PreviewTacitItemMedia`(L122) page-local 복제는 TASK-155 approved truth(sessionView 시그니처 변경 금지 + 범위 최소화) → 재논쟁 범위 밖. **결론: 회귀 재현 0, 코드 수정 불요.** Phase 0/4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`: Phase 0 baseline pass(831.64KB, TASK-157 종료 크기 동일 — 회귀 없음), Phase 4 도 코드 변경 0 이므로 동일 산출물 기대. LEARNINGS/MISTAKES 갱신: **해당 없음** — 신규 학습/에러 없음, 본 감사는 기존 학습 패턴(setSummary 호출 규칙 = builder 소비 필드 기반 / 선제 mutex 체인 / setBundle dual-guard / preview helper 재사용) 재검증에 불과.

## 직전 작업
TASK-20260420-157: `/practice/admin` 의 **action/ingredient parent `practice_recipe_nodes.step_no` correction save path 공통화**(동작 보존형 commonization). TASK-151(action step edit) + TASK-152(ingredient step edit) 로 중복된 두 `handle*StepEditSave` 의 save path 를 page-local helper 1 개로 축소 — 실제 차이는 `setBundle` mapper(`action_nodes` vs `ingredient_nodes`) 한 지점. 변경 파일: `src/pages/practice/PracticeAdminPage.tsx` 단일. queries.ts · adminView.ts · menuView.ts · sessionView.ts · runtime.ts · engine/* · CSS · router · store 전부 미터치. (a) type import 확장: 기존 `../../types/practice` type block 에 `PracticeRecipeNode` 1 줄 추가(helper 의 `mergeBundle(prev, updated: PracticeRecipeNode)` 파라미터용). (b) 컴포넌트 내부 async arrow helper `commitStepNoEdit` 1 개 신설(`handleActionStepEditStart` 직전 배치, 기존 save path 의 bundle/menu guard → `trim()` + `Number(trimmedStep)` + `Number.isInteger && >= 1` validation → `requestMenuId = selectedMenuId` 캡처 → `setSaving(true); setError(null);` → `try: await updatePracticeRecipeNodeStepNo({nodeId, stepNo})` → `setBundle(prev => !prev || prev.menu.id !== requestMenuId ? prev : mergeBundle(prev, updated))` → `selectedMenuIdRef.current === requestMenuId` 가드 하 `setDraft(null) + setError(null)` → catch `unknown` + `instanceof Error` 가드 + ref 가드 하 row-scoped `{nodeId, message}` 세팅 → finally ref 가드 하 `setSaving(false)` 전부 기존 두 handler 와 byte-for-byte 일치). signature: `{draft: {nodeId, stepNo: string}, setDraft, setSaving, setError, mergeBundle}` — React setter 는 개념상 `React.Dispatch<SetStateAction<T>>` 지만 helper 내부에서는 plain value 만 call 하므로 `(value: T | null) => void` 로 parameter 좁게 선언(함수 파라미터 contravariance 로 실제 setter 와 호환). (c) `handleActionStepEditSave` 를 wrapper 로 축소: `const draft = editingActionStepDraft; if (!draft) return; await commitStepNoEdit({draft, setDraft: setEditingActionStepDraft, setSaving: setEditingActionStepSaving, setError: setEditingActionStepError, mergeBundle: (prev, updated) => ({...prev, action_nodes: prev.action_nodes.map(a => a.node.id === draft.nodeId ? {...a, node: updated} : a)})})`. (d) `handleIngredientStepEditSave` 도 대칭 wrapper 로 축소 — mapper 만 `ingredient_nodes` / `n` 로 교체. ⚠️ **Start/Cancel 4 handler 일체 수정 금지** — mutex 체인이 kind-specific(ingredient 는 `showIngredientNodeInput` / `creatingIngredientNode` / `addHopTargetNodeId` / `creatingHop` / `deletingTailHopNodeId` 5 개를 추가 감시, action 은 `showActionNodeInput` / `creatingActionNode` 2 개). not-found 메시지 문구 literal 4 종(`대상 액션 노드를 찾지 못했습니다.` / `대상 재료 노드를 찾지 못했습니다.` / `step_no를 입력하세요.` / `step_no는 1 이상의 정수여야 합니다.`) 전부 유지. duplicate step_no 허용 유지(validation 에 중복 검사 없음). `setSummary` 신규 호출 0(summary 불변). state triple(`editingActionStepDraft / Saving / Error`, `editingIngredientStepDraft / Saving / Error`) 선언 유지(상태 통합 범위 밖). onChange 2 곳 유지. row-scoped error shape `{nodeId, message}` 동일. `setBundle` functional updater 가드 `!prev || prev.menu.id !== requestMenuId` 동일. 동작 등가성 17 포인트 체크(draft null 조기 return / bundle·menu guard / trimmed=='' 메시지 / 정수 검증 메시지 / duplicate 허용 / requestMenuId 캡처 시점 / setSaving·setError 순서 / await args / setBundle 가드 / action mapper / ingredient mapper / 성공 시 draft·error 정리 ref 가드 / catch 메시지 파생 / finally setSaving(false) ref 가드 / row error shape / onChange 계약 / Start·Cancel 4 handler 무변경) 전부 PASS. Phase 0/4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`: Phase 0 baseline pass(831.91KB), Phase 4 build pass(831.91→831.64KB, -0.27KB — 중복 제거 순영향), lint pass(기존 useGameTick warning 1 건만, 0 errors), `@self-reviewer` 1 차 PASS(요구사항 9/9, 금지사항 위반 0, extra_changes 0), tsc 0 errors. LEARNINGS/MISTAKES 갱신: 해당 없음 — page-local helper 추출은 기존 학습 패턴(selectedMenuIdRef stale guard / setBundle functional updater 가드 / row-scoped error / setSummary 호출 생략이 정답인 컬럼군)의 얇은 리팩토링에 불과. 새 패턴 없음.

## 직전 작업
TASK-20260420-156: `/practice/admin` 의 TASK-155 **bootstrap preview surface** 를 **로컬 드라이런 시뮬레이터**로 additive 확장. authored bundle 만으로 세션 생성 / DB write / practiceStore / Supabase / 새 query 없이, legal action 을 한 단계씩 클릭 적용 → progress / 현재 tacit / 다음 step preview 가 즉시 재계산되어 갱신. 변경 파일: `src/pages/practice/PracticeAdminPage.tsx` 단일. import 확장: 기존 `bootstrapEngineState` + `LegalAction` 옆으로 `tryPlaceIngredient` / `tryExecuteAction` / `tryPour` value + `PracticeEngineState` type 추가. 로컬 state 1 개 신설: `previewEngineState: PracticeEngineState | null`(`[bundle, setBundle]` 인접 배치). reset effect 1 개: `useEffect(..., [bundle, selectedMenuId])` — `bundle && bundle.menu.id === selectedMenuId` 이면 `setPreviewEngineState(bootstrapEngineState(bundle))`, 아니면 `null`. selectedMenuIdRef 계열 async stale guard 는 비동기 write 0 이므로 도입 금지(지시서 명시 제약). handler 2 개 신설: (a) `handlePreviewReset()` — `bundle && bundle.menu.id === selectedMenuId` 조건부 bootstrap 재실행; (b) `handlePreviewActionClick(action: LegalAction)` — functional updater + discriminated union switch(`place`/`action`/`pour`) → 각각 `tryPlaceIngredient`/`tryExecuteAction`/`tryPour` 호출, `r.allowed` 일 때만 `r.newState` 반영, 거부 시 prev 유지(사용자 명시 피드백 없음 — pure retry 가능). 기존 preview IIFE(line 5465~) 리팩터 요점: (1) **렌더 가드 강화** — `bundle && bundle.menu.id === selectedMenuId && previewEngineState && previewEngineState.bundle === bundle` 의 4번째 identity check 로 `setBundle(b)` 가 비동기로 객체 참조 교체한 직후 한 프레임 stale render 구멍 차단(effect 가 다음 프레임에 따라잡을 때까지 섹션 자체를 숨김). (2) `const previewState = bootstrapEngineState(bundle);` → `const previewState = previewEngineState;` 매 렌더 재계산 제거. TS narrowing 으로 non-null 단언 불요. (3) 헤딩 문구 `엔진 bootstrap 미리보기 (읽기 전용)` → `엔진 드라이런 시뮬레이터 (로컬 전용)` 1 줄 변경(`adminLinkNodeHeading` 유지). (4) 헤딩 직하 `retryButton` 클래스 재사용 reset 버튼 추가(`초기 상태로 리셋`). (5) progressText 확장 — `총 노드 N개 · 초기 legal action M개` → `진행률 satisfiedNodes/totalNodes · legal action M개` + `previewDerived.isComplete && <span className={statusBadge}>완료 가능</span>` 세션 페이지 완료 뱃지 미러. (6) 기존 `friendlyActionList` + `friendlyActionItem` 정보용 카드 블록은 **그대로 유지**(정적 `<div>`, TASK-155 승인 truth 뒤집지 않음) → 그 직후에 **신규 clickable 블록**으로 `actionList` + `actionButton` 추가(세션 페이지 L357-372 패턴 로컬 미러, 페이지 간 import 금지). `friendlyActionItem` 은 정적 카드 스타일 (PracticePlaceholder.module.css:305, `cursor:pointer` / `border` 없음) 이므로 버튼화 시 브라우저 기본 chrome 이 card 배경과 섞여 플랜 리뷰에서 교정됨 — `actionList` / `actionButton` 는 이미 클릭에 맞춰진 자산(L201/L209 cursor:pointer + border + hover). (7) 빈 상태 문구 미세 조정 — `초기 legal action이 없습니다` → `legal action이 없습니다 (노드/로케이션 구성을 확인하거나 리셋하세요)` 로 리셋 가능성 힌트 추가. `representativeAction` / `tacitPanel` / `nextPreviewCard` 3 서브블록 JSX 는 수정 0 — 입력 `previewState` 가 바뀌므로 클릭 시 자동 재계산. **금지사항 0**: DB write 0, 세션 API 0, Supabase 호출 0, practiceStore 참조 0, 새 query 0, 새 CSS 0, setSummary 호출 추가 0, CRUD handler 수정 0, authoring path 수정 0, setBundle write 경로 수정 0, 타 practice 페이지 import 0, any 타입 0, display:none 0, CSS 변수 인라인 하드코딩 0. PlaceResult/ActionResult/PourResult 는 `{allowed:true, newState} | {allowed:false, reason}` discriminated union 이므로 `r.allowed` narrowing 으로 any 불요. Phase 0/4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`: Phase 0 baseline pass(831.03KB), Phase 4 build pass(831.03→831.91KB, +0.88KB), lint pass(기존 useGameTick warning 1 건, 0 errors), `@self-reviewer` PASS(요구사항 7/7, 금지사항 위반 0, 강제 수정 A/B/C 전부 반영, TASK-155 truth 뒤집지 않음 확인, extra_changes 0), tsc 0 errors. LEARNINGS 갱신: **1 건 추가 — `setBundle` 비동기 참조 교체 + `useEffect` commit-after 타이밍 조합 시, 파생 로컬 state 를 effect 만으로 동기화하면 한 프레임 stale render 구멍이 생기므로 렌더 가드에 `derivedState.source === currentSource` identity check 를 효과와 쌍으로 배치**(이번 plan-review 교정 사례). MISTAKES 갱신: 해당 없음 — 초기 플랜에서 `friendlyActionItem` 을 `<button>` 으로 얹으려던 스타일 깨짐 시나리오는 플랜 리뷰 단계에서 차단되어 실제 코드에는 유입되지 않음.

## 직전 작업
TASK-20260420-155: `/practice/admin` 에 **엔진 bootstrap 미리보기(read-only) surface** 개통. authored bundle 만으로 세션 생성/DB write 없이 (1) 초기 legal actions, (2) 현재 스텝 tacit detail, (3) 다음 스텝 preview 3축을 즉시 확인. `src/pages/practice/PracticeAdminPage.tsx` 단일 파일. import 확장: (a) 기존 `../../types/practice` type block 에 `PracticeTacitMedia` 1줄 추가(`PracticeTacitMediaType` 은 기존 유지 — 두 타입 혼동 금지, helper 가 사용하는 것은 `PracticeTacitMedia`); (b) `../../lib/practice/engine` 에서 `bootstrapEngineState` value + `LegalAction` type; (c) `../../lib/practice/runtime` 에서 `computeDerivedData` value; (d) `../../lib/practice/sessionView` 에서 `formatLegalAction` / `formatFriendlyAction` / `buildLocationLabelMap` / `pickRepresentativeAction` / `buildTacitDetailViewModel` / `buildNextGroupPreview` values + `TacitDetailViewModel` / `NextGroupPreviewViewModel` types. 파일 상단(`MEDIA_TYPE_LABELS` 직후)에 presentational helper 2 개 신설 — `PREVIEW_SENSORY_FIELDS` 6 필드 상수(flame_level/color_note/viscosity_note/sound_note/texture_note/timing_note) + `PreviewTacitSensoryNotes({item})` + `PreviewTacitItemMedia({media})`. `PracticeSessionPage.tsx` L19-63 의 `TacitSensoryNotes` / `TacitItemMedia` 를 그대로 복제(공유 모듈화는 sessionView 시그니처 변경 금지 + 범위 최소화 이유로 이번 slice 범위 밖). preview section 삽입 위치: 렌더 JSX 의 `backButton`(기존 `연습 목록으로`) 바로 앞, 기존 authoring 블록(`bundle && bundle.menu.id === selectedMenuId` IIFE, L4169~5348) 완전 뒤. 가드는 동형 `{bundle && bundle.menu.id === selectedMenuId && (() => { ... })()}`. 블록 내부: `bootstrapEngineState(bundle)` → `computeDerivedData(state)` → `buildTacitDetailViewModel(state)` / `buildNextGroupPreview(state)` 로 3축 view model 선계산, `storeIngredientOptions` 로 `previewIngredientNames: Map<id, display_name>` 구성(누락 시 `formatFriendlyAction` 내부에서 raw id fallback), `buildLocationLabelMap(state.bundle.locations)` 로 location label map 구성. render 세부: `.adminLinkNodeSection`/`.adminLinkNodeHeading` 로 섹션 프레임, `.progressText` 로 `총 노드 N개 · 초기 legal action M개` 요약, `.representativeAction`/`.representativeLabel` 로 대표 초기 액션(`pickRepresentativeAction`), `.friendlyActionList`/`.friendlyActionItem` 로 전체 legal action + `.tacitSensoryTag` 로 raw `formatLegalAction` debug 라벨 병렬 노출. tacit detail 은 `.tacitPanel`/`.tacitHeader`/`.tacitStepNo`/`.tacitTitle`/`.tacitLocationBadge`/`.tacitSummary`/`.tacitItemList`/`.tacitItemCard`/`.tacitItemTitle`/`.tacitItemBody`/`.tacitEmptyHint` 체인. next preview 는 `.nextPreviewCard`/`.nextPreviewHeader`/`.nextPreviewLabel`/`.nextPreviewBody`/`.nextPreviewStepNo`/`.nextPreviewTitle`/`.nextPreviewLocationBadge`/`.nextPreviewSummary`/`.nextPreviewItemList`/`.nextPreviewItemTitle` 체인. 빈 상태 3 종: (a) legalActions.length === 0 → `.adminEmptyHint` 로 "초기 legal action이 없습니다 (노드/로케이션 구성을 확인하세요)"; (b) `tacitDetail == null` (step_group 없음 / 모든 group 의 node 가 비어 있음) → `.adminEmptyHint` 로 "활성 스텝 그룹이 없습니다 (스텝 그룹 + 노드 연결을 추가하세요)"; (c) `nextPreview == null` → 카드 자체 omit. ⚠️ **read-only surface** — preview 블록 내부 onClick/onChange/onSubmit/ref mutating 일체 없음(grep 0건), `setSummary`/Supabase/`setBundle`/practiceStore 호출 0, session/DB write 0. `bootstrapEngineState` 는 pure(engine/types.ts L73-96, bundle → empty `ingredient_instances` + 모든 node `is_satisfied:false` 인 `node_progress`), `computeDerivedData` 는 pure(runtime.ts L115-130, `computeLegalActions` + progress count), `buildTacitDetailViewModel` / `buildNextGroupPreview` 는 pure(sessionView.ts). 렌더 사이클마다 `bootstrapEngineState` 재계산이지만 결과 shape 이 작고 next cycle 에 버려지므로 memoization 불요. 기존 authoring 블록(line 4169~5348) JSX / `setSummary` mutation 경로 / authoring mutex / practiceStore / sessionView helper signatures / `src/lib/practice/*` / CSS / router / DB schema / queries 전부 불변. Phase 4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`: build pass(827.33→831.03KB, +3.70KB), lint pass(기존 useGameTick warning 1 건, 0 errors), `@self-reviewer` PASS(요구사항 7/7, 금지사항 위반 0, extra_changes 0 — 세션 baseline 대비 신규 변경은 `src/pages/practice/PracticeAdminPage.tsx` 단일, `src/pages/practice/` 전체가 baseline 시점 untracked 였으므로 diff 는 전체 파일 추가로 보이는 것이 정상), tsc 0 errors. LEARNINGS/MISTAKES 갱신: 해당 없음 — 기존 학습 패턴(pure runtime helper 재사용 / sessionView view model 재조립 / 기존 CSS 클래스만 사용 / snake_case `display_name` 소비 / read-only preview 분리) 의 조합 재사용. 새 패턴 없음.

## 직전 작업
TASK-20260420-153: `/practice/admin` 의 **ingredient node child `practice_ingredient_nodes` 3컬럼(`ingredient_id` / `is_deco` / `quantity`) inline correction** 경로 개통. TASK-149 action child-only edit 패턴을 ingredient 도메인으로 미러. parent `practice_recipe_nodes.step_no`는 TASK-152 에서 이미 개통됨 — 본 task 는 child 도메인만 터치, `n.node` spread 보존. `src/lib/practice/queries.ts` 에 `UpdatePracticeIngredientNodeInput { nodeId, ingredientId, isDeco, quantity }` + `updatePracticeIngredientNode(input): Promise<PracticeIngredientNode>` helper 1 개 신설(`updatePracticeActionNode` 직후, `deletePracticeRecipeNode` 직전 삽입 — action/ingredient child update 헬퍼 도메인 쌍). 본문 `.from('practice_ingredient_nodes').update({ingredient_id, is_deco, quantity}).eq('node_id', input.nodeId).select('node_id, ingredient_id, is_deco, quantity').single()` + `raise('updatePracticeIngredientNode', ...)` — 단일 구문, 부모 테이블 접근/rollback/도메인 검증 미소유. `src/pages/practice/PracticeAdminPage.tsx` 에 import 1 줄(`updatePracticeIngredientNode` — `updatePracticeActionNode` 인접) + state 3 개(`editingIngredientNodeDraft { nodeId, ingredientId, isDeco, quantity:string }` / `editingIngredientNodeSaving` / `editingIngredientNodeError { nodeId, message }`) + Effect 2 reset 3 줄 + handler 3 개(`EditStart` / `EditCancel` / `EditSave`) + ingredient row 4-way 분기(hop form → step edit form → **child edit form (NEW)** → viewMode) + `재료 편집` 버튼 신설(`stepNo 편집` 옆) + 상호배타 mutex 16 곳. EditStart 는 17 조건 mutex(self 포함) + `storeIngredientOptions.some(o => o.id === target.ingredient.ingredient_id) ? orig : ''` stale FK fallback(LEARNINGS 2026-04-19 패턴). EditSave validation 은 `handleIngredientNodeCreateSubmit`(L590-599) byte-for-byte 미러 — (1) `draft.ingredientId === ''` → `'재료를 선택하세요.'`; (2) `!storeIngredientOptions.some(o => o.id === draft.ingredientId)` → `'선택된 재료가 현재 매장 목록에 없습니다.'` ⚠️ **엔진 legality 방어선**; (3) `quantity.trim() === ''` → `'quantity를 입력하세요.'`; (4) `!Number.isFinite(parsed) || parsed < 0` → `'quantity는 0 이상의 숫자여야 합니다.'`. `isDeco` validation 없음(boolean). `quantity: string` draft 는 action edit `duration: string`(L292) 동형 — 타이핑 중 `''` 허용, save 시 `Number()` 캐스팅. ⚠️ **`setSummary` 호출 0, full rebuild 금지** — `buildMenuStructureSummary`(adminView.ts L172-219) 가 `ingredient_id/is_deco/quantity` 미소비(`ingredientNodeCount = bundle.ingredient_nodes.length` count-only). setBundle 은 `ingredient_nodes.map` 으로 같은 nodeId 만 in-place `{...n, ingredient: updated}` replace — `n.node`(step_no 포함) / `n.location_path` / 다른 ingredient_nodes / action_nodes / step_groups / step_group_nodes / tacit_items / tacit_media / locations 전부 spread 보존. row(`buildAdminIngredientNodeList` adminView.ts:462-484) + drilldown(`buildStepGroupDrilldown` adminView.ts:274-286) 두 surface 가 모두 `n.ingredient.*` 3필드를 직접 소비 → 다음 render 사이클에 즉시 동기화. plan_review 1 차 피드백 반영: (a) JSX `<option>` label 필드는 snake_case `display_name`(queries.ts L803 `StoreIngredientOption`), `displayName` 아님 — 기존 create form(L3561-3565) 선례 확인; (b) action row 3 버튼(stepNo 편집 L3260-3265 / 편집 L3274-3279 / × 삭제 L3288-3293) 도 UI disabled 에 `editingIngredientNodeDraft/Saving` append — smoke path "action row 편집/삭제 모두 disabled 확인" 과 일치. mutex 16 곳 내역: handler 7(unlink / deleteAction / actionNodeEdit / actionStepEdit / deleteIngredient / ingredientStepEdit / ingredientNodeEdit self) + action row 3 + ingredient row 4(stepNo 편집 / 재료 편집 / + hop 추가 / × 마지막 hop 제거 / × 삭제 중 self 제외 4) + page-level 2(+재료 노드 추가 / +노드 연결) + drilldown ingredient × 연결 해제 1 = 16. `+ 액션 노드 추가` 는 지시서 상호 배타 8 경로 미포함으로 범위 밖 유지. drilldown action × 연결 해제(L4725-4731)는 `handleUnlinkNode` 경유이므로 handler 선제 가드만으로 충분 — 미터치. queries 외 adminView/menuView/sessionView/engine/store/router/CSS 전부 불변. Phase 4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`: build pass(823.71→827.33KB, +3.62KB), lint pass(기존 useGameTick warning 1 건, 0 errors), `@self-reviewer` PASS(요구사항 14/14 충족, 금지사항 위반 0, extra_changes 0 — 세션 baseline 대비 신규 변경은 queries.ts + PracticeAdminPage.tsx 2 파일만), tsc 0 errors. LEARNINGS/MISTAKES 갱신: 해당 없음 — 기존 학습 패턴(single-column-set update helper / row-scoped error marker / stale async guard 3-point / stale FK fallback / setSummary 호출 생략이 정답인 컬럼군 / 3-way → 4-way 분기 render 전환 / mutex chain 일관성 / snake_case DB 필드명 확인) 의 조합 재사용.

## 직전 작업
TASK-20260420-152: `/practice/admin` 의 **ingredient node parent `practice_recipe_nodes.step_no` inline correction** 경로 개통. TASK-151 action step edit 패턴을 ingredient 도메인으로 미러 — 기존 hop add / × 마지막 hop 제거 / × 삭제 / drilldown × 연결 해제 경계 전부 유지, row 표시와 drilldown 표시 즉시 동기화. `src/lib/practice/queries.ts` 의 `updatePracticeRecipeNodeStepNo({nodeId, stepNo})` 기존 helper 재사용 — 신규 helper / 공통화 리팩토링 0. `src/pages/practice/PracticeAdminPage.tsx` 에 state 3 개(`editingIngredientStepDraft {nodeId, stepNo}` / `editingIngredientStepSaving` / `editingIngredientStepError {nodeId, message}`) + Effect 2 reset 3 줄 + handler 3 개(`EditStart` / `EditCancel` / `EditSave`) + ingredient row 3-way 분기(hop form → step edit form → viewMode) + 상호 배타 가드 11 곳. validation 은 TASK-151 action step 과 동형 — `Number(trimmedStep) + Number.isInteger(stepNo) && stepNo >= 1`. `parseInt` 절삭 미사용(step_no 는 `openStep.ts` / `phaseAdvance.ts` 엔진 순서 계산의 직접 입력이므로 절삭은 오염). **duplicate step_no 검증 없음** — DB uniqueness 무 가정, 제약 "duplicate 허용 유지" 준수. ⚠️ **`setSummary` 호출 0, full rebuild 금지** — `MenuStructureSummary.groups[]` 은 per-node step_no 미보유(LEARNINGS 2026-04-19 "path 카운트를 보유하지 않는 summary 는 …"). setBundle 은 `ingredient_nodes.map` 으로 같은 nodeId 만 in-place `{...n, node: updated}` replace — `n.ingredient`(child) / `n.location_path` / 다른 ingredient_nodes / action_nodes / step_groups / step_group_nodes / tacit_items / tacit_media / locations 전부 spread 보존. row(`buildAdminIngredientNodeList` adminView.ts:468) + drilldown(`buildStepGroupDrilldown`) 두 surface 가 모두 `n.node.step_no` 를 직접 소비 → 다음 render 사이클에 재정렬 포함 즉시 동기화. queries/adminView/menuView/sessionView/engine/store/router/CSS 전부 불변. Phase 4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`: build pass(821.47→823.71KB, +2.24KB), lint pass(기존 useGameTick warning 1 건만, 0 errors), `@self-reviewer` PASS(요구사항 15/15 충족, 금지사항 위반 0, extra_changes 0), tsc 0 errors. LEARNINGS/MISTAKES 갱신: 해당 없음 — 기존 학습 패턴(single-column update helper / row-scoped error marker / selectedMenuIdRef 가드 / `Number+Number.isInteger` vs `parseInt` 판단 / setSummary 호출 생략이 정답인 컬럼군 / 3-way 분기 render) 의 조합 재사용.

## 직전 작업
TASK-20260420-151: `/practice/admin` 의 **action node parent `practice_recipe_nodes.step_no` inline correction** 경로 개통. TASK-149 child-only edit(`editingActionNodeDraft`) 경계를 그대로 보존한 채, 별도 state triple · 별도 handler · 별도 save path 로 parent row 의 `step_no` 단일 컬럼만 교정 가능. `src/lib/practice/queries.ts` 에 `UpdatePracticeRecipeNodeStepNoInput { nodeId, stepNo }` + `updatePracticeRecipeNodeStepNo(input): Promise<PracticeRecipeNode>` helper 1 개 신설(`deletePracticeRecipeNode` 블록 직후, `.from('practice_recipe_nodes').update({ step_no: input.stepNo }).eq('id', input.nodeId).select('id, menu_id, node_type, step_no').single()` + `raise` — 단일 컬럼 update, 부모 테이블 generic primitive, action/ingredient 구분은 호출자 책임). `src/pages/practice/PracticeAdminPage.tsx` 에 state 3 개(`editingActionStepDraft { nodeId, stepNo }` / `editingActionStepSaving` / `editingActionStepError { nodeId, message }`) + Effect 2 reset 3 줄 + handler 3 개(`EditStart` / `EditCancel` / `EditSave`) + action row 3-way 분기(child edit form → step edit form → display) + 상호배타 gate 7 곳(`handleActionNodeEditStart` / `handleDeleteActionNode` early-return / stepNo 편집 버튼 / 편집 버튼 / × 삭제 버튼 / `+ 액션 노드 추가` / `+ 노드 연결` / drilldown `× 연결 해제`). validation 은 `handleActionNodeCreateSubmit`(L459-L467) 과 동형 — `Number(trimmedStep) + Number.isInteger(stepNo) && stepNo >= 1`. `parseInt` 절삭(`1.5→1`, `1abc→1`) 미사용 — step_no 가 `openStep.ts:20` / `phaseAdvance.ts:29` 엔진 순서 계산의 직접 입력이므로 절삭은 correction 이 아닌 오염. **duplicate step_no 검증 없음** — DB uniqueness 무 가정, 제약 "duplicate 허용 유지" 준수. ⚠️ **`setSummary` 호출 0, full rebuild 금지** — `MenuStructureSummary.groups[]` 은 per-node step_no 미보유(LEARNINGS 2026-04-19 "path 카운트를 보유하지 않는 summary 는 …" 와 동형). setBundle 은 `action_nodes.map` 으로 같은 nodeId 만 in-place `{...a, node: updated}` replace — `a.action`(child) + 다른 action_nodes / ingredient_nodes / step_groups / step_group_nodes / tacit_items / tacit_media / locations 전부 spread 보존. row(`buildAdminActionNodeList` adminView.ts:389) + drilldown(`buildStepGroupDrilldown` adminView.ts:293) 두 surface 가 모두 `n.node.step_no` 를 직접 소비 → 다음 render 사이클에 재정렬 포함 즉시 동기화. adminView/menuView/sessionView/engine/store/router/CSS 전부 불변. Phase 4 순서 `npm run build` → `npm run lint` → `@self-reviewer` → `npx tsc --noEmit`: build pass(819.10→821.47KB), lint pass(기존 useGameTick warning 1 건, 0 errors), `@self-reviewer` PASS(요구사항 7/7, 금지사항 위반 0, extra_changes 0), tsc 0 errors. LEARNINGS/MISTAKES 갱신: 해당 없음 — 기존 학습 패턴(single-column update helper / row-scoped error marker / selectedMenuIdRef 가드 / `Number+Number.isInteger` vs `parseInt` 판단 / setSummary 호출 생략이 정답인 컬럼군) 의 조합 재사용.

## 직전 작업
TASK-20260419-149: `/practice/admin` 의 **action node row inline edit** 경로 개통. action_type / location_id / duration_sec 3개 컬럼을 child table `practice_action_nodes` **single-table update** 로 즉시 교정. step_no 는 범위 밖(다음 slice). `src/lib/practice/queries.ts` 에 `updatePracticeActionNode({nodeId, actionType, locationId, durationSec})` helper 1 개 신설(`.update({action_type, location_id, duration_sec}).eq('node_id', nodeId).select('node_id, action_type, location_id, duration_sec').single()` + `raise` 단일 구문 — parent `practice_recipe_nodes` 접근/rollback/도메인 검증 미소유). `src/pages/practice/PracticeAdminPage.tsx` 에 state 3 개(`editingActionNodeDraft` / `editingActionNodeSaving` / `editingActionNodeError`) + Effect 2 reset 3 줄 + handler 3 개(`EditStart` / `EditCancel` / `EditSave`) + action row view/edit 이원 분기(createMenuForm · metaEdit\* 재사용, 인라인 style 0) + 충돌 gate 3 곳(`+ 액션 노드 추가` / `+ 노드 연결` / drilldown action `× 연결 해제`) 확장. ⚠️ **`setSummary` 호출 0, summary full rebuild 금지** — row 수 불변(카운트 영향 없음). setBundle 은 `action_nodes.map` 으로 해당 nodeId 만 in-place replace(spread 로 `node` 보존, `action` 만 updated 교체). ingredient/tacit/media/step-group unrelated gate 미터치. adminView/menuView/sessionView/CSS 전부 불변.

## 이전 작업 (TASK-150)
TASK-20260419-150: `/practice`, `/practice/menu/:menuId` 가 store 미선택 시 `/sim/join` 으로 튕기던 문제 해소. router.tsx 에서 두 라우트를 `<ProtectedRoute requireStore />` 블록에서 꺼내 `<ProtectedRoute />`(기본 requireAuth=true) 아래로 이동 → 로그인 보호만 유지. `/practice/session/:sessionId`(requireStore requireUser), `/practice/admin`(requireStore requireUser requireAdmin) 블록은 불변. PracticePage.tsx: `!selectedStore` 에서 `return null` → subtitle 안내 문구 + 홈 버튼 렌더하는 early return 분기로 교체(로딩 문구와 충돌 방지). PracticeMenuPage.tsx: early return 조건을 `!menuId` 로 축소(bundle fetch 는 menuId 만으로 동작), `연습 시작` disabled 에 `!selectedStore` 추가, store 미선택 시 hintText 안내 추가. 기존 `selectedStore && !selectedUser` 힌트는 보존. handleStart 내부 `selectedUser && selectedStore` 가드는 불변. CSS 무수정.

## 완료된 것 (이번 세션, TASK-149)
- `src/lib/practice/queries.ts`:
  - `UpdatePracticeActionNodeInput { nodeId: string; actionType: PracticeActionType; locationId: string; durationSec: number | null }` + `updatePracticeActionNode(input): Promise<PracticeActionNode>` 신설 — `createPracticeActionNode` 블록 직후, `DeletePracticeRecipeNodeInput` 직전 배치(action 도메인 인접). 본문은 `.from('practice_action_nodes').update({action_type: input.actionType, location_id: input.locationId, duration_sec: input.durationSec}).eq('node_id', input.nodeId).select('node_id, action_type, location_id, duration_sec').single()` + `raise('updatePracticeActionNode', error.message)`. **도메인 경계 소유 금지** — `practice_recipe_nodes` 접근/rollback/중복검증/배치 처리 일체 없음. `node_id` 가 PK(child-per-recipe-node) 이므로 단일 row hit 보장. `updatePracticeStepGroupMeta` / `updatePracticeTacitItem` / `updatePracticeTacitMediaUrl` 와 동형 패턴.
- `src/pages/practice/PracticeAdminPage.tsx`:
  - imports: value `updatePracticeActionNode` 추가(`deletePracticeStepGroup` 바로 뒤, `fetchStoreIngredientOptions` 앞).
  - state 3 개 신설(`deleteActionNodeError` 블록 직후 배치):
    * `editingActionNodeDraft: { nodeId: string; actionType: PracticeActionType; locationId: string; duration: string } | null` — 편집 대상 식별 + draft 값. duration 은 문자열('' 허용) 유지로 빈 입력 → null 변환.
    * `editingActionNodeSaving: boolean` — in-flight marker + 저장 중 cancel/재클릭 차단.
    * `editingActionNodeError: { nodeId: string; message: string } | null` — row-scoped 에러(TASK-147/148 패턴 미러).
  - Effect 2 reset 블록: `setDeleteActionNodeError(null)` 바로 뒤에 `setEditingActionNodeDraft(null); setEditingActionNodeSaving(false); setEditingActionNodeError(null);` 3 줄 추가(메뉴 전환 시 편집 draft/saving/error 동시 해제).
  - handler 3 개 신설(`handleDeleteActionNode` 바로 뒤):
    * `handleActionNodeEditStart(item)` — **10 선제 가드**: `showActionNodeInput` / `creatingActionNode` / `showLinkNodeInput` / `linkingNode` / `unlinkingNodeId !== null` / `deletingActionNodeId !== null` / `deletingIngredientNodeId !== null` / `deletingStepGroupId !== null` / `editingActionNodeDraft !== null`(self) / `editingActionNodeSaving`. `bundle && bundle.menu.id === selectedMenuId` + `selectedMenuId` null guard. target 조회: `bundle.action_nodes.find(a => a.node.id === item.nodeId)`. 없으면 `setEditingActionNodeError({nodeId, message: '대상 액션 노드를 찾지 못했습니다.'})` + early return. draft 채움: `actionType: target.action.action_type`, `locationId: bundle.locations.some(l => l.id === target.action.location_id) ? target.action.location_id : ''` (LEARNINGS 2026-04-19 "stale FK 를 EditStart 에서 '' 로 fallback" 패턴 적용 — race safe), `duration: target.action.duration_sec == null ? '' : String(...)`. `setEditingActionNodeError(null)` 동반.
    * `handleActionNodeEditCancel()` — `if (editingActionNodeSaving) return;` 후 draft/error reset.
    * `handleActionNodeEditSave()` — validation 은 `handleActionNodeCreateSubmit` 을 step_no 만 제외하고 그대로 미러: (1) `!PRACTICE_ACTION_TYPES.includes(draft.actionType)` → 'action_type이 올바르지 않습니다.'; (2) `draft.locationId === ''` → 'location을 선택하세요.'; (3) `!bundle.locations.some(l => l.id === draft.locationId)` → '선택된 location이 현재 메뉴에 없습니다.'; (4) duration trim → `''` 이면 `durationSec=null`, 아니면 `Number(trimmed)` + `Number.isFinite` + `>= 0` 검증 → 실패 시 'duration_sec는 0 이상의 숫자여야 합니다.' TASK-128 stale guard: `const requestMenuId = selectedMenuId; setEditingActionNodeSaving(true); setEditingActionNodeError(null);` → `await updatePracticeActionNode(...)`. 성공 시 `setBundle(prev => prev && prev.menu.id === requestMenuId ? {...prev, action_nodes: prev.action_nodes.map(a => a.node.id === draft.nodeId ? {...a, action: updated} : a)} : prev)` — ⚠️ **in-place map only, `{...a, action: updated}`** 으로 `a.node`(recipe node) 보존. 다른 action_nodes/ingredient_nodes/step_groups/step_group_nodes/tacit_items/tacit_media/locations 전부 spread 보존. ⚠️ **`setSummary` 호출 없음** — action_type/location_id/duration_sec 변경은 actionNodeCount/totalNodes/groups[].nodeCount 카운트에 영향 없음(LEARNINGS 2026-04-19 "path 카운트를 보유하지 않는 summary 는 삭제/추가 시 불변 유지" 와 동형). `selectedMenuIdRef.current === requestMenuId` 가드 하 draft/error null. catch: `unknown` + `instanceof Error` 가드 → `setEditingActionNodeError({nodeId, message})`. finally: 같은 가드 하 saving=false.
  - action row render(기존 L2571-2599) 을 view/edit 이원 분기로 교체:
    * edit mode(`editingActionNodeDraft?.nodeId === item.nodeId`): `createMenuForm` wrapper + 3 필드(action_type select / location select / duration_sec input) — `metaEditLabel` / `metaEditInput` 재사용, `value` → functional updater `setEditingActionNodeDraft(d => d ? {...d, ...} : d)`, `disabled={editingActionNodeSaving}`. error 는 `editingActionNodeError?.nodeId === item.nodeId && <p className={styles.errorText}>` 로 같은 행 form 내부에 노출. `metaEditActions` 내부 `저장` / `취소` 버튼(saving 중 disabled + label '저장 중...').
    * view mode: 기존 label/detail span + `metaEditActions` 안에 **`편집` 버튼 추가(`metaEditSaveBtn` 스타일, `× 삭제` 앞)** + 기존 `× 삭제` 버튼. 두 버튼 모두 `disabled={deletingActionNodeId !== null || editingActionNodeDraft !== null || editingActionNodeSaving}` — 다른 row 편집/삭제 in-flight 중 클릭 차단. `deletingActionNodeId === item.nodeId` → '삭제 중…', `deleteActionNodeError?.nodeId === item.nodeId && deletingActionNodeId === null` → error div 기존 그대로 유지. 추가로 `editingActionNodeError?.nodeId === item.nodeId && editingActionNodeDraft === null && !editingActionNodeSaving` → error div(편집 취소/성공 후 stale 에러 노출용, saving/form 중에는 form 내부로만 출력해 중복 회피).
    * **CSS 파일 수정 없음 · 인라인 `style=` 없음** — createMenuForm / metaEditLabel / metaEditInput / metaEditActions / metaEditSaveBtn / metaEditCancelBtn / errorText / nodeSummary / adminNodeItem / adminNodeLabel / adminNodeDetail 재사용. MISTAKES 2026-04-18 인라인 style 재발 방지.
  - 충돌 gate 3 곳 확장:
    * `+ 액션 노드 추가` 버튼 gate: `hasLocations && !showActionNodeInput && deletingActionNodeId === null` 뒤에 `&& editingActionNodeDraft === null && !editingActionNodeSaving` append.
    * `+ 노드 연결` 버튼 gate: 기존 10-조건 체인 끝에 `&& editingActionNodeDraft === null && !editingActionNodeSaving` append. unrelated tacit/media/step-group 조건 byte-for-byte 불변.
    * drilldown action `× 연결 해제` 버튼 disabled(`unlinkingNodeId !== null || deletingActionNodeId !== null`) 에 `|| editingActionNodeDraft !== null || editingActionNodeSaving` append. ingredient drilldown unlink 버튼(같은 파일 별 분기)은 지시서 메모 5 준수로 미터치.
  - adminView.ts/menuView.ts/sessionView.ts/engine/CSS 전부 미수정. TASK-148 ingredient delete, TASK-147 action delete 핸들러/게이트 byte-for-byte 불변. `handleActionNodeCreateSubmit` / `handleDeleteActionNode` 자체도 불변.
- Phase 4 결과:
  - `npm run build`: pass (815.14KB → 818.73KB, +3.59KB)
  - `npm run lint`: pass (기존 useGameTick warning 1 건만, 변경 파일 무관 · 0 errors)
  - `@self-reviewer`: PASS 10/10 요구사항 충족, 금지사항 위반 없음, extra_changes 없음
  - `npx tsc --noEmit`: 0 errors
  - targeted vitest 5 suite: 132/132 pass (adminView 40 + sessionView 42 + menuView 38 + phaseAdvance 6 + ingredientAdd 6)
- 활성화 경로:
  - **action row inline edit**: `buildAdminActionNodeList(bundle)` 가 `bundle.action_nodes` 에서 derive → `setBundle` map 으로 같은 nodeId 만 교체 → 다음 렌더에서 label/detail 갱신. nodeId 유지(삭제-재생성 회피) → drilldown step_group_nodes 연결/tacit 첨부 전부 보존.
  - **summary 정합**: `buildMenuStructureSummary` 는 action 의 3 컬럼을 카운트 재료로 쓰지 않음(actionNodeCount = length, totalNodes = sum). 따라서 edit save 는 summary 불변이 정답 — `setSummary` 호출 0.
  - **동시성 차단 매트릭스**: EditStart 10 가드 + EditSave 내부 saving flag + view-mode 두 버튼 disable + 3 외부 gate + drilldown action unlink disable → 편집 중 다른 액션-row edit/삭제/노드 연결/추가/drilldown unlink 전부 잠김. ingredient/tacit/media/step-group 경로는 의도적 미터치.
  - **stale drift 방어**: `requestMenuId` 캡처 + `selectedMenuIdRef.current === requestMenuId` 3 지점(success/catch/finally) + setBundle functional updater 내부 `prev.menu.id === requestMenuId` → 메뉴 전환 중 race 시 bundle/error/saving flag 누수 0. EditStart 의 stale FK locationId fallback(`.some(l => l.id === ...)` 검증 후 '' ) 으로 다른 클라이언트가 location 삭제 직후 편집 진입해도 select 옵션 missing 경고 회피.
- LEARNINGS/MISTAKES 갱신: 해당 없음 — 이번 slice 는 기존 학습된 패턴(single-table update helper / row-scoped error marker / 10-가드 체인 / stale guard / stale FK fallback / `setSummary` 호출 생략이 정답인 컬럼군) 의 조합 재사용. 새 패턴 없음.

## 이전 작업
TASK-20260419-148: /practice/admin의 **ingredient node parent delete** 경로 개통. ingredient node row 에 `× 삭제` 버튼을 노출, TASK-147 에서 이미 신설된 `deletePracticeRecipeNode({ nodeId })` helper 를 그대로 재사용. DB cascade 로 `practice_recipe_nodes` → `practice_ingredient_nodes`(008 FK), `practice_node_location_path`(008 FK), `practice_step_group_nodes`(008 FK) 연쇄 삭제. NON-CASCADE FK(`practice_ingredient_instances.ingredient_node_id`, `practice_node_progress.node_id`) 가 참조 중이면 PostgreSQL 23503 raise → caller catch → row-scoped inline error 로만 노출(session cleanup 으로 우회 절대 금지). queries/adminView/menuView/sessionView/CSS 전부 미수정 — 단일 write set = PracticeAdminPage.tsx. summary 는 `ingredientNodeCount-1`, `totalNodes-1`, linkedGroup 존재 시 해당 `groups[].nodeCount-1` 만 targeted update. setBundle 은 `ingredient_nodes` + `step_group_nodes` 둘 다 filter(unlinked option 재출현 방지). self-reviewer 1차 지적 반영 — ingredient unlink 와 parent delete 동시 실행 시 양쪽 모두 `nodeCount -= 1` 적용해 summary drift 발생하므로 `handleUnlinkNode` early-return + drilldown `× 연결 해제` 버튼 disabled 양쪽에서 `deletingIngredientNodeId !== null` 으로 lock.

## 완료된 것 (이번 세션, TASK-148)
- `src/pages/practice/PracticeAdminPage.tsx`:
  - state 2 개 신설(`deleteActionNodeError` 블록 직후 배치):
    * `deletingIngredientNodeId: string | null` — in-flight marker.
    * `deleteIngredientNodeError: { nodeId: string; message: string } | null` — row-scoped 에러.
  - handler 1 개 신설(`handleDeleteActionNode` 바로 뒤, Effect 1 직전): `handleDeleteIngredientNode(nodeId)`.
    * **11+self 선제 가드**: showIngredientNodeInput / creatingIngredientNode / addHopTargetNodeId!==null / creatingHop / deletingTailHopNodeId!==null / showLinkNodeInput / linkingNode / unlinkingNodeId!==null / deletingActionNodeId!==null / deletingIngredientNodeId!==null(self) / deletingStepGroupId!==null. hop correction 3종 state 포함 — hop 와 delete race 방어(지시서 주의 1).
    * target lookup: `bundle.ingredient_nodes.find(n => n.node.id === nodeId)`. 없으면 row error + early return.
    * linkedGroup snapshot 캡처(delete 이전): `bundle.step_group_nodes.find(sgn => sgn.node_id === nodeId)?.step_group_id ?? null`.
    * TASK-128 stale-guard: `requestMenuId = selectedMenuId` + `selectedMenuIdRef.current === requestMenuId` + functional updater `prev.menu.id === requestMenuId` / `prev.menuId === requestMenuId`.
    * `await deletePracticeRecipeNode({ nodeId })` — 기존 helper 재사용, 신규 helper 0.
    * setBundle: `ingredient_nodes` filter + `step_group_nodes` filter(unlinked 재출현 방지).
    * setSummary: `ingredientNodeCount-1` + `totalNodes-1` + linkedGroupId!=null 시 `groups.map(g => g.groupId === linkedGroupId ? {...g, nodeCount:-1} : g)`. 나머지 필드(stepGroupCount/actionNodeCount/tacit*/linkedMediaCount/groups[].textTacitCount 등) 전부 spread 보존. **full rebuild 금지.**
    * catch: `unknown` + `instanceof Error` 가드 → row error {nodeId, message}. session cleanup 없음.
  - `handleUnlinkNode` early-return 보강: `if (unlinkingNodeId !== null)` → `if (unlinkingNodeId !== null || deletingIngredientNodeId !== null || deletingActionNodeId !== null)`. action 쪽도 대칭 포함(drift 방어 일반화).
  - Effect 2 reset 블록: `setDeleteActionNodeError(null)` 바로 뒤에 `setDeletingIngredientNodeId(null); setDeleteIngredientNodeError(null);` 2 줄 추가.
  - ingredient row view-mode branch UI 보강(`metaEditActions` 내부):
    * `+ hop 추가` disabled chain 확장: `... || deletingIngredientNodeId !== null || deletingActionNodeId !== null` (hop correction 직렬화).
    * `× 마지막 hop 제거` disabled chain 동일 확장.
    * `× 삭제` 버튼 추가: `disabled={addHopTargetNodeId !== null || deletingTailHopNodeId !== null || deletingIngredientNodeId !== null}`.
    * `deletingIngredientNodeId === item.nodeId && <span>삭제 중…</span>` — in-flight indicator.
    * `deleteIngredientNodeError?.nodeId === item.nodeId && deletingIngredientNodeId === null && <div>{msg}</div>` — row-scoped error(in-flight 중에는 미노출).
    * hop form branch(`addHopTargetNodeId === item.nodeId`) 에는 delete 버튼 미노출(지시서 메모 4).
  - 충돌 게이트 2 곳:
    * `+ 재료 노드 추가` 버튼 gate: `canCreate && !showIngredientNodeInput`에 `&& deletingIngredientNodeId === null` append.
    * `+ 노드 연결` 버튼 gate: 기존 8-조건 체인 끝에 `&& deletingIngredientNodeId === null` append. tacit/media/step-group unrelated gate 미터치(지시서 메모 5).
  - drilldown ingredient `× 연결 해제` 버튼 disabled chain 보강(self-reviewer 1차 지적): `disabled={unlinkingNodeId !== null}` → `disabled={unlinkingNodeId !== null || deletingIngredientNodeId !== null}`. action 쪽은 이미 `deletingActionNodeId !== null` 포함 — 기존 유지.
  - **CSS 파일 수정 없음** — `.metaEditCancelBtn` / `.metaEditActions` / `.nodeSummary` / `.errorText` 재사용.
  - queries/adminView/menuView/sessionView/engine 전부 미수정. TASK-147 action-delete 핸들러/게이트 byte-for-byte 불변.
- Phase 4 결과:
  - `npm run build`: pass (813.77KB → 815.14KB, +1.37KB)
  - `npm run lint`: pass (기존 useGameTick warning 1 건만, 변경 파일 무관 · 0 errors)
  - `@self-reviewer`: PASS 12/12 요구사항 충족, 금지사항 위반 없음
  - `npx tsc --noEmit`: 0 errors
  - targeted vitest 5 suite: 132/132 pass (adminView 40 + sessionView 42 + menuView 38 + phaseAdvance 6 + ingredientAdd 6)
- 활성화 경로:
  - **ingredient row delete**: `buildAdminIngredientNodeList(bundle)` 가 `bundle.ingredient_nodes` 로부터 ingredientList 를 derive → setBundle filter 후 다음 렌더에서 row 사라짐. delete 중 `+ hop 추가` / `× 마지막 hop 제거` / `+ 재료 노드 추가` / `+ 노드 연결` / 같은 node 의 drilldown `× 연결 해제` 전부 disabled / 비노출.
  - **summary 정합**: targeted 3 필드만 변경 — full rebuild 금지 제약 충족. `groups[].nodeCount` 는 linkedGroup 있을 때만 -1, 나머지 group 불변.
  - **FK 거절**: practice_ingredient_instances / practice_node_progress 가 참조 중이면 23503 → row-scoped inline error 노출 + bundle/summary 무변경(try 내부에서만 setBundle/setSummary 호출).
  - **unlink-delete race 방어**: handleUnlinkNode early-return + drilldown disabled 양쪽에서 `deletingIngredientNodeId !== null` lock → 같은 node 의 unlink 와 parent delete 동시 실행 시 summary drift 0(양쪽 모두 `nodeCount -= 1` 중복 적용 차단).
- LEARNINGS/MISTAKES 갱신: 해당 없음(TASK-147 패턴 — `requestMenuId` / `selectedMenuIdRef` / `functional updater + menuId 검증` / `delete 이전 linkedGroupId snapshot` / `targeted summary update 3 필드` / `row-scoped error marker` — 재사용. 1차 self-review 에서 지적된 unlink-delete race 방어 패턴은 summary drift 방지의 일반 원칙으로 TASK-147 action 경로와 대칭 — 새 패턴 아님).

## 완료된 것 (직전 세션, TASK-147)
TASK-20260419-147: /practice/admin의 **action node parent delete** 경로 개통. action node row에 `× 삭제` 버튼을 노출, DB cascade로 `practice_action_nodes`/`practice_step_group_nodes`/`practice_node_location_path`가 함께 정리되고, NON-CASCADE FK인 `practice_node_progress.node_id → practice_recipe_nodes.id`(008_practice_schema.sql:158)로 인한 23503 거절은 row-scoped inline error로만 노출. session cleanup으로 우회하지 않음. queries.ts에 `deletePracticeRecipeNode({ nodeId })` helper 1개 신설(single `.delete().eq()` + `raise` 패턴, action-only 판정/session 처리 미소유 — caller가 action row에서만 호출). PracticeAdminPage.tsx에 state 2개(`deletingActionNodeId`/`deleteActionNodeError`) + Effect 2 reset 2줄 + `handleDeleteActionNode` 핸들러(7+self 충돌 가드) + action node row UI(× 삭제/삭제 중/inline error) + 충돌 게이트 3곳(`+ 액션 노드 추가`, `+ 노드 연결`, action-node unlink 버튼 disable). setSummary는 targeted update 3필드만 — `actionNodeCount-1`, `totalNodes-1`, linkedGroup 있을 때 해당 `groups[].nodeCount-1`. **`stepGroupCount`/`ingredientNodeCount`/`tacitItemCount`/`tacitMediaCount`/`textTacitCount`/`pureMediaTacitCount`/`linkedMediaCount` 전부 불변**. setBundle은 `action_nodes` + `step_group_nodes` 둘 다 filter(TASK-146 후 unlinked option 재출현 방지, 지시서 주의사항). adminView/menuView/sessionView/CSS/ingredient delete 경로 전부 불변.

## 완료된 것 (이번 세션, TASK-147)
- `src/lib/practice/queries.ts`:
  - `DeletePracticeRecipeNodeInput { nodeId: string }` + `deletePracticeRecipeNode(input): Promise<void>` 신설 — `createPracticeActionNode` 블록 직후 배치(action 도메인 인접). 본문은 `.from('practice_recipe_nodes').delete().eq('id', input.nodeId)` 단일 구문 + `raise('deletePracticeRecipeNode', err.message)`. action-only 판정 / session cleanup / multi-table 로직 미소유. DB cascade로 `practice_action_nodes`(005 FK), `practice_step_group_nodes`(008 FK), `practice_node_location_path`(008 FK) 자동 정리. `practice_node_progress.node_id`는 NON-CASCADE → 활성 session이 참조 중이면 23503 raise.
- `src/pages/practice/PracticeAdminPage.tsx`:
  - imports: value `deletePracticeRecipeNode` 추가(`deletePracticeNodeLocationPathTailHop` 바로 뒤).
  - state 2개 신설(`deleteStepGroupError` 블록 직후 배치):
    * `deletingActionNodeId: string | null` — in-flight marker(동시 delete 차단 + 충돌 게이트 확장).
    * `deleteActionNodeError: { nodeId: string; message: string } | null` — row-scoped 에러(`{nodeId, message}` 객체로 단일 행만 노출).
  - handler 1개 신설(`handleDeleteStepGroup` 바로 뒤, Effect 1 직전): `handleDeleteActionNode(nodeId)`.
    * **7+self 선제 가드**: `showActionNodeInput` / `creatingActionNode` / `showLinkNodeInput` / `linkingNode` / `unlinkingNodeId !== null` / `deletingActionNodeId !== null`(self) / `deletingStepGroupId !== null`. 하나라도 truthy면 early return. unrelated tacit/media/step-group helper 가드 미확장(지시서 명시).
    * `bundle && bundle.menu.id === selectedMenuId` + `selectedMenuId` null guard.
    * **target 존재 확인**: `bundle.action_nodes.find(a => a.node.id === nodeId)`. 없으면 `setDeleteActionNodeError({ nodeId, message: '대상 액션 노드를 찾지 못했습니다.' })` + early return — bundle drift 방어.
    * ⚠️ **linkedGroup snapshot 캡처(delete 호출 이전)**: `bundle.step_group_nodes.find(sgn => sgn.node_id === nodeId)?.step_group_id ?? null`. delete 후엔 setBundle race로 snapshot drift 가능 — TASK-146/144 패턴 미러.
    * TASK-128 stale-guard 패턴: `const requestMenuId = selectedMenuId; setDeletingActionNodeId(nodeId); setDeleteActionNodeError(null);` → `await deletePracticeRecipeNode({ nodeId })`.
    * try 성공(모두 functional updater + `prev.menu.id === requestMenuId` / `prev.menuId === requestMenuId` 가드 하):
      - `setBundle(prev => prev && prev.menu.id === requestMenuId ? { ...prev, action_nodes: filter !== nodeId, step_group_nodes: filter sgn.node_id !== nodeId } : prev)` — DB cascade와 동치. ⚠️ **`step_group_nodes`도 같이 filter**(TASK-146 후 unlinked option 재출현 방지, 지시서 주의사항). `ingredient_nodes`/`tacit_items`/`tacit_media`/`step_groups`/`locations` 전부 spread로 보존.
      - `setSummary(prev => prev && prev.menuId === requestMenuId ? { ...prev, actionNodeCount: -1, totalNodes: -1, groups: linkedGroupId == null ? prev.groups : prev.groups.map(g => g.groupId === linkedGroupId ? { ...g, nodeCount: -1 } : g) } : prev)` — ⚠️ **targeted update만**, full rebuild 금지. `stepGroupCount`/`ingredientNodeCount`/`tacitItemCount`/`tacitMediaCount`/`groups[].textTacitCount/pureMediaTacitCount/linkedMediaCount` 전부 spread로 보존.
      - `setDeleteActionNodeError(null)` — 성공 시 stale error 정리.
    * catch: `selectedMenuIdRef.current === requestMenuId` 시 `setDeleteActionNodeError({ nodeId, message: e instanceof Error ? e.message : String(e) })`. `unknown` + `instanceof` 타입 가드. session FK 23503 메시지가 그대로 inline 노출.
    * finally: 같은 가드 하 `setDeletingActionNodeId(null)`.
  - Effect 2 reset 블록: `setDeleteStepGroupError(null)` 바로 뒤에 `setDeletingActionNodeId(null); setDeleteActionNodeError(null);` 2줄 추가. 메뉴 전환 시 in-flight marker + row error 둘 다 해제.
  - action node list row UI 3블록 추가(`<div className={styles.adminNodeItem}>` 내부 마지막):
    * `<button className={styles.metaEditCancelBtn} disabled={deletingActionNodeId !== null} onClick={() => handleDeleteActionNode(item.nodeId)}>× 삭제</button>` — disabled로 동시 delete 차단.
    * `deletingActionNodeId === item.nodeId && <span className={styles.nodeSummary}>삭제 중…</span>` — in-flight 표시.
    * `deleteActionNodeError?.nodeId === item.nodeId && deletingActionNodeId === null && <div className={styles.errorText}>{msg}</div>` — row-scoped error는 in-flight 중에는 안 보이게(라벨 전이와 겹치지 않음).
  - 충돌 게이트 3곳:
    * `+ 액션 노드 추가` 버튼 gate: `hasLocations && !showActionNodeInput`에 `&& deletingActionNodeId === null` append.
    * `+ 노드 연결` 버튼 gate: 기존 7-조건에 `&& deletingActionNodeId === null` append.
    * action-node unlink 버튼(drilldown.actionNodes.map 내부): `disabled={unlinkingNodeId !== null}`를 `disabled={unlinkingNodeId !== null || deletingActionNodeId !== null}`로 확장. ingredient unlink 버튼은 슬라이스 범위 밖이라 미수정.
  - **CSS 파일 수정 없음** — `.metaEditCancelBtn`/`.metaEditActions`/`.nodeSummary`/`.errorText` 재사용. 신규 클래스 0.
  - adminView.ts/menuView.ts/sessionView.ts/engine 전부 미수정. TASK-146 step-group delete + TASK-145 text-tacit + TASK-144 pure-media delete 핸들러와 가드 byte-for-byte 불변.
- Phase 4 결과:
  - `npm run build`: pass (812.35KB → 813.77KB, +1.42KB)
  - `npm run lint`: pass (기존 useGameTick warning 1건만, 변경 파일 무관 · 0 errors)
  - `@self-reviewer`: PASS 15/15 요구사항 충족, 금지사항 위반 없음, extra_changes 없음
  - `npx tsc --noEmit`: 0 errors
  - targeted vitest 5 suite: 132/132 pass (adminView 40 + sessionView 42 + menuView 38 + phaseAdvance 6 + ingredientAdd 6)
- 활성화 경로:
  - **action row delete**: `buildAdminActionNodeList(bundle)`이 `bundle.action_nodes`로부터 actionList를 derive → setBundle filter 후 다음 렌더에서 row 사라짐. delete 버튼은 `deletingActionNodeId === null` 때만 클릭 가능, in-flight 중에는 disabled로 모든 row delete 차단(동시 delete 0).
  - **summary 정합**: `buildMenuStructureSummary`는 `actionNodeCount = bundle.action_nodes.length`, `totalNodes = ingredientNodeCount + actionNodeCount`로 산출. targeted setSummary(-1/-1 + linkedGroup nodeCount -1)가 이 계산과 동치.
  - **DB cascade**: `practice_action_nodes`/`practice_step_group_nodes`/`practice_node_location_path` 자동 정리(005/008 FK on delete cascade). 별도 child delete 코드 불필요.
  - **session FK 거절 경로**: 활성 session이 진행 중인 menu의 action을 delete 시도 → PostgreSQL 23503 → caller catch → `deleteActionNodeError`에 `{nodeId, message}` 저장 → 같은 row 옆에 inline 노출. summary/bundle 둘 다 미변경(setBundle/setSummary는 try 블록 내부에서만 호출).
  - **TASK-146 race 회피**: step_group에 연결된 action node를 delete하면 `setBundle`이 `step_group_nodes` filter도 함께 적용 → 다음 렌더에서 unlinked node options에 재출현하지 않음(`buildAdminUnlinkedNodeOptions`가 step_group_nodes를 참조하므로 정합).
  - **충돌 차단 매트릭스**: handleDeleteActionNode 내부 7가드(showActionNodeInput/creatingActionNode/showLinkNodeInput/linkingNode/unlinkingNodeId/deletingActionNodeId/deletingStepGroupId) + UI disabled(같은 행 delete in-flight 중 다른 행 delete 차단 + `+ 액션 노드 추가` / `+ 노드 연결` / action unlink 모두 막힘). action 슬라이스 외 경로(메타 편집/미디어/암묵지/스텝그룹 메타)는 의도적 미터치.
- LEARNINGS/MISTAKES 갱신: 해당 없음(TASK-143/144/145/146 패턴 — `requestMenuId`/`selectedMenuIdRef`/`functional updater + menuId 검증`/`delete 이전 snapshot 캡처`/`targeted summary update`/`row-scoped error marker` — 재사용. 새 패턴 없음).

## 완료된 것 (직전 세션, TASK-146)
- `src/lib/practice/queries.ts`:
  - `DeletePracticeStepGroupInput { stepGroupId: string }` + `deletePracticeStepGroup(input): Promise<void>` 신설 — `deletePracticeTacitItem` 블록(L437-445) 직후에 배치. 본문은 `.from('practice_step_groups').delete().eq('id', input.stepGroupId)` 단일 구문 + `raise('deletePracticeStepGroup', err.message)`. **도메인 규칙 미소유** — 노드/암묵지/미디어 필터/검증 없음. 반환 `Promise<void>`. DB cascade(008_practice_schema.sql:106 step_group_nodes, :114 tacit_items, :129 tacit_media)로 child row 자동 제거, 명시적 child delete 불필요. 잘못 만든 스텝 그룹을 admin UI에서 직접 정리 가능하게 하고, DB FK `on delete cascade`(008_practice_schema.sql:106 step_group_nodes, :114 tacit_items)로 step_group 삭제 시 step_group_nodes + tacit_items가 연쇄 삭제되고, tacit_items → tacit_media의 cascade(:129)로 media까지 한 번에 정리됨. `practice_recipe_nodes` 본체는 FK 대상이 아니므로 **unlinked 상태로 생존**(지시서 핵심 요건). queries.ts에 `deletePracticeStepGroup({ stepGroupId })` helper 1개 신설(single `.delete().eq()` + `raise` 패턴). PracticeAdminPage.tsx에 state 2개(`deletingStepGroupId`/`deleteStepGroupError`) + Effect 2 reset 2줄 + `handleDeleteStepGroup` 핸들러 + UI 3곳(메타 편집 row `&& deletingStepGroupId === null` gate + × 삭제 버튼 + row-scoped error/진행상태, "+ 스텝 그룹 추가" gate). setSummary는 full rebuild 금지 — `stepGroupCount-1`, `tacitItemCount-removedTacitCount`, `tacitMediaCount-removedMediaCount`, `groups.filter(g => g.groupId !== stepGroupId)` 4필드만 targeted update. **`totalNodes`/`ingredientNodeCount`/`actionNodeCount` 불변**(노드 생존 원칙). TASK-145 14-state pure-media/text-tacit 삭제 가드는 byte-for-byte 불변(unrelated tacit/media card gate 미확장 — 지시서 명시). adminView.ts/menuView.ts/sessionView.ts/CSS 전부 불변.

## 완료된 것 (이번 세션, TASK-146)
- `src/lib/practice/queries.ts`:
  - `DeletePracticeStepGroupInput { stepGroupId: string }` + `deletePracticeStepGroup(input): Promise<void>` 신설 — `deletePracticeTacitItem` 블록(L437-445) 직후에 배치. 본문은 `.from('practice_step_groups').delete().eq('id', input.stepGroupId)` 단일 구문 + `raise('deletePracticeStepGroup', err.message)`. **도메인 규칙 미소유** — 노드/암묵지/미디어 필터/검증 없음. 반환 `Promise<void>`. DB cascade(008_practice_schema.sql:106 step_group_nodes, :114 tacit_items, :129 tacit_media)로 child row 자동 제거, 명시적 child delete 불필요.
- `src/pages/practice/PracticeAdminPage.tsx`:
  - imports: value `deletePracticeStepGroup` 추가(`deletePracticeTacitItem` 바로 뒤).
  - state 2개 신설(`deleteTextTacitError` 블록 직후 배치):
    * `deletingStepGroupId: string | null` — in-flight marker(동시 delete 차단 + render gate 확장).
    * `deleteStepGroupError: { stepGroupId: string; message: string } | null` — row-scoped 에러.
  - handler 1개 신설(`handleDeleteTextTacit` 바로 뒤, Effect 1 직전): `handleDeleteStepGroup(stepGroupId)`.
    * **18-state 선제 가드**: step-group edit/media/tacit/delete 계열 + self. 구성: `editingStepGroupDraft`/`showStepGroupInput`/`editingStepGroupSaving`(step-group CRUD 3) + `editingMediaUrlDraft`/`editingMediaUrlSaving`(media edit 2) + `editingPureMediaTitleDraft`/`editingPureMediaTitleSaving`(pure-media title 2) + `detachingMediaId`(media detach 1) + `editingTacitItemDraft`/`editingTacitItemSaving`(tacit edit 2) + `creatingTacitItem`/`creatingTacitMedia`/`creatingStepGroup`(create 3) + `linkingNode`/`unlinkingNodeId`(link 2) + `deletingPureMediaTacitId`/`deletingTextTacitId`(tacit delete 2) + `deletingStepGroupId`(self 1). 하나라도 truthy면 early return.
    * `bundle && bundle.menu.id === selectedMenuId` + `selectedMenuId` null guard.
    * ⚠️ **snapshot 캡처(delete 호출 이전)**: `removedTacitIds = bundle.tacit_items.filter(ti => ti.step_group_id === stepGroupId).map(ti => ti.id)`; `removedTacitIdSet = new Set(removedTacitIds)`; `removedTacitCount = removedTacitIds.length`; `removedMediaCount = bundle.tacit_media.filter(m => removedTacitIdSet.has(m.tacit_item_id)).length`. LAST_SESSION #7 race drift 회피.
    * TASK-128 stale-guard 패턴: `const requestMenuId = selectedMenuId; setDeletingStepGroupId(stepGroupId); setDeleteStepGroupError(null);` → `await deletePracticeStepGroup({ stepGroupId })`.
    * try 성공(모두 `selectedMenuIdRef.current === requestMenuId` 가드 하):
      - `setBundle(prev => prev && prev.menu.id === requestMenuId ? { ...prev, step_groups: filter !== stepGroupId, step_group_nodes: filter step_group_id !== stepGroupId, tacit_items: filter step_group_id !== stepGroupId, tacit_media: filter !removedTacitIdSet.has(tacit_item_id) } : prev)` — DB cascade와 동치로 parent + child 4 테이블 로컬 제거. **ingredient_nodes/action_nodes/recipe_nodes/location_path 불변** — 노드 본체 생존(지시서 핵심).
      - `setSummary(prev => prev && prev.menuId === requestMenuId ? { ...prev, stepGroupCount: prev.stepGroupCount - 1, tacitItemCount: prev.tacitItemCount - removedTacitCount, tacitMediaCount: prev.tacitMediaCount - removedMediaCount, groups: prev.groups.filter(g => g.groupId !== stepGroupId) } : prev)` — ⚠️ **targeted update만**, full rebuild 금지. **`totalNodes`/`ingredientNodeCount`/`actionNodeCount`는 spread로 그대로 유지** — 지시서 명시 "노드는 살아 있어야 하므로 불변".
      - `setExpandedGroupId(prev => prev === stepGroupId ? null : prev)` — 삭제한 그룹이 펼쳐져 있었다면 접고, 다른 그룹 펼친 상태는 보존.
      - `setDeleteStepGroupError(null)` — 성공 시 stale error 정리.
    * catch: `selectedMenuIdRef.current === requestMenuId` 시 `setDeleteStepGroupError({ stepGroupId, message: e instanceof Error ? e.message : String(e) })`. `unknown` + `instanceof` 타입 가드(`any` 없음).
    * finally: 같은 가드 하 `setDeletingStepGroupId(null)`.
  - Effect 2 reset 블록: `setDeleteTextTacitError(null)` 바로 뒤에 `setDeletingStepGroupId(null); setDeleteStepGroupError(null);` 2줄 추가. 메뉴 전환 시 in-flight marker lock 해제.
  - step_groups.map row 내부 "메타 편집" 블록 gate 끝에 `&& deletingStepGroupId === null` append(8-조건 → 9-조건). 같은 block 내부 Fragment로 "메타 편집" 버튼 뒤에 **"× 삭제" 버튼** 추가(`className={styles.metaEditCancelBtn}` 재사용, `onClick={() => handleDeleteStepGroup(g.id)}`). block 바깥에 row-scoped UI 2개: 진행 중 표시(`deletingStepGroupId === g.id && <span className={styles.nodeSummary}>삭제 중…</span>`) + 에러(`deleteStepGroupError?.stepGroupId === g.id && deletingStepGroupId === null && <div className={styles.errorText}>{msg}</div>`).
  - "+ 스텝 그룹 추가" 버튼 gate 끝에 `&& deletingStepGroupId === null` append(7-조건 → 8-조건).
  - **CSS 파일 수정 없음** — `.metaEditTrigger`/`.metaEditCancelBtn`/`.errorText`/`.nodeSummary` 재사용. 신규 클래스 0.
  - adminView.ts/menuView.ts/sessionView.ts/engine 전부 미수정. TASK-144/145의 pure-media/text-tacit delete 핸들러와 13·14-state 가드 byte-for-byte 불변. unrelated tacit/media card gate에 `deletingStepGroupId` 주입 없음(지시서 명시).
- Phase 4 결과:
  - `npm run build`: pass (baseline 포함, 번들 사이즈 ~1.5KB 증가 → 812.35KB)
  - `npm run lint`: pass (기존 useGameTick warning 1건만, 변경 파일 무관 · 0 errors)
  - `@self-reviewer`: PASS 10/10 요구사항 충족, 금지사항 위반 없음, extra_changes false
  - `npx tsc --noEmit`: 0 errors
  - targeted vitest 5 suite: 132/132 pass (adminView 40 + sessionView 42 + menuView 38 + phaseAdvance 6 + ingredientAdd 6)
- 활성화 경로:
  - **Admin step-group list**: `summary.groups.map`이 filter 결과로 대상 row를 즉시 제거 → 다음 렌더에서 카드 사라짐. 메타 편집 row의 "× 삭제" 버튼은 18-state 가드 통과 시에만 노출.
  - **Coverage summary**: `buildMenuStructureSummary`는 `stepGroupCount = bundle.step_groups.length`, `tacitItemCount = bundle.tacit_items.length`, `tacitMediaCount = bundle.tacit_media.length`로 산출. targeted setSummary(-1/-removedTacitCount/-removedMediaCount + groups filter)가 이 계산과 동치 — full rebuild 없이도 정합.
  - **노드 본체 생존**: `bundle.ingredient_nodes`/`action_nodes`/`recipe_nodes`/`node_location_path` 로컬 4 테이블은 setBundle에서 spread로 그대로 유지. DB도 step_group_nodes cascade로 연결만 끊고 recipe_nodes 본체는 FK 대상이 아니므로 생존. 삭제된 step-group의 노드는 unlinked 상태로 재연결 가능.
  - **세션 뷰/menuView**: session/menu 뷰 빌더는 bundle snapshot을 그대로 받으므로 setBundle 이후 자동 정합. 계약 수정 없음.
  - **cascade 경로 격리**: step-group delete 핸들러는 parent row를 지우고 DB cascade에 위임. tacit item/media 개별 delete 경로(TASK-144/145)는 병렬 가드로 상호 배타 — 핸들러 내부 18-state mutex로 동시 실행 차단.
- LEARNINGS/MISTAKES 갱신: 해당 없음(TASK-143/144/145 패턴 — `requestMenuId`/`selectedMenuIdRef`/`functional updater + menuId 검증`/`delete 이전 snapshot 캡처`/`targeted summary update` — 재사용. 새 패턴은 (1) Set 기반 multi-id filter(`removedTacitIdSet.has(...)`) (2) expandedGroupId conditional collapse(`prev === deletedId ? null : prev`) 정도로 국지적 · 문서화 불필요).

## 완료된 것 (직전 세션, TASK-145)
- `src/pages/practice/PracticeAdminPage.tsx`:
  - state 2개 신설(`deletePureMediaError` 블록 직후 배치):
    * `deletingTextTacitId: string | null` — in-flight marker(동시 delete 차단 + render gate 확장).
    * `deleteTextTacitError: { tacitItemId: string; message: string } | null` — card-scoped 에러.
  - handler 1개 신설(`handleDeletePureMediaTacit` 바로 뒤, Effect 1 직전): `handleDeleteTextTacit(tacitItemId)`.
    * **14-state 선제 가드**: TASK-144 13-state + 1 self(`deletingTextTacitId !== null`).
    * `bundle && bundle.menu.id === selectedMenuId` + `selectedMenuId` null guard.
    * `bundle.tacit_items.find(t => t.id === tacitItemId && t.tacit_type !== 'media')` → `parentTacit`. 실패 시 `setDeleteTextTacitError({ tacitItemId, message: '대상 tacit을 찾을 수 없습니다.' })` + early return. **`tacit_type !== 'media'` 내부 조건**으로 pure-media 입력 오경로를 동일 경로로 차단 — slice 명시 "pure-media delete 로직 불변" 충족(교차 호출 방지).
    * `targetStepGroupId = parentTacit.step_group_id` + `removedMediaCount = bundle.tacit_media.filter(m => m.tacit_item_id === tacitItemId).length` — ⚠️ **delete 호출 이전** 캡처(delete 후엔 async race로 snapshot drift 가능).
    * TASK-128 stale-guard 패턴: `const requestMenuId = selectedMenuId; setDeletingTextTacitId(tacitItemId); setDeleteTextTacitError(null);` → `await deletePracticeTacitItem({ tacitItemId })`.
    * try 성공:
      - `setBundle(prev => prev && prev.menu.id === requestMenuId ? { ...prev, tacit_items: prev.tacit_items.filter(t => t.id !== tacitItemId), tacit_media: prev.tacit_media.filter(m => m.tacit_item_id !== tacitItemId) } : prev)` — local에서 parent text tacit + 연결 media 전부 제거(FK cascade와 동치). text tacit이 media를 가지지 않는 일반 케이스에서도 filter는 no-op으로 안전.
      - `setSummary(prev => prev && prev.menuId === requestMenuId ? { ...prev, tacitItemCount: prev.tacitItemCount - 1, tacitMediaCount: prev.tacitMediaCount - removedMediaCount, groups: prev.groups.map(g => g.groupId === targetStepGroupId ? { ...g, textTacitCount: g.textTacitCount - 1, linkedMediaCount: g.linkedMediaCount - removedMediaCount } : g) } : prev)` — ⚠️ **targeted update만**, full rebuild 금지. 타 그룹 필드 및 menu-level `actionNodeCount/ingredientNodeCount/stepGroupCount/totalNodes/pureMediaTacitCount` 전부 불변.
    * catch: `selectedMenuIdRef.current === requestMenuId` 시 `setDeleteTextTacitError({ tacitItemId, message: e instanceof Error ? e.message : String(e) })`. `unknown` + `instanceof` 타입 가드 유지(`any` 없음).
    * finally: 같은 가드 하 `setDeletingTextTacitId(null)`.
  - Effect 2 reset 블록: `setDeletePureMediaError(null)` 바로 뒤에 `setDeletingTextTacitId(null); setDeleteTextTacitError(null);` 2줄 추가. 메뉴 전환 시 in-flight marker lock 해제.
  - text tacit card view-mode 내부 3개 render gate 끝에 `&& deletingTextTacitId === null` append:
    * `drilldown.textTacitItems.map` 내부 "URL 수정" 게이트(linkedMedia row, 기존 12플래그 → 13플래그)
    * 같은 loop 내부 "연결 해제" 게이트(기존 12플래그 → 13플래그)
    * text tacit card 레벨 "편집" 게이트(기존 7플래그 → 8플래그)
    → 삭제 in-flight 중 같은 카드의 URL 수정/연결 해제/편집 동시 조작 차단(사용자 관점 상호 배타).
  - text tacit card 닫는 `</div>` 직전에 신규 블록 2개 추가(편집 버튼 바로 뒤):
    * "삭제" 버튼: 13플래그 gate(TASK-144 precedent 미러 — `!showMediaAttachInput` 포함) + self `deletingTextTacitId === null`. `className={styles.metaEditTrigger}` 재사용, `onClick={() => handleDeleteTextTacit(t.id)}`.
    * card-scoped error: `deleteTextTacitError?.tacitItemId === t.id && deletingTextTacitId === null && <p className={styles.errorText}>...</p>`.
  - **CSS 파일 수정 없음** — `.metaEditTrigger`/`.errorText` 재사용. 신규 클래스 0. import 경계 불변(admin↔game 분리 유지).
  - `src/lib/practice/queries.ts` 불변(slice 제약): `deletePracticeTacitItem` 재사용만, 신규 query/type 없음.
  - adminView.ts/menuView.ts/sessionView.ts/engine 전부 미수정. pure-media 렌더 블록(`drilldown.pureMediaItems.map`) 및 `handleDeletePureMediaTacit` 핸들러 byte-for-byte 불변. TASK-143 safe-detach 규칙(`pm.media.length > 1`) 무변경.
- Phase 4 결과:
  - `npm run build`: pass (baseline 포함, 번들 사이즈 ~1.3KB 증가 → 810.84KB)
  - `npm run lint`: pass (기존 useGameTick warning 1건만, 변경 파일 무관 · 0 errors)
  - `@self-reviewer`: PASS 6/6 요구사항 충족, 금지사항 위반 없음, extra_changes 없음
  - `npx tsc --noEmit`: 0 errors
  - targeted vitest 5 suite: 132/132 pass (adminView 40 + menuView 38 + sessionView 42 + ingredientAdd 6 + phaseAdvance 6)
- 활성화 경로:
  - **Admin drilldown**: `buildStepGroupDrilldown`이 매 렌더 `bundle.tacit_items` 중 `tacit_type !== 'media'`를 `textTacitItems`로 공급. targeted setBundle이 text tacit 1개 + 연결 media N개를 동시 필터 제거하면 next render의 `textTacitItems` 배열에서 카드 자체가 사라짐. 해당 카드에 달린 media row(linkedMedia)도 연쇄 제거.
  - **Coverage summary**: `buildMenuStructureSummary`는 `textTacitCount = filter(t => step_group_id === g.id && tacit_type !== 'media').length`, `linkedMediaCount = bundle.tacit_media.filter(m => parent.step_group_id === g.id).length`로 산출. targeted setSummary(-1/-removedMediaCount/-1/-removedMediaCount)가 이 계산과 동치 — full rebuild 없이도 카운트 정합.
  - **세션 뷰/menuView**: `sessionView.ts buildTacitDetailViewModel`과 `menuView.ts`의 text tacit path는 bundle snapshot을 그대로 받으므로 setBundle 이후 자동 정합. 계약 수정 없음.
  - **pure-media 경로 격리**: text tacit delete 핸들러의 `find` predicate `tacit_type !== 'media'`로 pure-media id 주입 오경로 early-return. pure-media card 렌더 블록(`drilldown.pureMediaItems.map`)과 그 내부 "제목 수정"/"URL 수정"/"연결 해제"/"삭제" 게이트는 이번 slice에서 **무변경** — TASK-143/TASK-144 불변성 충족.
  - **상호 배타 가드**: 14-state 집합(TASK-144 13 + 1 self `deletingTextTacitId`). 기존 pure-media 핸들러/게이트에는 `deletingTextTacitId` 주입하지 않음(slice "unrelated pure-media/step group gate 확대 금지" 준수). 새 state의 영향권은 text tacit card cluster 내부 URL 수정/연결 해제/편집/삭제 4 버튼으로 제한.
- 발견 이슈: 플랜 재제출 라운드에서 평가자 harness가 stale 스냅샷을 반복 인용하는 현상 관측(본문 실제 내용과 불일치). Write로 전체 플랜 파일을 덮어써서 해소. 코드 구현에는 영향 없음.
- LEARNINGS/MISTAKES 갱신: 해당 없음(TASK-143/144 패턴 — `requestMenuId`/`selectedMenuIdRef`/`functional updater + menuId 검증`/`delete 이전 snapshot 캡처` — 을 그대로 재사용했을 뿐 신규 패턴 없음).

## 완료된 것 (선행 세션, TASK-144)
- `src/lib/practice/queries.ts`:
  - `DeletePracticeTacitItemInput { tacitItemId }` + `deletePracticeTacitItem(input): Promise<void>` 신설 — `updatePracticeTacitItem` 구현 종료(L431) 직후, `CreatePracticeTacitMediaInput`(L447) 앞. 본문은 `.from('practice_tacit_items').delete().eq('id', input.tacitItemId)` 단일 구문 + `raise('deletePracticeTacitItem', err.message)`. **도메인 규칙 미소유** — `tacit_type` 필터/검증/cascade 보정 없음. 반환 `Promise<void>`. FK 영향: `practice_tacit_media.tacit_item_id` → `practice_tacit_items(id) on delete cascade`(008_practice_schema.sql:129) → parent 삭제로 child row 자동 제거, 명시적 child delete 불필요.
- `src/pages/practice/PracticeAdminPage.tsx`:
  - imports: value `deletePracticeTacitItem` 추가(`deletePracticeTacitMedia` 바로 뒤). 타입 import 변경 없음.
  - state 2개 신설(`detachingMediaId`/`detachMediaError` 블록 직후 배치):
    * `deletingPureMediaTacitId: string | null` — in-flight marker(동시 delete 차단 + render gate 동원).
    * `deletePureMediaError: { tacitItemId: string; message: string } | null` — card-scoped 에러(마커 패턴 LEARNINGS 2026-04-19 유지).
  - handler 1개 신설(`handleDetachTacitMedia` 바로 뒤): `handleDeletePureMediaTacit(tacitItemId)`.
    * **13-state 선제 가드**: 기존 12-state(TASK-142 도입) + 1 self(`deletingPureMediaTacitId !== null`).
    * `bundle && bundle.menu.id === selectedMenuId` + `selectedMenuId` null guard.
    * `bundle.tacit_items.find(t => t.id === tacitItemId && t.tacit_type === 'media')` → `parentTacit`. 실패 시 `setDeletePureMediaError({ tacitItemId, message: '대상 tacit을 찾을 수 없습니다.' })` + early return. **`tacit_type === 'media'` 내부 조건**으로 text tacit 입력 오경로도 동일 경로로 차단 — slice 명시 "text tacit delete 범위 밖" 충족.
    * `targetStepGroupId = parentTacit.step_group_id` + `removedMediaCount = bundle.tacit_media.filter(m => m.tacit_item_id === tacitItemId).length` — ⚠️ **delete 호출 이전** 캡처. bundle mutation 이전 snapshot이라 race-free.
    * TASK-128 stale-guard 패턴: `const requestMenuId = selectedMenuId; setDeletingPureMediaTacitId(tacitItemId); setDeletePureMediaError(null);` → `await deletePracticeTacitItem({ tacitItemId })`.
    * try 성공:
      - `setBundle(prev => prev && prev.menu.id === requestMenuId ? { ...prev, tacit_items: prev.tacit_items.filter(t => t.id !== tacitItemId), tacit_media: prev.tacit_media.filter(m => m.tacit_item_id !== tacitItemId) } : prev)` — local에서 parent + 모든 child media 제거(서버 cascade와 로컬 일관).
      - `setSummary(prev => prev && prev.menuId === requestMenuId ? { ...prev, tacitItemCount: prev.tacitItemCount - 1, tacitMediaCount: prev.tacitMediaCount - removedMediaCount, groups: prev.groups.map(g => g.groupId === targetStepGroupId ? { ...g, pureMediaTacitCount: g.pureMediaTacitCount - 1, linkedMediaCount: g.linkedMediaCount - removedMediaCount } : g) } : prev)` — ⚠️ **targeted update만**, full rebuild 금지. 타 그룹 `pureMediaTacitCount/linkedMediaCount/nodeCount/textTacitCount/displayStepNo/title/summary/primaryLocationLabel` 및 menu-level `actionNodeCount/ingredientNodeCount/stepGroupCount/totalNodes` 모두 불변.
    * catch: `selectedMenuIdRef.current === requestMenuId` 시 `setDeletePureMediaError({ tacitItemId, message: e instanceof Error ? e.message : String(e) })`. `unknown` + `instanceof` 타입 가드 유지.
    * finally: 같은 가드 하 `setDeletingPureMediaTacitId(null)`.
  - Effect 2 reset 블록: `setDetachMediaError(null)` 바로 뒤에 `setDeletingPureMediaTacitId(null); setDeletePureMediaError(null);` 2줄 추가. **필수** — in-flight 중 메뉴 전환 시 finally의 stale-guard로 marker 해제 skip되더라도 새 메뉴 진입 시 lock 풀림.
  - pure-media card view-mode `<>` fragment 내부(기존 "제목 수정" 버튼 바로 뒤, `pm.media.map(...)` 외곽)에 신규 블록 2개 추가:
    * "삭제" 버튼: 13-state gate(기존 12 + `deletingPureMediaTacitId === null`). `className={styles.metaEditTrigger}` 재사용, `onClick={() => handleDeletePureMediaTacit(pm.id)}`.
    * card-scoped error: `deletePureMediaError?.tacitItemId === pm.id && deletingPureMediaTacitId === null && <p className={styles.errorText}>...</p>`.
  - 3 render gate에 `deletingPureMediaTacitId === null` `&&` append: pure-media card "제목 수정"(기존 gate 끝), pure-media inner "URL 수정"(기존 gate 끝), pure-media inner "연결 해제"(기존 gate 끝). "삭제" 버튼 자체 gate와 상호 배타 유지(버튼 3종 + self = 동시 노출 0).
  - **CSS 파일 수정 없음** — `.metaEditTrigger`/`.errorText` 재사용. 신규 클래스 0. import 경계 불변(admin↔game 분리 유지).
  - adminView.ts/menuView.ts/sessionView.ts/engine 전부 미수정. `deletePracticeTacitMedia` 시그니처·구현 불변.
- Phase 4 결과:
  - `npm run build`: pass (baseline 포함, 동일 번들 사이즈 소폭 증가 ~1.5KB)
  - `npm run lint`: pass (기존 useGameTick warning 1건만, 변경 파일 무관)
  - `@self-reviewer`: approved 13/13 (위반 0, extra_changes 0 코드 기준 — `queries.ts` + `PracticeAdminPage.tsx` 2파일만 수정)
  - `npx tsc --noEmit`: 0 errors
  - targeted vitest 5 suite: 132/132 pass (adminView 40 + menuView 38 + sessionView 42 + ingredientAdd 6 + phaseAdvance 6)
- 활성화 경로:
  - **Admin drilldown**: `buildStepGroupDrilldown`이 매 렌더 `bundle.tacit_items`(tacit_type === 'media')를 pureMediaItems로 공급, 각 pm에 `bundle.tacit_media` 중 `tacit_item_id` 일치 row 배열을 `pm.media`로 붙임. targeted setBundle이 parent tacit 1개 + 해당 media N개를 한 번에 필터 제거하면 next render의 pureMediaItems 배열에서 카드 자체가 사라짐. menu-level 뷰 포인트 누락 없음.
  - **Coverage summary**: `buildMenuStructureSummary`는 `tacitItemCount = bundle.tacit_items.length`, `tacitMediaCount = bundle.tacit_media.length`, 그룹별 `pureMediaTacitCount = filter(t => step_group_id === g.id && tacit_type === 'media').length`, `linkedMediaCount = bundle.tacit_media.filter(m => parent.step_group_id === g.id).length`로 산출. targeted setSummary(-1/-removedMediaCount/-1/-removedMediaCount)가 이 계산과 동치 — full rebuild(`buildMenuStructureSummary(bundle)` 재호출) 없이도 카운트 정합.
  - **세션 뷰/menuView**: `sessionView.ts buildTacitDetailViewModel`과 `menuView.ts`의 pure-media path는 bundle snapshot을 그대로 받으므로 setBundle 이후 자동 정합. 계약 수정 없음.
  - **계약 준수**: pure-media scaffold가 `length >= 1` 상태에서 parent delete되면 카드 자체가 사라지고 `length === 0` 상태 자체가 존재하지 않게 됨 → adminView.ts `length>0` 필터, menuView.ts `length>0` 필터, sessionView.ts title-only render 모두 영향 없음.
  - **last-media detach 규칙 불변**: row-level "연결 해제" 버튼의 `pm.media.length > 1` 조건은 수정 없음. 신규 parent delete는 이 규칙과 독립 경로로 last-media 상황 클린업만 제공.
  - **상호 배타 가드**: 13-state 집합(TASK-143 12 + 1 self `deletingPureMediaTacitId`). 새 state도 기존 11-state 중 하나의 소유자 핸들러 early-exit에는 주입하지 않음 — 범위 확대 방지. UI 단 render gate 3곳에만 `deletingPureMediaTacitId === null` append로 버튼 동시 노출 차단(사용자 관점 상호 배타 성립). 기존 핸들러의 in-flight 방어는 각자 saving state가 담당.
- 발견 이슈: 없음.
- LEARNINGS/MISTAKES 갱신: 해당 없음(TASK-143의 LEARNINGS 패턴 — `requestMenuId`/`selectedMenuIdRef`/`functional updater + menuId 검증` — 을 그대로 재사용했을 뿐 신규 패턴 없음).

## 완료된 것 (선행 세션, TASK-143)
- `src/pages/practice/PracticeAdminPage.tsx` (단일 변경 파일):
  - `handleDetachTacitMedia`(line 1557~) 내 `targetStepGroupId = parentTacit.step_group_id` 직후, `setDetachingMediaId` 호출 이전에 신규 guard 블록 삽입:
    ```ts
    if (parentTacit.tacit_type === 'media') {
      const siblingCount = bundle.tacit_media.filter(
        (m) => m.tacit_item_id === parentTacit.id,
      ).length;
      if (siblingCount <= 1) {
        setDetachMediaError({ mediaId, message: '마지막 미디어는 제거할 수 없습니다.' });
        return;
      }
    }
    ```
    - **외층 `tacit_type === 'media'` 분기**로 text tacit(tacit_type === 'text') 경로는 guard 코드 자체를 skip → TASK-142 동작 완전 보존. unconditional filter + 동시 조건식 대신 분기 내부화로 회귀 불가능 구조.
    - `setDetachingMediaId` 이전 삽입이라 in-flight marker 누수 없음.
    - setBundle/setSummary/try-catch/finally(기존 로직) 완전 재사용 — `tacitMediaCount - 1` + 대상 group `linkedMediaCount - 1` targeted update 공용.
  - pure-media inner media row(기존 `pm.media.map((m) => ...)`) view-mode `<>` fragment 내부, 기존 "URL 수정" 버튼 블록 바로 뒤에 2개 블록 추가 — text tacit detach 블록(line 3529-3557)을 1:1 미러하되 `pm.media.length > 1`을 12-state chain **맨 앞 첫 조건**으로 추가:
    * "연결 해제" 버튼: `pm.media.length > 1 && editingMediaUrlDraft === null && ...(12-state chain) && detachingMediaId === null`. `className={styles.metaEditTrigger}` 재사용, `onClick={() => handleDetachTacitMedia(m.id)}`.
    * per-row error 블록: `detachMediaError?.mediaId === m.id && detachingMediaId === null`. **`pm.media.length > 1` 미포함** — 마지막 1개로 떨어진 뒤에도 직전 시도의 에러 메시지는 사용자에게 유지(UX).
  - **CSS 파일 수정 없음** — `.metaEditTrigger`/`.errorText` 재사용. 신규 클래스 0. import 경계 불변(admin↔game 분리 유지, value/type import 추가 없음).
  - queries.ts/adminView.ts/menuView.ts/sessionView.ts/engine 전부 미수정.
- Phase 4 결과:
  - `npm run build`: pass
  - `npm run lint`: pass (기존 useGameTick warning 1건만)
  - `@self-reviewer`: approved 7/7 (위반 0, extra_changes 0 코드 기준)
  - `npx tsc --noEmit`: 0 errors
  - targeted vitest 5 suite: 132/132 pass (adminView 40 + menuView 38 + sessionView 42 + ingredientAdd 6 + phaseAdvance 6)
- 활성화 경로:
  - **Admin drilldown**: `buildStepGroupDrilldown`이 매 렌더 `bundle.tacit_media`를 `tacit_item_id` 기준으로 그룹핑해 `pureMediaItems[pm].media`를 재공급. targeted setBundle의 `filter(m => m.id !== mediaId)`가 해당 row를 제거하면 next render의 `pm.media` 배열이 줄어들고, `pm.media.length > 1` 가드가 새로 1로 떨어진 row에서 detach 버튼 자연 은폐.
  - **계약 비대칭 회피**: `pm.media.length > 1` 가드로 인해 admin에서 detach 가능한 경우는 오직 `length >= 2` 상태. detach 후 최소 `length === 1` 잔존 → adminView.ts:347/menuView.ts:220의 `length > 0` 필터 모두 통과 → 세션 title 렌더(PracticeSessionPage.tsx:304)와 정합. session·menu·admin 3면 contract 유지.
  - **text tacit 경로**: `handleDetachTacitMedia`의 새 guard는 `parentTacit.tacit_type === 'media'`에서만 실행. text tacit(tacit_type === 'text')은 siblingCount 계산/제약 모두 skip → TASK-142 동작 그대로.
  - **상호 배타 가드**: 12-state 집합(TASK-142 도입한 11 + 1 self = `detachingMediaId`) 그대로 재사용. pure-media 신규 버튼도 같은 12-state chain 공유.

## 완료된 것 (선행 세션, TASK-142)
- `src/lib/practice/queries.ts`:
  - `DeletePracticeTacitMediaInput { mediaId }` + `deletePracticeTacitMedia(input)` 신설(`updatePracticeTacitMediaUrl` 바로 뒤, create/update/delete 인접 규약). 본문은 `.from('practice_tacit_media').delete().eq('id', input.mediaId)` 단일 구문 + `raise('deletePracticeTacitMedia', err.message)`. **도메인 규칙 미소유** — "text tacit만" 필터, tacit_type 검증, seq 재정렬, cascade 보정 모두 없음. 반환 `Promise<void>`. FK 영향: `008_practice_schema.sql` inbound FK 없음(grep 0건) → 단일 row delete 안전.
- `src/pages/practice/PracticeAdminPage.tsx` (단일 추가 파일):
  - imports: value `deletePracticeTacitMedia` 추가(`updatePracticeTacitMediaUrl` 바로 뒤). 타입 import 변경 없음.
  - state 2개 신설(`editingPureMediaTitle*` 블록 직후 배치):
    * `detachingMediaId: string | null` — in-flight marker(동시 detach 방지).
    * `detachMediaError: { mediaId: string; message: string } | null` — 직전 시도 row 한정 에러(마커 패턴 LEARNINGS 2026-04-19).
  - handler 1개 신설(`handleTacitMediaCreateCancel` 바로 뒤): `handleDetachTacitMedia(mediaId: string)`.
    * **12-state 선제 가드**: `editingMediaUrlDraft !== null || editingMediaUrlSaving || showTacitItemInput || showMediaAttachInput || editingTacitItemDraft !== null || editingTacitItemSaving || editingStepGroupDraft !== null || editingStepGroupSaving || editDraft !== null || editingPureMediaTitleDraft !== null || editingPureMediaTitleSaving || detachingMediaId !== null` — 기존 11-state(TASK-141) + 1 self.
    * `bundle && bundle.menu.id === selectedMenuId` 가드 + `selectedMenuId` null guard.
    * `bundle.tacit_media.find(m => m.id === mediaId)` → `targetMedia`. 실패 시 `setDetachMediaError({ mediaId, message: '대상 미디어를 찾을 수 없습니다.' })` + early return(in-flight 마커 미사용).
    * `bundle.tacit_items.find(t => t.id === targetMedia.tacit_item_id)` → `parentTacit`. 실패 시 `setDetachMediaError({ mediaId, message: '연결된 스텝 그룹을 찾을 수 없습니다.' })` + early return. `targetStepGroupId = parentTacit.step_group_id` 확보 — ⚠️ **BEFORE delete**, delete 후 resolve 금지(플랜 검증).
    * TASK-128 stale-guard 패턴 그대로: `const requestMenuId = selectedMenuId; setDetachingMediaId(mediaId); setDetachMediaError(null);` → `await deletePracticeTacitMedia({ mediaId })`.
    * try 성공:
      - `setBundle(prev => prev && prev.menu.id === requestMenuId ? { ...prev, tacit_media: prev.tacit_media.filter(m => m.id !== mediaId) } : prev)`.
      - `setSummary(prev => prev && prev.menuId === requestMenuId ? { ...prev, tacitMediaCount: prev.tacitMediaCount - 1, groups: prev.groups.map(g => g.groupId === targetStepGroupId ? { ...g, linkedMediaCount: g.linkedMediaCount - 1 } : g) } : prev)` — ⚠️ **targeted update만**, full rebuild 금지. `tacitItemCount/actionNodeCount/ingredientNodeCount/stepGroupCount/totalNodes` 및 group의 `nodeCount/textTacitCount/pureMediaTacitCount/displayStepNo/title/summary/primaryLocationLabel` 모두 불변.
    * catch: `selectedMenuIdRef.current === requestMenuId` 시 `setDetachMediaError({ mediaId, message: e instanceof Error ? e.message : String(e) })`. `unknown` + `instanceof` 타입 가드 유지, `as any` 없음.
    * finally: 같은 가드 하 `setDetachingMediaId(null)`.
  - Effect 2 reset 블록: `setEditingPureMediaTitleError(null)` 바로 뒤에 `setDetachingMediaId(null); setDetachMediaError(null);` 2줄 추가. **필수** — in-flight 중 메뉴 전환 시 finally의 stale-guard로 marker 해제가 skip되더라도 새 메뉴 진입 시 lock 풀림.
  - 4 handler 선제 가드에 `detachingMediaId !== null` `||` append: `handleStepGroupEditStart`(831), `handleTacitItemEditStart`(956), `handleMediaUrlEditStart`(1054), `handlePureMediaTitleEditStart`(1135). 프로그래매틱 진입 차단.
  - 9 render gate에 `detachingMediaId === null` `&&` append: step group "메타 편집"(2505), "+ 스텝 그룹 추가"(2524), "+ 노드 연결"(2665), "+ 암묵지 추가"(2786), "+ 미디어 첨부"(2945), text-tacit linked-media "URL 수정"(3434), text-tacit "편집"(3490), pure-media "제목 수정"(3566), pure-media inner "URL 수정"(3654). 전 지점 기계적 확인 완료(grep `editingMediaUrlDraft === null` 10건 = 기존 9 + neu detach 버튼 자체 gate).
  - 렌더 `drilldown.textTacitItems[*].linkedMedia.map((m) => ...)` 내부 view-mode 프래그먼트(`editingMediaUrlDraft?.mediaId === m.id`의 else 블록)에 ⚠️ **"연결 해제" 버튼** + row-한정 error 블록 추가(기존 "URL 수정" 버튼 바로 뒤):
    ```
    { ...12-state gate with detachingMediaId === null... && <button className={styles.metaEditTrigger} onClick={() => handleDetachTacitMedia(m.id)}>연결 해제</button> }
    { detachMediaError?.mediaId === m.id && detachingMediaId === null && <p className={styles.errorText}>{detachMediaError.message}</p> }
    ```
    - URL 편집 폼이 같은 row에 열리면 `editingMediaUrlDraft?.mediaId === m.id ? <form> : <>...</>` 분기로 연결 해제 버튼은 자연 숨김(추가 조건 불필요).
    - pure-media `drilldown.pureMediaItems.map((pm) => ...)`의 `pm.media.map((m) => ...)`(pure-media inner media row, ~3662-)에는 ⚠️ **detach UI 없음** — slice 명시 범위 밖.
  - **CSS 파일 수정 없음** — `.metaEditTrigger`/`.errorText` 재사용. 신규 클래스 0. import 경계 불변(admin↔game 분리 유지).
- Phase 4: build pass / lint pass(기존 useGameTick warning 1건만) / @self-reviewer approved 11/11(위반 0, extra_changes 0) / tsc 0 errors / targeted vitest 132/132 pass(adminView 40 + menuView 38 + sessionView 42 + ingredientAdd 6 + phaseAdvance 6).
- 활성화 경로:
  - **Admin drilldown**: `buildStepGroupDrilldown`이 매 렌더 `bundle.tacit_media`를 `tacit_item_id`로 그룹핑해 `textTacitItems[t].linkedMedia` 공급. targeted setBundle로 해당 row가 제거되면 다음 렌더에 linkedMedia 리스트에서 즉시 사라짐. `pureMediaItems`는 `bundle.tacit_items`(tacit_type === 'media')에 `media`(bundle.tacit_media 중 tacit_item_id 일치 row 배열)를 붙여 공급 — slice 대상 아님이나 같은 bundle filter 경로라 무관 갱신 0.
  - **Coverage summary**: `buildMenuStructureSummary`가 `tacit_media`를 group tacit item ids로 필터해 `linkedMediaCount` 산출 + menu-level `tacitMediaCount = bundle.tacit_media.length` 합산. targeted setSummary(둘 모두 -1)가 이 계산과 정확히 일치 — full rebuild(`buildMenuStructureSummary(bundle)` 재호출) 없이도 카운트 정합.
  - **Browse (menuView.ts)**: 순수 media_type tacit 카드 노출 경로 — text tacit linked media는 render 경로가 달라(text tacit → sensory pills) menu view 영향 없음. 수정 없이 자동 반영.
  - **Session (sessionView.ts)**: `buildTacitDetailViewModel`이 text tacit 의 linked media URL/thumbnail을 VM에 주입 — setBundle 후 기존 경로 그대로 동작.
  - **Read/Engine 계약 불변**: queries.ts 기존 helper, adminView.ts/menuView.ts/sessionView.ts/engine 전부 미수정. 유일한 queries.ts 변경은 `deletePracticeTacitMedia` **신설** 1건.
  - **상호 배타 가드**: 12-state 집합(TASK-141 11 + 1 self). `detachingMediaId !== null`인 동안 step group meta/create, link node, tacit item create/edit, media attach, media URL edit(text-tacit + pure-media), pure-media title edit 모두 trigger 숨김 + handler 선제 가드로 프로그래매틱 진입 차단. 반대 방향도 모두 대칭.

## 완료된 것 (선행 세션, TASK-141)
- `PracticeAdminPage.tsx` (단일 변경 파일):
  - state 3개 신설: `editingPureMediaTitleDraft: {tacitItemId, title} | null`, `editingPureMediaTitleSaving`, `editingPureMediaTitleError`. 선언 위치는 `editingMediaUrl*` 블록 직후 — title/url 편집 state 인접 배치.
  - handler 3개 신설(handleMediaUrlEditSave 직후 배치):
    * `handlePureMediaTitleEditStart(tacitItemId: string, currentTitle: string)`: primitive 파라미터 2개(TASK-140 `handleMediaUrlEditStart` 패턴 미러) — `AdminPureMediaDetail` 타입 import 없이 render 측에서 `pm.id`/`pm.title`만 추출해 전달. **11-state 상호 배타 가드**(`editingPureMediaTitleDraft !== null || editingPureMediaTitleSaving || editingMediaUrlDraft !== null || editingMediaUrlSaving || showTacitItemInput || showMediaAttachInput || editingTacitItemDraft !== null || editingTacitItemSaving || editingStepGroupDraft !== null || editingStepGroupSaving || editDraft !== null`) + `bundle.menu.id !== selectedMenuId` 가드 + `bundle.tacit_items.some(t => t.id === tacitItemId && t.tacit_type === 'media')` 소속·타입 검증(stale id·텍스트 tacit fallback). 전부 통과 시 draft=`{tacitItemId, title: currentTitle}`, error=null.
    * `handlePureMediaTitleEditCancel()`: saving 중 무시. draft null + error null.
    * `handlePureMediaTitleEditSave()`: validation(`trim() === ''` → '제목을 입력하세요.' / `bundle.tacit_items.find(t => t.id === draft.tacitItemId)` raw lookup 실패 → '대상 암묵지를 찾을 수 없습니다.'). TASK-128 3중 stale-guard 그대로(`requestMenuId` 캡처 + `setBundle` functional updater `prev.menu.id !== requestMenuId` skip + `selectedMenuIdRef.current === requestMenuId` try/catch/finally 3곳). **call**: `updatePracticeTacitItem({ tacitItemId: raw.id, title: trimmedTitle, body: raw.body, flameLevel: raw.flame_level, colorNote: raw.color_note, viscosityNote: raw.viscosity_note, soundNote: raw.sound_note, textureNote: raw.texture_note, timingNote: raw.timing_note })` — ⚠️ **raw row nullable 값 그대로 전달**, `trim() || null` 재정규화 금지(PracticeTacitItem의 body/6 sensory 모두 `string | null` 타입 일치). 성공 시 `bundle.tacit_items`를 `map(t => t.id === updated.id ? updated : t)` 교체. **sort_order 불변** → re-sort 없음. **tacit_type 불변** → bucket 이동 없음. **setSummary 미호출**(slice 제약) — StepGroupCoverage의 4 카운트(`nodeCount/textTacitCount/pureMediaTacitCount/linkedMediaCount`) 및 상위 `tacitItemCount/tacitMediaCount/*NodeCount/stepGroupCount/totalNodes` 모두 title 비의존.
  - Effect 2 (메뉴 전환 reset)에 3줄 추가(`setEditingPureMediaTitleDraft(null)`, `setEditingPureMediaTitleSaving(false)`, `setEditingPureMediaTitleError(null)`) — `setEditingMediaUrlError(null)` 직후.
  - 렌더(`drilldown.pureMediaItems.map((pm) => ...)` 내부): **title 영역만** 조건 분기 주입. 기존 `<span .adminTacitTitle>{pm.title}</span>`를 `editingPureMediaTitleDraft?.tacitItemId === pm.id` 분기로 교체 — true면 `.metaEditForm`(제목 input + errorText + `.metaEditActions` save("저장 중..." 라벨 전이)/cancel 버튼), false면 기존 span + 11-state 가드 하 `.metaEditTrigger` "제목 수정" 버튼. ⚠️ **media row 그대로 유지** — `<div .adminTacitMediaRow>` 및 내부 `pm.media.map(m => ...)` (TASK-140 media URL 인라인 편집 포함)은 분기 바깥 하단에 그대로 렌더되어 title 편집 중에도 미디어 미리보기 유지.
  - 기존 8 trigger 가드에 자가 조건 2개(`editingPureMediaTitleDraft === null && !editingPureMediaTitleSaving`) `&&` append:
    * step group "메타 편집" 버튼
    * "+ 스텝 그룹 추가" 버튼
    * "+ 노드 연결" 버튼
    * "+ 암묵지 추가" 버튼
    * "+ 미디어 첨부" 버튼
    * text-tacit linked-media "URL 수정" 버튼
    * text-tacit "편집" 버튼
    * pure-media inner "URL 수정" 버튼
  - 기존 3 handler 선제 가드에 자가 조건 2개(`editingPureMediaTitleDraft !== null || editingPureMediaTitleSaving`) `||` append: `handleStepGroupEditStart`, `handleTacitItemEditStart`, `handleMediaUrlEditStart`. 프로그래매틱 진입 차단.
  - **CSS 파일 수정 없음** — `.metaEditForm/.metaEditLabel/.metaEditInput/.metaEditActions/.metaEditSaveBtn/.metaEditCancelBtn/.metaEditTrigger/.errorText` + `.adminTacitCard/.adminTacitTitle/.adminTacitMediaRow` 전부 재사용. 신규 CSS 0. import 경계 불변(admin↔game 분리, value/type import 추가 없음).
- Phase 4: build pass / lint pass(기존 useGameTick warning 1건만) / @self-reviewer approved 11/11(위반 0, extra_changes 0) / tsc 0 errors / targeted vitest 132/132 pass(adminView 40 + menuView 38 + sessionView 42 + ingredientAdd 6 + phaseAdvance 6).
- 활성화 경로:
  - **Admin drilldown**: `buildStepGroupDrilldown`([adminView.ts:339-349](src/lib/practice/adminView.ts#L339-L349))이 매 렌더 `bundle.tacit_items`(tacit_type === 'media')를 필터·매핑해 `pureMediaItems[pm]`의 `title: t.title`을 재공급 → targeted update된 row가 다음 렌더에 즉시 반영. sort_order/tacit_type/step_group_id 불변이라 드릴다운 섹션 이동·재정렬 없음.
  - **Browse (menuView.ts)**: pure-media는 `title + media[]` container로 노출 — `bundle.tacit_items` 소비 경로 동일. 수정 없이 자동 반영.
  - **Session (sessionView.ts)**: `buildTacitDetailViewModel` current/next는 linked step group 내 tacit_items title을 VM에 주입. 수정 없이 자동 반영.
  - **Read/Engine 계약 불변**: `adminView.ts`/`menuView.ts`/`sessionView.ts`/queries.ts 타입·시그니처·본문 전혀 미수정. engine(phaseAdvance/legalActions/pourDryRun) 미수정 — tacit_items title 미소비.
  - **상호 배타 가드**: 11-state 집합(TASK-140 9 + 2 self)으로 기존 drilldown 편집·생성 폼과 동시 활성 차단. `editingPureMediaTitleDraft`가 활성인 동안 step group meta/create, link node, tacit item create/edit, media attach, media URL edit(text-tacit linked + pure-media 양쪽) 모두 trigger 숨김 + handler 선제 가드로 프로그래매틱 진입도 차단. 반대 방향도 대칭.

## 완료된 것 (선행 세션, TASK-140)
- `queries.ts`:
  - `UpdatePracticeTacitMediaUrlInput { mediaId, url }` + `updatePracticeTacitMediaUrl(input)` 신설. `.from('practice_tacit_media').update({ url: input.url }).eq('id', mediaId).select('id, tacit_item_id, media_type, url, sort_order').single()` → `raise('updatePracticeTacitMediaUrl', err.message)`. **update payload는 `url` 단일 컬럼** — `media_type`/`sort_order`/`tacit_item_id` 의도적 제외(slice 제약 + helper 경계 축소). select 컬럼 순서는 `fetchPracticeTacitMedia` / `createPracticeTacitMedia`와 1:1 미러. 위치: `createPracticeTacitMedia` 바로 뒤(create/update 인접 규약, TASK-139 패턴 유지). `updatePracticeTacitItem`과 동일 1:1 업데이트 패턴.
- `PracticeAdminPage.tsx`:
  - imports: value `updatePracticeTacitMediaUrl` 추가. `PracticeTacitMedia` 타입 import 불필요(draft는 `{mediaId, url}` 2필드만 사용, 대상 lookup은 `AdminMediaDetail.id`).
  - state 3개: `editingMediaUrlDraft: {mediaId, url} | null`, `editingMediaUrlSaving`, `editingMediaUrlError`. 선언 위치는 `tacitMediaError` 직후(미디어 관련 state 인접).
  - handler 3개:
    * `handleMediaUrlEditStart(mediaId, currentUrl)`: **9-state 상호 배타 가드**(`editingMediaUrlDraft !== null || editingMediaUrlSaving || showTacitItemInput || showMediaAttachInput || editingTacitItemDraft !== null || editingTacitItemSaving || editingStepGroupDraft !== null || editingStepGroupSaving || editDraft !== null`) + `bundle.menu.id !== selectedMenuId` 가드 + `bundle.tacit_media.some(m => m.id === mediaId)` 소속 검증(stale id fallback).
    * `handleMediaUrlEditCancel()`: saving 중 무시. draft null + error null.
    * `handleMediaUrlEditSave()`: validation(`trim() === ''` → 'URL을 입력하세요.' / `some(id)` 재확인 실패 → '대상 미디어를 찾을 수 없습니다.'). TASK-128 3중 stale-guard 그대로(`requestMenuId` 캡처 + `setBundle` functional updater `prev.menu.id !== requestMenuId` skip + `selectedMenuIdRef.current === requestMenuId` try/catch/finally 3곳). 성공 시 `bundle.tacit_media`를 `map(m => m.id === updated.id ? updated : m)` 교체. **sort_order 불변** → 배열 순서/re-sort 없음. **media_type 불변** → bucket 이동 없음. **tacit_item_id 불변** → parent tacit 재귀속 없음. **setSummary 미호출**(slice 제약 준수) — URL 변경은 `StepGroupCoverage.{nodeCount, textTacitCount, pureMediaTacitCount, linkedMediaCount}` 및 상위 `tacitMediaCount`/`tacitItemCount`/`*NodeCount`/`stepGroupCount`/`totalNodes` 어떤 카운트에도 영향 없음.
  - Effect 2 (메뉴 전환 reset)에 3줄 추가(`setEditingMediaUrlDraft(null)`, `setEditingMediaUrlSaving(false)`, `setEditingMediaUrlError(null)`) — `setTacitMediaError(null)` 직후.
  - 렌더 **두 위치에 대칭 분기 주입**:
    * **text tacit linked media 블록**(drilldown.textTacitItems.map 내부의 `t.linkedMedia.map((m) => ...)`): 각 `.adminMediaItem` 내부에서 `editingMediaUrlDraft?.mediaId === m.id` 분기. true면 `.metaEditForm`(URL label + `.metaEditInput type="url"` + errorText + `.metaEditActions` save/cancel). false면 기존 `.adminMediaUrl` span + 9-state 비활성 조건 하에 `.metaEditTrigger` "URL 수정" 버튼.
    * **pure-media 블록**(`drilldown.pureMediaItems.map(pm => pm.media.map(m => ...))`): 동일 대칭 분기. **두 경로 모두 inline URL correction 허용**(지시서 "양쪽 경로 open" 준수).
  - 기존 form trigger 가드에 `editingMediaUrlDraft === null && !editingMediaUrlSaving` 추가(8곳 + handler 선제 가드 2곳):
    * handler 선제: `handleStepGroupEditStart`, `handleTacitItemEditStart`.
    * 렌더 조건 8곳: step group "메타 편집" 버튼, "+ 스텝 그룹 추가", "+ 노드 연결", "+ 암묵지 추가", "+ 미디어 첨부", 텍스트 tacit "편집" 버튼, text-tacit linked-media "URL 수정" 버튼, pure-media "URL 수정" 버튼. **drilldown 섹션 내부 범위로 제한** — menu meta/location/action/ingredient create 등 slice 범위 밖 form에는 cross-lock 확장 없음(미니멀 원칙).
  - **CSS 파일 수정 없음** — `.metaEditForm/.metaEditLabel/.metaEditInput/.metaEditActions/.metaEditSaveBtn/.metaEditCancelBtn/.metaEditTrigger/.errorText` + `.adminTacitMediaRow/.adminMediaItem/.adminTacitMediaThumb/.adminVideoLabel/.adminMediaUrl` 전부 재사용. 신규 CSS 0.
- Phase 4: build pass / lint pass(기존 useGameTick warning 1건만) / @self-reviewer approved 11/11(위반 0, extra_changes 0) / tsc 0 errors / targeted vitest 132/132 pass(adminView 40 + menuView 38 + sessionView 42 + ingredientAdd 6 + phaseAdvance 6).
- 활성화 경로:
  - **Admin drilldown**: `buildStepGroupDrilldown`([adminView.ts:307-322](src/lib/practice/adminView.ts#L307-L322))이 매 렌더 `bundle.tacit_media`에서 `mediaByTacitId` 재빌드 → targeted update된 row가 다음 렌더의 `linkedMedia`/`pureMediaItems.media`에 즉시 반영. sort_order/media_type/tacit_item_id 불변이라 목록 순서·분리 유지.
  - **Browse (menuView.ts)**: `buildTacitMediaByItemIdMap`([menuView.ts:99-110](src/lib/practice/menuView.ts#L99-L110))이 `bundle.tacit_media`를 소비 → url 필드 그대로 전달. 수정 없이 자동 반영.
  - **Session (sessionView.ts)**: `buildTacitMediaByItemIdMap`([sessionView.ts:187-200](src/lib/practice/sessionView.ts#L187-L200))이 동일 경로로 소비. 수정 없이 자동 반영.
  - **Read/Engine 계약 불변**: `adminView.ts`/`menuView.ts`/`sessionView.ts` 타입·시그니처·본문 전혀 미수정. engine(phaseAdvance/legalActions/pourDryRun) 미수정 — tacit_media 미소비.
  - **상호 배타 가드**: 9-state 집합으로 두 form 동시 활성 차단. `editingMediaUrlDraft`가 활성인 동안 다른 drilldown 내부 폼(step group meta/create, link node, tacit item create/edit, media attach)의 trigger가 모두 숨김 처리 + handler 선제 가드로 프로그래매틱 진입도 차단.

## 완료된 것 (선행 세션, TASK-139)
- `queries.ts`:
  - `UpdatePracticeTacitItemInput { tacitItemId, title, body, flameLevel, colorNote, viscosityNote, soundNote, textureNote, timingNote }` + `updatePracticeTacitItem(input)` 신설. `.from('practice_tacit_items').update({title, body, flame_level, color_note, viscosity_note, sound_note, texture_note, timing_note}).eq('id', tacitItemId).select('id, step_group_id, tacit_type, title, body, sort_order, flame_level, color_note, viscosity_note, sound_note, texture_note, timing_note').single()`. `raise('updatePracticeTacitItem', err.message)` 미러. `createPracticeTacitItem` 바로 뒤 배치(create/update 인접). select 컬럼 순서는 `fetchPracticeTacitItems` / `createPracticeTacitItem`과 1:1 미러. **update payload에 step_group_id/tacit_type/sort_order 의도적으로 제외**(이번 slice 범위 밖 + helper 경계 축소). `updatePracticeStepGroupMeta` 패턴 1:1 미러.
- `PracticeAdminPage.tsx`:
  - imports: value `updatePracticeTacitItem` + type `PracticeTacitItem` 추가.
  - state 3개: `editingTacitItemDraft: {tacitItemId, title, body, flameLevel, colorNote, viscosityNote, soundNote, textureNote, timingNote} | null`(모든 텍스트 필드 string, '' 허용), `editingTacitItemSaving`, `editingTacitItemError`.
  - handler 3개:
    * `handleTacitItemEditStart(item: PracticeTacitItem)`: 3중 상호 배타 가드(`editingTacitItemDraft !== null || showTacitItemInput || editingTacitItemSaving`) + `bundle.menu.id !== selectedMenuId` 가드 + `item.tacit_type === 'media'` 가드(media 제외). draft 초기값은 item 원본 값, null → ''.
    * `handleTacitItemEditCancel()`: saving 중 무시. draft null + error null.
    * `handleTacitItemEditSave()`: validation(title trim ≠ ''), **body + 6 sensory 모두 `draft.X.trim() || null` 정규화**(지시서 "빈 문자열 저장 금지" 강제). TASK-128 3중 stale-guard 그대로(`requestMenuId` 캡처 + `setBundle` functional updater `prev.menu.id !== requestMenuId` skip + `selectedMenuIdRef.current === requestMenuId` try/catch/finally 3곳). 성공 시 `bundle.tacit_items`를 `map(t => t.id === updated.id ? updated : t)` 교체. **sort_order 불변** → re-sort 없음. **tacit_type 불변** → bucket 이동 없음. **setSummary 미호출**(지시서 제약 준수) — StepGroupCoverage가 tacit title/body/sensory 필드 비보유 + textTacitCount/pureMediaTacitCount bucket 이동 없음.
  - Effect 2 (메뉴 전환 reset)에 3줄 추가(`setEditingTacitItemDraft(null)`, `setEditingTacitItemSaving(false)`, `setEditingTacitItemError(null)`) — `setTacitItemError(null)` 직후.
  - 렌더 (drilldown.textTacitItems.map): callback body에 `editingTacitItemDraft?.tacitItemId === t.id` 분기 도입. 편집 중이면 `.metaEditForm`으로 교체(제목 input + 본문 textarea + ADMIN_SENSORY_FIELDS.map 6 input + errorText + save/cancel 버튼). 편집 중 아니면 기존 read-only card 구조 유지 + `editingTacitItemDraft === null && !showTacitItemInput` 조건 시 `.metaEditTrigger` "편집" 버튼 노출. **draft 초기화 소스**: `bundle?.tacit_items.find(x => x.id === t.id)`로 원본 PracticeTacitItem 조회 후 handleTacitItemEditStart 전달 — `AdminTacitItemDetail.sensoryEntries`는 derived 배열이라 원본 값 미보유. adminView helper 경계 무변경.
  - 렌더 ("+ 암묵지 추가" 버튼): 기존 `!showTacitItemInput` 조건에 `editingTacitItemDraft === null` AND 추가(상호 배타 가드). **CSS 파일 수정 없음** — `.metaEditForm/.metaEditLabel/.metaEditInput/.metaEditTextarea/.metaEditActions/.metaEditSaveBtn/.metaEditCancelBtn/.errorText/.metaEditTrigger` + `.adminTacitCard/.adminTacitHeader/.adminTacitTag/.adminTacitTitle/.adminTacitBody/.adminSensoryRow/.adminSensoryTag/.adminSensoryLabel/.adminTacitMediaRow/.adminMediaItem/.adminTacitMediaThumb/.adminVideoLabel/.adminMediaUrl` 전부 재사용.
- Phase 4: build pass / lint pass(기존 useGameTick warning 1건만) / @self-reviewer approved 13/13(위반 0, extra_changes 0) / tsc 0 errors / vitest 172/172 pass(adminView 40 + menuView 38 + sessionView 42 + ingredientAdd 6 + phaseAdvance 6 + 기타 40).
- 활성화 경로:
  - **Admin drilldown**: `buildStepGroupDrilldown`이 매 렌더 `bundle.tacit_items`에서 `textTacitItems`(tacit_type !== 'media')를 재계산 → targeted update한 row가 즉시 read-only card에 반영. sort_order 불변이라 목록 순서 유지. tacit_type 불변이라 pureMediaItems ↔ textTacitItems 이동 없음.
  - **Read/Engine 계약 불변**: `fetchPracticeMenuBundle`/`menuView`/`sessionView`/engine(phaseAdvance/legalActions/pourDryRun) 수정 없음. `adminView.ts`/`menuView.ts`/`sessionView.ts` helper 경계 불변(타입 AdminTacitItemDetail 무변경). `buildMenuStructureSummary` 전역 카운트 + StepGroupCoverage 4 카운트 필드 전부 불변 (tacit 메타 수정은 어떤 카운트에도 영향 없음).
  - **상호 배타 가드 3중**: (1) handleTacitItemEditStart에서 showTacitItemInput 체크, (2) "+ 암묵지 추가" 버튼 렌더 조건에 editingTacitItemDraft === null 추가, (3) card "편집" 버튼 렌더 조건에 !showTacitItemInput 추가. 두 form이 동시에 열릴 수 없음.

## 완료된 것 (선행 세션, TASK-138)
- `queries.ts`:
  - `DeletePracticeStepGroupNodeLinkInput { stepGroupId, nodeId }` + `deletePracticeStepGroupNodeLink(input)` 신설. `.from('practice_step_group_nodes').delete().eq('step_group_id').eq('node_id')` 단일 row delete. `raise('deletePracticeStepGroupNodeLink', err.message)` 미러. `createPracticeStepGroupNode` 바로 뒤 배치(insert/delete 짝 인접). helper 도메인 개념(group 소유·last node 여부) 비소유 — caller 책임. TASK-136 `deletePracticeNodeLocationPathTailHop` 1:1 패턴 미러.
- `PracticeAdminPage.tsx`:
  - imports: `deletePracticeStepGroupNodeLink` 추가.
  - state 3개: `unlinkingNodeId: string | null`(단일 inflight), `lastUnlinkAttemptNodeId: string | null`(에러 per-row 스코프 마커), `unlinkNodeError: string | null`(단일 슬롯).
  - handler `handleUnlinkNode(stepGroupId, nodeId)`: 선제 validation(`bundle && menu 일치`, link row 존재 확인, `unlinkingNodeId !== null` 동시성 가드) + TASK-128 3중 stale guard(`requestMenuId` 캡처 + `setBundle` functional updater `prev.menu.id !== requestMenuId` skip + `setSummary` `prev.menuId !== requestMenuId` skip + `selectedMenuIdRef.current === requestMenuId`로 에러/로딩 가드). 성공 시 `bundle.step_group_nodes`에서 `(step_group_id === stepGroupId && node_id === nodeId)` 단일 row filter + `summary.groups`의 해당 group `nodeCount - 1`만 targeted update. **`stepGroupCount/totalNodes/ingredientNodeCount/actionNodeCount/tacitItemCount/tacitMediaCount/textTacitCount/pureMediaTacitCount/linkedMediaCount` 전부 불변**(지시서 카운트 불변 제약).
  - Effect 2 (메뉴 전환 reset)에 3줄 추가(`setUnlinkingNodeId(null)`, `setLastUnlinkAttemptNodeId(null)`, `setUnlinkNodeError(null)`) — `setLinkNodeError(null)` 직후.
  - 렌더: drilldown `ingredientNodes.map` + `actionNodes.map` 두 row 블록 모두 기존 span 뒤에 `.metaEditActions` 래퍼 주입(`.metaEditCancelBtn` `× 연결 해제` 버튼 + `.errorText` per-row 에러 슬롯). 버튼 disabled=`unlinkingNodeId !== null`. `g.groupId`는 outer `summary.groups.map` 클로저 캡처. **CSS 파일 수정 없음** — `metaEditActions/metaEditCancelBtn/errorText` 전부 TASK-136 hop delete와 동일 기존 클래스 재사용.
- Phase 4: build pass / lint pass(기존 useGameTick warning 1건만) / @self-reviewer approved 8/8(위반 0, extra_changes 0) / tsc 0 errors / vitest 132/132 pass(adminView 40 + menuView 38 + sessionView 42 + ingredientAdd 6 + phaseAdvance 6).
- 활성화 경로:
  - **Admin drilldown**: `buildStepGroupDrilldown`은 `new Set(bundle.step_group_nodes.filter(sgn => sgn.step_group_id === groupId).map(sgn => sgn.node_id))`로 매 렌더 필터링 — row filter 한 건만으로 drilldown `ingredientNodes`/`actionNodes`에서 자동 소멸. `drilldown.ingredientNodes.length===0 && actionNodes.length===0` empty hint는 기존 로직으로 zero-node 상태 자연 표시.
  - **Unlinked 재노출**: `buildAdminUnlinkedNodeOptions`는 `linked = new Set(bundle.step_group_nodes.map(n => n.node_id))`로 linked 집합 산출 → row filter 후 `linked.has(n.node.id)===false`로 자동 후보 복귀. 재link 왕복 성립.
  - **Read/Engine 계약 불변**: `fetchPracticeMenuBundle`/`menuView`/`sessionView`/engine 수정 없음. `buildMenuStructureSummary` 전역 카운트(`totalNodes/ingredientNodeCount/actionNodeCount/stepGroupCount/tacitItemCount/tacitMediaCount`)는 node/tacit 테이블 직접 카운트 → linkage row 변화에 무관.

## 완료된 것 (선행 세션, TASK-137)
- `queries.ts`:
  - `UpdatePracticeStepGroupMetaInput { stepGroupId, displayStepNo, title, summary, primaryLocationId }` + `updatePracticeStepGroupMeta(input)` 신설. `.from('practice_step_groups').update({display_step_no, title, summary, primary_location_id}).eq('id').select('id, menu_id, display_step_no, title, summary, primary_location_id').single()` → `raise('updatePracticeStepGroupMeta', err.message)` 미러. select 컬럼 순서는 `fetchPracticeStepGroups`와 1:1. **menu_id 갱신 X (불변)**. `updatePracticeMenuMeta` 1:1 미러. helper 경계 확장 X — caller가 정렬·소속·중복·자기 자신 제외 모두 책임.
- `PracticeAdminPage.tsx`:
  - imports: `updatePracticeStepGroupMeta`, `type PracticeStepGroup` 추가.
  - state 3개: `editingStepGroupDraft`(single active row, `{groupId, displayStepNo, title, summary, primaryLocationId}` — '' = null), `editingStepGroupSaving`, `editingStepGroupError`.
  - handler 3개:
    * `handleStepGroupEditStart(g)`: 다른 form(`editingStepGroupDraft !== null || showStepGroupInput || editingStepGroupSaving`) 활성 시 무시. **stale primary_location_id fallback** — `bundle.locations`에 없는 id면 ''로 fallback해 controlled select React 경고 차단. `bundle.menu.id !== selectedMenuId`도 가드.
    * `handleStepGroupEditCancel()`: saving 중 무시. draft null + error null.
    * `handleStepGroupEditSave()`: validation 5단(stepNo 정수 ≥1, title trim ≠ '', summary trim || null, primaryLocationId '' → null 또는 소속 검증, **자기 자신 제외 중복** `g.id !== draft.groupId && g.display_step_no === stepNo`). TASK-128 3중 stale-guard(`requestMenuId` 캡처 + `setBundle` functional updater `prev.menu.id !== requestMenuId` skip + `setSummary` `prev.menuId !== requestMenuId` skip + `selectedMenuIdRef.current === requestMenuId`로 success/catch/finally 가드). 성공 시 `bundle.step_groups`를 map(updated 교체) + `display_step_no` ASC sort. `summary.groups`는 map(updated.id === g.groupId면 `displayStepNo/title/summary` spread 갱신, 카운트 4종(`nodeCount/textTacitCount/pureMediaTacitCount/linkedMediaCount`)과 `groupId` 보존) + `displayStepNo` ASC sort. **`stepGroupCount/tacitItemCount/tacitMediaCount/totalNodes/*NodeCount` 미변경**(지시서 카운트 불변 제약). DB unique race는 catch에서 `editingStepGroupError`로 메시지 그대로 노출.
  - Effect 2 (메뉴 전환 reset)에 3줄 추가(`setStepGroupError(null)` 직후): `setEditingStepGroupDraft(null)`, `setEditingStepGroupSaving(false)`, `setEditingStepGroupError(null)`.
  - 렌더(line ~1985-2027): `bundle.step_groups.map((g) => { ... })` 내부 `editingStepGroupDraft?.groupId === g.id` 분기 — true면 `.metaEditForm` 안에 4 label(`.metaEditLabel`)·input/textarea(`.metaEditInput`/`.metaEditTextarea`)·primary_location select(첫 옵션 `<option value="">(선택 없음)</option>` + `bundle.locations.map`)·`.errorText`·`.metaEditActions`(`.metaEditSaveBtn`/`.metaEditCancelBtn`) 렌더. 분기 false면 기존 read-only row + `editingStepGroupDraft === null && !showStepGroupInput && !editingStepGroupSaving` 조건의 `.metaEditTrigger` "메타 편집" 버튼. "+ 스텝 그룹 추가" 버튼 렌더 조건에 `editingStepGroupDraft === null` 추가(편집 폼 열린 동안 create 진입 차단 — 단일 active form). **CSS 파일 수정 없음** — 기존 클래스만 재사용.
- Phase 4: build pass / lint pass(기존 useGameTick warning 1건만) / @self-reviewer approved 14/14(위반 0, extra_changes 0) / tsc 0 errors / vitest 132/132 pass(adminView 40 + menuView 38 + sessionView 42 + ingredientAdd 6 + phaseAdvance 6).
- 활성화 경로:
  - **Admin drilldown**: `buildStepGroupDrilldown(g.groupId, bundle)`(line 2436)은 매 렌더 호출이고 `setBundle`/`setSummary` 갱신 후 next render에서 fresh bundle로 자동 재계산. groupId 불변이므로 `expandedGroupId` 매칭 유지 — drilldown 펼침 상태에서 같은 그룹 메타 수정해도 헤더 라벨/번호만 갱신, 내부 노드/암묵지/미디어 항목 변동 없음.
  - **Read/Engine 계약 불변**: `fetchPracticeMenuBundle`/`menuView`/`sessionView`/engine 수정 없음. `display_step_no` 변경은 menuView.ts:233(browse), sessionView.ts:96/151(session current/next), adminView.ts:178(summary)에서 모두 자체 ASC 정렬로 자동 정합. fresh fetch로 일관.

## 완료된 것 (선행 세션, TASK-136)
- `queries.ts`:
  - `DeletePracticeNodeLocationPathTailHopInput { nodeId, seq }` + `deletePracticeNodeLocationPathTailHop(input)` 신설. `.from('practice_node_location_path').delete().eq('node_id').eq('seq')` 단일 row delete. `raise('deletePracticeNodeLocationPathTailHop', err.message)` 미러. **helper는 "tail" 개념 비소유** — caller가 tail seq를 계산해 전달하므로 helper 경계 확장 금지 제약 준수.
  - TASK-135의 `createPracticeNodeLocationPathHop` 시그니처/본문 완전 불변.
- `PracticeAdminPage.tsx`:
  - imports: `deletePracticeNodeLocationPathTailHop` 추가.
  - 신규 state 3개: `deletingTailHopNodeId`(in-flight 행, 단일 inflight), `lastTailHopDeleteNodeId`(마지막 시도 행 — 에러 표시 per-row 스코프), `tailHopDeleteError`(단일 슬롯 에러).
  - `handleDeleteTailHop(nodeId)`: 선제 validation(`selectedMenuId & bundle` truthy, target node 존재, `target.location_path.length > 1` — **UI min-row 게이트 재확인**) → `tailSeq = target.location_path[length-1].seq`(TASK-135 hop-append가 이미 seq ASC 정렬 저장). TASK-128 3중 stale guard(`requestMenuId`/`requestNodeId` 캡처 + `setBundle` functional updater `prev.menu.id !== requestMenuId` skip + `selectedMenuIdRef.current === requestMenuId` 최종 상태 변경 가드). 성공 시 `bundle.ingredient_nodes[i].location_path.filter(p => p.seq !== tailSeq)` — 중간 hop 재배열/seq 재번호매김 없음. **setSummary 미호출**(summary는 location_path를 label 문자열 생성에만 사용 — adminView.ts:282,475 근거, 카운트 미보유).
  - Effect 2 (메뉴 전환 reset) 블록에 3줄 추가(`setDeletingTailHopNodeId(null)` + 2).
  - 렌더: 재료 섹션 각 row의 기존 `+ hop 추가` 버튼을 `.metaEditActions` 래퍼로 묶고 `locationPathLabels.length > 1` 조건 하에 `× 마지막 hop 제거` 버튼을 `.metaEditCancelBtn`로 병기. 에러는 `lastTailHopDeleteNodeId === item.nodeId && deletingTailHopNodeId === null && tailHopDeleteError`에서만 해당 행에 표시 — N회 중복 렌더 방지. 버튼 disabled 조건은 `addHopTargetNodeId !== null || deletingTailHopNodeId !== null`(전역 상호 배타, hop-add 버튼 기존 UX와 일관). **CSS 파일 수정 없음**.
- Phase 4: build pass / lint pass(기존 useGameTick warning 1건만) / @self-reviewer approved 14/14(위반 0, extra_changes 0) / tsc 0 errors / vitest 132/132 pass(adminView 40 + menuView 38 + sessionView 42 + ingredientAdd 6 + phaseAdvance 6).
- 3중 min-row 게이트: 핸들러 early-exit, setBundle functional updater 내부 방어, 렌더 `locationPathLabels.length > 1`. DB `trg_locpath_min_row`(DEFERRABLE AFTER DELETE) 위반 불가.
- 활성화 경로:
  - **Admin drilldown**: `buildAdminIngredientNodeList`가 재정렬된 `location_path`를 즉시 재소비. path 길이 1로 축소되면 다음 렌더에서 `× 마지막 hop 제거` 버튼 자동 소멸.
  - **Read/Engine 계약 불변**: `fetchPracticeMenuBundle`, `menuView`, `sessionView`, engine(phaseAdvance/legalActions/pourDryRun) 수정 없음. tail hop 한 row delete로 `location_path[curSeq+1]` 규칙은 그대로 유효.

## 완료된 것 (선행 세션, TASK-135)
- `queries.ts`:
  - `CreatePracticeNodeLocationPathHopInput { nodeId, seq, locationId }` + `createPracticeNodeLocationPathHop(input)` 신설. `.from('practice_node_location_path').insert({node_id, seq, location_id}).select('node_id, seq, location_id').single()` 단일 테이블 insert. `createPracticeStepGroupNode` / `createPracticeTacitMedia` 패턴 그대로 미러. RPC/롤백 없음. 기존 `PracticeNodeLocationPath` type import 재사용.
  - `createPracticeIngredientNodeViaRpc` 시그니처/본문 완전 불변. seq=0 path 생성은 여전히 RPC가 단일 TX로 수행.
- `PracticeAdminPage.tsx`:
  - imports: `createPracticeNodeLocationPathHop` 추가.
  - 신규 state 4개: `addHopTargetNodeId: string | null`(단일 active row 패턴), `hopLocationIdDraft`, `creatingHop`, `hopError`.
  - helper 3개: `resetHopDrafts`, `handleAddHopOpen(nodeId)`, `handleAddHopCancel`.
  - `handleAddHopSubmit`: 선제 validation(selectedMenuId & bundle truthy, target node 존재, location 선택, `bundle.locations` 소속) → `nextSeq = max(existing seqs) + 1`(seq=0 이미 있으므로 ≥1) → `createPracticeNodeLocationPathHop` 호출. TASK-128 3중 stale guard(`requestMenuId` 캡처 + `setBundle` functional updater의 `prev.menu.id !== requestMenuId` 가드 + `selectedMenuIdRef.current === requestMenuId`로 상태 변경 가드) + 추가로 `requestNodeId` 캡처로 node-level 레이스 차단. **핵심 idempotent append 계약**: setBundle 내부에서 `n.location_path.some(p => p.seq === created.seq)` 시 skip — same-menu roundtrip(A→B→A) 중 fresh fetch가 이미 신규 hop을 포함한 경우의 중복 append를 원천 차단(`PracticeNodeLocationPath`는 id가 없고 (node_id, seq)가 PK이므로 seq 기준 비교가 정확). append 시 `[...n.location_path, created].sort((a,b)=>a.seq-b.seq)`로 서버 `order('seq')`와 동기화. `summary`는 어떤 카운트도 변경 없음(지시서 제약).
  - Effect 2 (메뉴 전환 reset) 블록에 4줄 append(`setAddHopTargetNodeId(null)` + 3개 draft).
  - 렌더: 재료 섹션 `ingredientList.map` 각 row 내부(기존 path span 바로 뒤)에 `addHopTargetNodeId === item.nodeId ? form : button` 분기 주입. 기존 CSS 클래스 100% 재사용(`.adminNodeItem`, `.createMenuForm`, `.metaEditLabel`, `.metaEditInput`, `.errorText`, `.metaEditActions`, `.metaEditSaveBtn`, `.metaEditCancelBtn`, `.createMenuBtn`). 버튼 disabled 조건: `addHopTargetNodeId !== null || bundle.locations.length === 0`(단일 active row 보장 + 무-location 방어). **CSS 파일 수정 없음**.
- Phase 4: build pass / lint pass(기존 useGameTick warning 1건만) / @self-reviewer approved 16/16(위반 0, extra_changes 0) / tsc 0 errors / engine 회귀 `ingredientAdd.test.ts` 6/6 + `phaseAdvance.test.ts` 6/6 pass.
- 활성화 경로:
  - **Admin drilldown**: `buildAdminIngredientNodeList`가 `location_path`를 seq ASC로 재정렬·라벨 매핑 — 이번 slice의 local append + 재정렬이 서버 `order('seq')` fetch와 동일 결과를 즉시 표출.
  - **Engine (phaseAdvance, legalActions, pourDryRun)**: `location_path[0]`(initial) + `[curSeq+1]`(next) 규칙이 실제 multi-hop 데이터 위에서 동작 가능해짐. 이번 task는 write path만 담당, 엔진·read path는 수정 없이 자동 소비.

## 완료된 것 (선행 세션, TASK-134)
- `queries.ts`:
  - `CreatePracticeTacitItemInput.tacitType`를 `Exclude<PracticeTacitType,'media'>` → `PracticeTacitType`로 widen. 함수 본문은 insert-select-single 그대로. 이는 주의사항의 "안전한 helper 확장" 경로.
  - `PracticeTacitMediaType` type import 추가.
  - `CreatePracticeTacitMediaInput` + `createPracticeTacitMedia(input)` 신설. `.from('practice_tacit_media').insert({tacit_item_id, media_type, url, sort_order}).select('id, tacit_item_id, media_type, url, sort_order').single()`. select column 순서는 `fetchPracticeTacitMedia`와 1:1 미러. RPC/rollback 없음(단일 테이블).
- `PracticeAdminPage.tsx`:
  - imports: `createPracticeTacitMedia`, `PracticeTacitMediaType`, `PRACTICE_TACIT_TYPES`, `PRACTICE_TACIT_MEDIA_TYPES` 추가.
  - 모듈 상수 2개: `TACIT_TYPE_FULL_LABELS`(adminView의 `ADMIN_TACIT_TYPE_LABELS` spread + `media: '미디어'`), `MEDIA_TYPE_LABELS`(image/video 한글 라벨). adminView.ts 수정 금지 제약을 페이지 로컬 상수로 대체.
  - `tacitTypeDraft` state를 `PracticeTacitType`로 widen.
  - `handleTacitItemCreateSubmit`:
    * validation을 `PRACTICE_TACIT_TYPES.includes`로 전환(기존 `in ADMIN_TACIT_TYPE_LABELS`는 media 제외였음).
    * `isMedia = tacitTypeDraft === 'media'`일 때 body + 6개 sensory 7필드를 **submit 강제 null 정규화**. 사용자가 observe 상태에서 입력한 draft가 media로 전환 후 서버로 새지 않도록 방어.
    * summary targeted update 분기: 공통 `tacitItemCount+1`, `created.tacit_type === 'media'`이면 해당 group의 `pureMediaTacitCount+1`, 아니면 기존 `textTacitCount+1`. 다른 카운트 불변.
  - pure-media(body/sensory) 계약 2중 가드:
    * **UI 숨김** — 렌더 조건 `tacitTypeDraft !== 'media'`일 때만 본문 textarea + 6 sensory input 블록 렌더. media 선택 시 step group + type + title만 노출.
    * **Submit 정규화** — 위 isMedia 삼항. UI와 submit 양쪽 차단.
  - 타입 select가 `PRACTICE_TACIT_TYPES` 5값 전체 + `TACIT_TYPE_FULL_LABELS` 라벨로 바뀜. 헤딩은 "암묵지 (텍스트) 추가" → "암묵지 추가"로 단순화.
  - 신규 state 8개: `showMediaAttachInput`, `mediaTargetStepGroupIdDraft`, `mediaTargetTacitItemIdDraft`, `mediaTypeDraft`(default `'image'`), `mediaUrlDraft`, `creatingTacitMedia`, `tacitMediaError`.
  - `resetTacitMediaDrafts` helper(5 draft).
  - `handleTacitMediaCreateSubmit`: 선제 validation 6단(bundle sync, step group 선택+소속, tacit 선택+소속+group 일치, media_type enum, url trim ≠ '', selectedMenuId truthy). `sortOrder` = `max(existing same-tacit sort_orders)+1` 또는 0 (fetchPracticeTacitMedia `.order('sort_order')` 미러). TASK-128 3중 stale guard 패턴 그대로. 성공 시 `bundle.tacit_media` append + summary **tacitMediaCount+1** + 해당 group **linkedMediaCount+1**만 targeted update — `tacitItemCount`/`pureMediaTacitCount`/`textTacitCount`/`nodeCount`/`totalNodes`/`*NodeCount`/`stepGroupCount` 불변.
  - `handleTacitMediaCreateCancel` helper.
  - Effect 2 reset에 8개 신규 state 추가 + 기존 중복된 `setCreatingTacitItem(false)`/`setTacitItemError(null)` 1쌍 제거.
  - 렌더 섹션: 기존 `.adminTacitItemsSection` 종료 직후, summary `.stepList` 앞에 `.adminMediaAttachSection` + `.adminMediaAttachHeading`. 2단 empty 가드(step_groups 0 / tacit_items 0). 폼은 4 label(스텝 그룹 select, 대상 암묵지 select — 현재 선택된 group 내 tacit을 sort_order ASC로 필터+정렬하여 `${TACIT_TYPE_FULL_LABELS[t.tacit_type]} · ${t.title}` 라벨로 노출, 미디어 타입 select, URL input `type="url"` placeholder). `.createMenuForm`/`.metaEditLabel`/`.metaEditInput`/`.metaEditActions`/`.metaEditSaveBtn`/`.metaEditCancelBtn`/`.errorText`/`.adminEmptyHint`/`.createMenuBtn` 전부 재사용.
- `PracticePlaceholder.module.css`:
  - `.adminMediaAttachSection` + `.adminMediaAttachHeading` 2개만 append (`.adminTacitItemsSection`/`.adminTacitItemsHeading` 1:1 mirror, `var(--...)` 토큰만).
- Phase 4: build pass / lint pass(기존 useGameTick warning 1건만) / @self-reviewer approved 20/20(위반 0, 본 task 범위 내 extra_changes 0) / tsc 0 errors / vitest 120/120 pass (adminView 40 + menuView 38 + sessionView 42).
- 활성화 경로:
  - **Admin drilldown**: `buildStepGroupDrilldown`이 `mediaByTacitId`로 신규 `tacit_media`를 자동 소비, text tacit 카드의 `linkedMedia` + pure-media 카드가 즉시 갱신. media 0개 pure-media는 기존처럼 필터로 숨겨짐(adminView.ts:347) — attach 성공 후에만 등장.
  - **Browse (menuView.ts) + Session (sessionView.ts)**: 수정 없이 `fetchPracticeMenuBundle` 공유로 자동 반영. pure-media browse VM은 title+media만, session current tacit detail은 body/sensory 원본 그대로 반환 — 이 계약에 맞춰 "pure-media = title+media container"가 submit 정규화 + UI 숨김으로 강제됨.

## 완료된 것 (선행 세션, TASK-133)
- `queries.ts`:
  - `PracticeTacitType` type import 추가
  - `CreatePracticeTacitItemInput` + `createPracticeTacitItem(input)` 추가. `.from('practice_tacit_items').insert({...}).select(...).single()` 단일 테이블 insert. `fetchPracticeTacitItems` select 컬럼 순서와 동일하게 미러링. `tacitType: Exclude<PracticeTacitType, 'media'>` 타입 시그니처로 media 선제 차단. RPC/롤백 없음 (단일 테이블, DB CHECK는 서버-side 제약).
- `PracticeAdminPage.tsx`:
  - imports: `createPracticeTacitItem`, `ADMIN_SENSORY_FIELDS`, `PracticeTacitType` 추가
  - state 13개: `showTacitItemInput`, `tacitStepGroupIdDraft`, `tacitTypeDraft` (default 'observe'), `tacitTitleDraft`, `tacitBodyDraft`, 6 sensory drafts (`tacitFlameLevelDraft`, `tacitColorNoteDraft`, `tacitViscosityNoteDraft`, `tacitSoundNoteDraft`, `tacitTextureNoteDraft`, `tacitTimingNoteDraft`), `creatingTacitItem`, `tacitItemError`
  - `resetTacitItemDrafts` helper (10개 draft 리셋)
  - `handleTacitItemCreateSubmit`: 선제 validation (stepGroupId 선택 + bundle.step_groups 소속, tacit_type ∈ ADMIN_TACIT_TYPE_LABELS, title trim ≠ '', selectedMenuId). `sortOrder` 계산: `Math.max(...siblings) + 1` (siblings 0개면 0). body/6 sensory `trim() || null`. TASK-128 3중 stale guard (`requestMenuId` 캡처 + `selectedMenuIdRef.current === requestMenuId` + `prev.menu.id === requestMenuId` / `prev.menuId === requestMenuId` functional updater). 성공 시 `bundle.tacit_items` append + `summary.tacitItemCount + 1` + 해당 `groups[i].textTacitCount + 1` **targeted** 변경 — 전체 `buildMenuStructureSummary` rebuild 없음. `tacitMediaCount`/`pureMediaTacitCount`/`linkedMediaCount`/`nodeCount`/`totalNodes`/`*NodeCount`/`stepGroupCount` 불변.
  - `handleTacitItemCreateCancel` helper
  - Effect 2 reset에 13개 state 추가
  - 렌더 섹션: link-node IIFE 뒤, summary stepList 앞. wrapper `.adminTacitItemsSection` + heading `.adminTacitItemsHeading`. step_groups 0개일 때 "스텝 그룹 먼저 추가" 힌트. 폼: step group select (display_step_no ASC), type select (`Object.keys(ADMIN_TACIT_TYPE_LABELS)` 4값, media 제외), title input, body textarea, 6 sensory input (ADMIN_SENSORY_FIELDS.map으로 field별 명시적 getter/setter 분기). 기존 `.createMenuBtn`/`.createMenuForm`/`.metaEditLabel`/`.metaEditInput`/`.metaEditTextarea`/`.metaEditActions`/`.metaEditSaveBtn`/`.metaEditCancelBtn`/`.errorText`/`.adminEmptyHint` 재사용.
- `PracticePlaceholder.module.css`:
  - `.adminTacitItemsSection` + `.adminTacitItemsHeading` 2개만 추가 (`.adminLinkNodeSection`/`.adminLinkNodeHeading` 1:1 mirror, `var(--...)` 토큰만)
- Phase 4: build pass / lint pass (기존 useGameTick warning 1건만) / @self-reviewer approved 8/8 (위반 0, extra_changes 0) / tsc 0 errors / vitest read helper 회귀 120/120 pass (adminView 40 + menuView 38 + sessionView 42)
- Read path 활성화:
  - **Admin drilldown (즉시, 무조건):** `buildStepGroupDrilldown`이 `bundle.tacit_items`를 `step_group_id` + `tacit_type !== 'media'`로 필터 → 신규 row 즉시 표출.
  - **Browse (/practice/menu, 즉시, 무조건):** `menuView.getTextTacitItems`도 동일 필터 → TacitPreview 섹션에 즉시 노출.
  - **Session (/practice/session, 조건부):** `sessionView.getCurrentStepGroup`/`getNextStepGroup`가 `step_group_nodes` linked group만 대상. `buildTacitDetailViewModel`은 current만. 해당 step group이 node에 linked + 런타임 current/next 조건을 만족할 때만 패널/next preview에 노출 — 이 slice는 데이터 공급, linkage/runtime은 이전 slice가 담당.

## 완료된 것 (선행 세션, TASK-132)
- `queries.ts`:
  - `CreatePracticeStepGroupNodeInput` + `createPracticeStepGroupNode(input)` 추가. `.from('practice_step_group_nodes').insert({step_group_id, node_id}).select(...).single()`. PK `(step_group_id, node_id)` + `UNIQUE(node_id)` + trigger `fn_sgn_same_menu`는 server-side 제약. RPC/롤백 없음 (단일 테이블).
- `adminView.ts`:
  - `AdminUnlinkedNodeOption` 타입 + `buildAdminUnlinkedNodeOptions(bundle)` pure helper. `linked=new Set(step_group_nodes.node_id)`로 제외, `action_nodes` → `ingredient_nodes` 순 수집 후 정렬 `stepNo ASC → nodeType(action before ingredient) → nodeId ASC`. 이름 해석(ingredient displayName)은 bundle에 없으므로 페이지가 후처리.
- `adminView.test.ts`:
  - `describe('buildAdminUnlinkedNodeOptions')` 8 케이스 append (empty / all linked / mixed partial / stepNo 정렬 / action-before-ingredient 동률 / action 필드 매핑 / ingredient 필드 매핑 / nodeId ASC 동률). 40/40 pass.
- `PracticeAdminPage.tsx`:
  - state 5개: `showLinkNodeInput`, `linkStepGroupIdDraft`, `linkNodeIdDraft`, `linkingNode`, `linkNodeError`
  - Effect 2 reset에 5개 포함
  - `handleLinkNodeSubmit`: 선제 validation (groupId ∈ step_groups, nodeId ∈ ingredient ∪ action, 중복 선가로채기 "이미 연결된 노드입니다"). TASK-128 3중 stale guard 패턴 그대로 (`requestMenuId` 캡처 + `selectedMenuIdRef.current` + functional updater `prev.menu.id === requestMenuId` / `prev.menuId === requestMenuId`). 성공 시 `bundle.step_group_nodes` append + `summary.groups[i].nodeCount +1` **targeted** 변경 — 전체 `buildMenuStructureSummary` rebuild 없음. `totalNodes`/tacit counts는 link 행위로 변하지 않음.
  - 렌더 섹션: step groups(L1339) 뒤, summary drilldown 앞. IIFE로 `buildAdminUnlinkedNodeOptions` 1회 호출 + `ingredientNameById`/`locationLabelById` Map 조립. 3분기 empty state (그룹 없음 / 노드 없음 / 모두 링크됨). 폼: step group `<select>` + node `<select>` with `<optgroup label="action"|"ingredient">`. action option 라벨 `"step N · actionType @ locLabel"`, ingredient option 라벨 `"step N · displayName(deco) xQty"`. 기존 `.adminNodeItem`/`.createMenuForm`/`.metaEditInput`/`.metaEdit*` 재사용.
- `PracticePlaceholder.module.css`:
  - `.adminLinkNodeSection` + `.adminLinkNodeHeading` 2개만 추가 (`.adminStepGroupsSection` mirror, `var(--...)` 토큰만)
- Phase 4: build pass / lint pass (기존 useGameTick warning 1건만) / self-reviewer approved 6/6 (위반 0, extra_changes 0) / tsc 0 errors
- 활성화 경로: `menuView.ts`/`sessionView.ts`/`runtime.ts` 미수정. 데이터 공급만으로 summary.groups[].nodeCount, drilldown, session current/next step group tacit panel / next preview가 활성화됨.

## 완료된 것 (선행 세션, TASK-131)
- `queries.ts`:
  - `CreatePracticeStepGroupInput` + `createPracticeStepGroup(input)` 추가. 단일 테이블 `.from('practice_step_groups').insert({...}).select(...).single()` 패턴. `createPracticeLocation` / `createPracticeMenu` 스타일 mirror. RPC/롤백 없음 — 부모·자식 쌍 아님.
- `PracticeAdminPage.tsx`:
  - imports: `createPracticeStepGroup` + `StepGroupCoverage` 추가
  - state 7개: `showStepGroupInput`, `stepGroupDisplayStepNoDraft`, `stepGroupTitleDraft`, `stepGroupSummaryDraft`, `stepGroupPrimaryLocationIdDraft`, `creatingStepGroup`, `stepGroupError`
  - `handleStepGroupCreateSubmit`: validation (stepNo 정수≥1, title 비어있지 않음, summary `trim() || null`, primaryLocationId `''→null` + bundle.locations 소속 검증, 중복 display_step_no 선 가로채기 → "이미 존재하는 단계 번호입니다"). TASK-128 stale-response guard 3중 패턴 그대로 (`requestMenuId` 캡처 + `selectedMenuIdRef.current === requestMenuId` + `prev.menu.id === requestMenuId` / `prev.menuId === requestMenuId` functional updater).
  - 성공 시 `bundle.step_groups` append + `display_step_no` ASC 재정렬 (서버 `order('display_step_no')` mirror). `summary.stepGroupCount += 1` + `summary.groups`에 신규 `StepGroupCoverage{nodeCount=0,textTacitCount=0,pureMediaTacitCount=0,linkedMediaCount=0}` append + displayStepNo ASC sort. totalNodes / *NodeCount / tacitItemCount / tacitMediaCount 불변 (노드·암묵지 신규 0건).
  - Effect 2 reset 블록에 step group 7개 리셋 추가
  - 렌더 섹션: ingredient-nodes IIFE 뒤, summary stepList 앞. wrapper `.adminStepGroupsSection` + heading `.adminStepGroupsHeading`. 리스트 행은 `.adminNodeItem` / `.adminNodeLabel` / `.adminNodeDetail` 재사용. primary_location은 nullable이라 `hasLocations` 게이팅 X — IIFE outer의 `bundle && menu 일치` 만 요구. 폼 primary_location select 첫 option `(선택 없음)` value=''.
- `PracticePlaceholder.module.css`:
  - `.adminStepGroupsSection` + `.adminStepGroupsHeading` 2개만 추가 (`.adminIngredientNodesSection` / `.adminIngredientNodesHeading` 1:1 mirror, var(--...) 토큰만)
- Phase 4: build pass / lint pass (기존 useGameTick warning 1건만) / self-reviewer approved 3/3 (위반 0, extra_changes 0) / tsc 0 errors
- 브라우즈 read path: `PracticeMenuPage.tsx`는 `fetchPracticeMenuBundle` 공유 → 코드 수정 없이 신규 group 자동 반영

## 완료된 것 (선행 세션, TASK-130)
- `supabase/migrations/009_practice_ingredient_node_rpc.sql` **신설**:
  - plpgsql 함수 `create_practice_ingredient_node_with_path(p_menu_id, p_step_no, p_ingredient_id, p_is_deco, p_quantity, p_initial_location_id)` returns table(7 컬럼) 추가
  - 단일 TX에서 recipe_node → ingredient_node → path(seq=0) 3 INSERT → COMMIT 시 DEFERRED T11이 path row 1개 발견 → 통과
  - `return query select ...`로 클라이언트 append용 full payload echo
- `queries.ts`:
  - `CreatePracticeIngredientNodeInput` + `createPracticeIngredientNodeViaRpc` 추가 (코드베이스 최초 `supabase.rpc(...)` 호출)
  - 런타임 shape 검증용 `isIngredientNodeRpcRow` 타입 가드 (unknown → IngredientNodeRpcRow)
  - 성공 시 `PracticeIngredientNodeWithPath` 조립 후 반환 → 호출부가 bundle에 즉시 append 가능
  - `StoreIngredientOption` + `fetchStoreIngredientOptions(storeId)` 추가 (store_id scope, display_name ordered)
- `adminView.ts`:
  - `AdminIngredientNodeListItem` 타입 + `buildAdminIngredientNodeList(bundle)` pure helper 추가 (`buildAdminActionNodeList` 대칭)
  - 정렬: step_no ASC → nodeId ASC, location_path는 seq ASC로 재정렬 후 label 배열 (unknown location은 location_id 그대로 fallback)
- `adminView.test.ts`:
  - `describe('buildAdminIngredientNodeList')` 6 테스트 append (empty / 필드 매핑 / unknown location / 정렬 / authoring-before-group / seq 역순 재정렬)
  - 기존 `makeBundle` `makeLocation` `makeIngredientNodeFull` factory 재사용
  - 32/32 pass
- `PracticeAdminPage.tsx`:
  - ingredient 관련 state 11개 추가: 옵션 소스(3) + 폼(8)
  - **Effect 1b 신규** (selectedStore scope): `fetchStoreIngredientOptions` 독립 fetch. 페이지 wide `error`에 쓰지 않고 `storeIngredientOptionsError` 전용 state에만 기록 → 옵션 실패가 메뉴 리스트/메타 편집/location·action create/drilldown read path를 막지 않음
  - Effect 2 리셋에 ingredient 폼 state 8개 추가 (selectedStore scope 3개는 제외)
  - `handleIngredientNodeCreateSubmit`: stepNo 정수 ≥1, ingredientId ∈ storeIngredientOptions, quantity finite ≥0, initialLocationId ∈ bundle.locations validation. TASK-128 stale-response guard 패턴 그대로 (`requestMenuId` 캡처, `selectedMenuIdRef`/functional updater 2중 가드). RPC 성공 시 반환 row를 `bundle.ingredient_nodes`에 append + `summary.ingredientNodeCount`/`totalNodes` 증분 → 재조회 없음 → "write 성공 + refetch 실패" 애매 상태 부재
  - render 섹션: action nodes 섹션 뒤, summary stepList 앞. IIFE로 list + hasLocations + canCreate 한 번 계산. 단계별 가드:
    - `!hasLocations` → "위치 먼저 추가" 힌트
    - `storeIngredientOptionsLoading` → 로딩 텍스트
    - `storeIngredientOptionsError != null` → 로컬 에러 텍스트 (페이지 전역 미오염)
    - 매장 재료 0개 → "현재 매장에 재료 없음" 힌트
    - 기존 `.adminNodeItem`/`.adminNodeLabel`/`.adminNodeDetail`/`.adminNodePath`/`.createMenuForm`/`.metaEdit*` 재사용, 새 CSS 클래스는 섹션 wrapper 2개만
- `PracticePlaceholder.module.css`:
  - `.adminIngredientNodesSection` / `.adminIngredientNodesHeading` 2개만 추가 (`.adminActionNodesSection` mirror, var(--...) 토큰만)
- Phase 4: build pass / lint pass (기존 useGameTick warning 1건만) / self-reviewer approved 6/6 (위반 0, extra_changes 0) / tsc 0 errors

## 완료된 것 (선행 세션, TASK-129)
- `queries.ts`:
  - `CreatePracticeActionNodeInput` 인터페이스 + `createPracticeActionNode(input)` 추가
  - 두 단계 INSERT: `practice_recipe_nodes(node_type='action', step_no)` → `practice_action_nodes(node_id, action_type, location_id, duration_sec)`
  - 자식 INSERT 실패 시 부모 `DELETE` 롤백. 롤백 실패는 swallow, 원본 자식 에러에 `(cleanup ok | cleanup failed: ...)` 주석을 붙여 `raise`
  - `PracticeActionType` import 추가 (types/practice.ts 변경 없음)
- `adminView.ts`:
  - `AdminActionNodeListItem` 타입 신설 (기존 `AdminActionNodeDetail`은 step-group 스코프 전용이라 `locationId` 없음 — 분리 유지)
  - `buildAdminActionNodeList(bundle)` pure helper: `bundle.action_nodes` 전체를 `step_group_nodes` 참조 없이 매핑, step_no ASC → nodeId ASC 정렬, 알려지지 않은 location_id는 label null
- `adminView.test.ts`:
  - `describe('buildAdminActionNodeList')` 5 테스트 append (empty / 필드 매핑 / unknown location / 정렬 / authoring-before-group / null durationSec)
  - 기존 factory (`makeBundle`, `makeActionNodeFull`, `makeLocation`) 재사용 — 새 factory 생성 없음
- `PracticeAdminPage.tsx`:
  - action node state 7개 (`showActionNodeInput`, `actionStepNoDraft`, `actionTypeDraft`, `actionLocationIdDraft`, `actionDurationDraft`, `creatingActionNode`, `actionNodeError`)
  - `handleActionNodeCreateSubmit`:
    - validation: stepNo 정수 ≥1 (unique 강제 X), action_type ∈ PRACTICE_ACTION_TYPES, location 선택 필수 + bundle.locations 소속 확인, duration 빈 값 → null, 아니면 ≥0 number
    - TASK-128 stale-response guard 패턴: `requestMenuId` 캡처 + `setBundle` / `setSummary` functional updater 양쪽에서 `prev.menu.id === requestMenuId` / `prev.menuId === requestMenuId` 가드 + `selectedMenuIdRef.current === requestMenuId`로 폼 리셋/에러/로딩 가드
    - summary 동기화: `actionNodeCount`, `totalNodes` 증가만 (group 카운트는 step_group_nodes 기반이라 무변경)
  - Effect 2 (menu 선택 변경) 리셋에 action node state 7개 추가
  - render 섹션: locations 섹션 뒤, summary stepList 앞. IIFE로 `buildAdminActionNodeList` + `hasLocations` 한 번 계산. location 0개면 "위치를 먼저 추가한 뒤 액션 노드를 생성하세요." 힌트 + 생성 버튼 숨김. 폼은 number/select/select/number, 기존 `.metaEditInput` / `.createMenuForm` / `.metaEdit*` 재사용
- `PracticePlaceholder.module.css`:
  - `.adminActionNodesSection` / `.adminActionNodesHeading` 2개만 추가 (`.locationsSection` / `.locationsHeading` mirror, var(--...) 토큰만)
  - 행 렌더는 기존 `.adminNodeItem` / `.adminNodeLabel` / `.adminNodeDetail` / `.locationsList` 재사용
- Phase 4: build pass / lint pass (기존 useGameTick warning 1개만) / self-reviewer approved 6/6 (위반 0, extra_changes 0) / tsc 0 errors

## 완료된 것 (선행 세션)
- TASK-128: practice_locations create scaffold (selectedMenuIdRef stale-response guard 패턴 도입)
- TASK-127: practice_menus create scaffold
- TASK-125~126: step-group drilldown 확장 + inline meta editor

## 다음 할 일
- 없음 — practice 트랙은 사실상 마감(TASK-160 기준). 이후는 stop 또는 future bugfix only.
- 신규 feature 추가가 필요하면 별도 지시서로 열 것(step group create/link, node delete/edit, tacit/media authoring 등은 이미 구현 완료 상태이므로 "slice 후보"가 아님).

## 블로킹 이슈
없음

---

## 2026-04-22 — AUTO-HARNESS-001: `.harness/auto/` 자동화 워처 신규 구축

### 핵심 결정
- B1 `.harness/auto/` 서브 네임스페이스로 기존 `.harness/tasks/`·`eval_feedback/`·`manual_verification/`와 공존 (승인받음).
- B3 CLAUDE.md 수정 없음. 워처 공존 규칙은 `.harness/auto/README.md`에만 기재 (승인받음).

### 변경 파일
- 신규: `.harness/auto/watcher.js` (약 390줄, chokidar + spawnSync 기반), `config.json`, `package.json`+`package-lock.json`(`chokidar` 단독 의존성, `type: module`), `README.md`, `prompts/phase_{1..5}_*.txt` 5종, `phase_0/.gitkeep`, `approvals/.gitkeep`, `plans-library/.gitkeep`
- 기존 수정 2건: 루트 `.gitignore`(runtime 격리 16줄 추가), 루트 `package.json`(`harness:start`/`harness:stop` 2 scripts 추가, 의존성 무변화)

### Phase 4 검증 결과
- `npm run build`: pass (217 modules, 7.1s)
- `npm run lint`: 0 errors, 1 pre-existing warning (`useGameTick.ts:52` — 이번 세션 무관)
- `npx tsc -b --noEmit`: 0 errors
- `@self-reviewer`: 1회 통과 — 요구사항 9/9, 금지사항 위반 없음, 추가 변경 없음, 판정 이상 없음
- E1 단위 검증: 아래 "다음 할 일" 항목 참조

### 학습 포인트 (LEARNINGS/MISTAKES는 /session-end 시점 반영)
- `policy_firewall`은 Claude Code pre-tool-use hook이므로 외부 Node.js 프로세스(`watcher.js`)의 `fs.readFileSync`는 영향받지 않음 → 워처가 `.claude/`·`docs/commander/` 읽기 가능.
- chokidar 3대 Windows 옵션 필수: `usePolling: true`, `interval: 500`, `awaitWriteFinish.stabilityThreshold: 500` (Out-File 미감지 이슈).
- Bash tool cwd는 세션 간 유지됨 — `cd`로 이동 후 다음 명령에도 영향. 절대경로 또는 `cd "path" && cmd` 체인 권장.

### 다음 할 일
- E1 단위 검증 시나리오(빈 phase_0 투입, 잘못된 CLI 경로, 복구 루틴 로그)는 워처를 실제 기동해야 하므로 별도 세션에서 sjb 입회하에 수행 권장.
- E2 통합 검증용 저위험 태스크 1건 지정 필요(지시서 §10.8).
- CLI 인증 상태 점검 필요(지시서 §10.9 — `codex`·`claude` 각 1회 ping).
- /session-end에서 LEARNINGS 2~3줄 추가 반영(워처 아키텍처 교훈).

### 블로킹
없음 — 구현 자체는 완료. 실기동 검증만 남음.

---

## 2026-04-22 — AUTO-HARNESS-001-EXT: Phase 4 sub-step 루프 확장

### 요구사항 (사용자 직접 지시)
- Phase 3 Codex가 "분할 실행 필요"로 판정하면 step 계획을 파싱해서 순차 실행.
- 각 step: step_N 지시 + 이전 step 결과들을 Claude에 전달 → `phase_4/<TASK_ID>_stepN.md` 저장 → (선택) Codex 중간 평가 → "계속"이면 다음 step, "중단"이면 에러.
- "분할 불필요"로 판정되면 기존 단일 Phase 4.
- Phase 3 프롬프트 템플릿에 분할 출력 형식 명시.

### 변경 파일 (6개)
- 수정: `.harness/auto/prompts/phase_3_review.txt` (분할 섹션 추가), `.harness/auto/config.json` (`phase4.{allowSplit, midReview, maxSteps}` 추가), `.harness/auto/watcher.js` (parseSplitPlan, extractMidVerdict, runPhase4Split, dispatchAdd 필터), `.harness/auto/README.md` (분할 모드 섹션 추가)
- 신규: `.harness/auto/prompts/phase_4_execute_step.txt`, `.harness/auto/prompts/phase_4_midreview.txt`

### 설계 요점
- **상태 파싱**: `## 분할 실행` 섹션 + `분할 필요: yes/no` + `#### step_N`의 전제 조건/목표/산출물 3라벨. 반각·전각 콜론 모두 허용.
- **이벤트 격리**: chokidar `phase_4` 이벤트에서 `/_step\d+(_review)?$/` 패턴은 Phase 5 트리거에서 제외. 통합 `<TASK_ID>.md` 쓰기만 Phase 5 유발.
- **중단 의미**: 중간 평가 "중단" 시 통합 산출물 미작성 → Phase 5 자동 진입 차단 → sjb 수동 결정 진입.
- **재작업 1회 한정**: mid-review "재작업" 시 동일 step 1회 재실행만. 재평가 없이 다음 step 진행 → 무한 루프 방지.

### Phase 4 검증 결과
- `node --check .harness/auto/watcher.js`: SYNTAX_OK
- `npm run build`: pass (217 modules, 3.63s)
- `npm run lint`: 0 errors, 1 pre-existing warning
- `npx tsc -b --noEmit`: 0 errors
- `parseSplitPlan` + `extractMidVerdict` 단위 테스트 (임시 `c:/tmp/parseSplit_test.mjs`): split 3 step 완전 파싱, no-split 폴백, section missing 폴백, verdict 4케이스 모두 통과 — 검증 후 파일 삭제
- `@self-reviewer`: 1회 통과 — 요구사항 4/4, 회귀 3/3, 금지사항 위반 無, 추가 변경 無. 조건부 통과(경미 개선 권고 3건 중 2건 반영: README에 권장/상한 차이 및 재작업 1회 한정 명시. 3번째는 선택사항이라 유보)

### 남은 사항
- E1 실기동 검증은 여전히 별도 세션에서 sjb 입회 하 수행 권장.
- `{{MISTAKES}}`를 `phase_4_midreview.txt`에도 주입하는 건 선택 개선 — 필요 시 후속 지시 기다림.

### 블로킹
없음.

